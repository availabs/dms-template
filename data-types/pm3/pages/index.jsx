import React from 'react';
import Create from './create';

const pages = {
  defaultPages: ['table', 'map', 'metadata'],
  sourceCreate: {
    name: 'Create',
    component: Create
  }
};

export default pages;
