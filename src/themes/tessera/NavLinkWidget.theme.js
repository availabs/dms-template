// Local fallback for the tessera NavLink widget; the live values come from
// theme.navLinkWidget in tessera-theme-v6.js (mono chrome voice + cobalt button,
// matching the TopNav navitem treatment).
export const navLinkWidgetTheme = {
  plain: 'text-sm font-medium text-gray-600 hover:text-gray-900',
  button: 'inline-flex items-center gap-1.5 text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 rounded-md px-3.5 py-2',
}
