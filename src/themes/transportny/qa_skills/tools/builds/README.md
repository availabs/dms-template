# builds/ — owning build scripts (content-as-code)

One committed, idempotent build script per page: find-or-create by slug, **wipe by PAGE ID with
loud failures** (never by slug — a slug-addressed delete silently no-ops and every rebuild
doubles the page's sections), recreate sections in order. The QA fix loop
(`qa_skills/qa-fix-ticket.md`) patches THESE scripts and re-runs them — never hand-edit a
section a script here owns. Run from the dms-template root with `DMS_AUTH_TOKEN` set
(mint via `src/dms/packages/dms/cli/bin/mint-token.mjs`). Draft-only: none of these publish.

Two lineages (task `planning/transportny/tasks/current/qa-build-scripts-migration.md`):
- **MIGRATED** — human-written scripts moved from scratchpad 2026-07-07, wipe-hardened.
- **GENERATED** — exported from the live draft state by `../page_to_build.mjs` (verbatim
  content-as-code; fidelity-gated: rebuild + re-export identical). Regenerate after intentional
  live/authored changes: `node ../page_to_build.mjs --pattern <p> --slug <s>`.

| Script | Page(s) | Lineage | Fidelity |
|---|---|---|---|
| build_cr_overview.mjs | sitemgmt/overview 2184939 | migrated | rebuilt 2026-07-29 ✓ (landing enrolled; before/after payload diff clean — see note) |
| build_cr_page.mjs | sitemgmt/page 2185886 | migrated | rebuilt 2026-07-07 ✓ |
| build_cr_tickets.mjs | sitemgmt/tickets 2185867 + ticket 2185870 | migrated | rebuilt from HERE 2026-07-07 ✓ |
| build_cr_design.mjs | sitemgmt/design 2186739 | migrated | last run 2026-06-30 — gate before first fix-loop rebuild |
| build_tsmo_home.mjs | tsmo2/home 1431215 — **⚠ DRIFT WIDENED 2026-08-24**: 14 client-batch tickets were fixed SURGICALLY on the draft sections (2193244/47/51/52/53/54/55/56/57/58/59/64/65/71/72/73/77) and are **NOT in this script**, which still hard-throws as STALE. Section 2193247 also changed TYPE (lexical → Card) so the year in the selector caption is live rather than typed. A verbatim export of the current draft is parked at `scratchpad/build_tsmo_home.EXPORT-2026-08-24.mjs` (5 groups / 35 sections; verified to contain every edit and none of the superseded strings) — that file is the backport, ready to be gated and adopted as owner. **Gating it means RUNNING it, which wipes and re-mints every section id**, and the live ticket set + `planning/transportny/qa-process/client-feedback-2026-08-21.md` both cite the current ids — so re-id deliberately, not by accident. | migrated | ⛔ do NOT run — throws; drift is now 14 sections wide |
| build_tsmo2_congestion_v2.mjs | tsmo2/congestion_v2 2175676 — **2026-07-31 (row 2197899): the §04 "Worst corridors" link column now BUILDS its href in SQL** (`'/corridor_view?county=' \|\| meta.county \|\| '&road=' \|\| meta.road \|\| '&direction=' \|\| meta.direction as view_link`). With `searchParams:"none"` the cell value *is* the href and nothing is appended, so all 12 rows had been landing on corridor_view's defaults. `\|\|` is mandatory here — a calc column must contain NO COMMAS, which rules out concat/format/replace. Draft-only; **awaiting publish.** | generated (replaced the stale migrated build_tsmo_congestion.mjs 2026-07-20 — it had drifted 19 vs live 20 sections and was guard-throwing; regenerated from live) | gated ✓ 2026-07-20 (rebuild 20/20 + re-export diff clean) · **parity re-proven 2026-07-31 before the fix run** (`fidelity_static` 20/20 identical), rebuilt 20/20, and 20/20 again afterwards |
| build_tsmo_reliability.mjs | tsmo2/reliability_v2 2180946 | migrated | same caveat |
| build_tsmo_incident_search.mjs | tsmo2/incident_search 2183804 | migrated | same caveat |
| build_fa_home.mjs | freightatlas2/home 2174663 | migrated | **gated ✓ 2026-07-31** — static parity proven BEFORE the run (27/27 sections byte-identical to live draft, group uuids aside), then rebuilt 27/27 for #196 + #200 items 2–3, and parity re-proven after. `fidelity_static.mjs` **cannot** be used here (migrated ⇒ no `const SECTIONS` literal; it grabs the first `[…]` in a comment and dies on `[BACKFILL→Graph]`) — see the migrated-builder parity recipe below |
| build_fa_gallery_about.mjs | freightatlas2/maps_gallery 2174664 + **about 2174665** (slug-swap per #107 done; about_deprecated DELETED 2026-07-13) | migrated | gallery rebuilt DATA-DRIVEN 2026-07-13 per the new design: tiles = live figures from `freightatlas_maps` (2189815/v2189816), 8 category groups, live status chips, `?layers=` deep-links. Gotchas encoded in comments: pageSize REQUIRED with usePagination:false; no literal ' as ' in calc string literals (chr(32) dodge). |
| build_tsmo2_about.mjs | tsmo2/about 2184040 | generated | gated ✓ |
| build_tsmo2_methodology.mjs | tsmo2/methodology 2184101 | generated | gated ✓ |
| build_tsmo2_incidents_v2.mjs | tsmo2/incidents_v2 2181461 | generated — **REGENERATED from live 2026-07-27** after a re-run silently reverted the 07-16 graph fix (`yAxis.tickSpacing` 2000000 → 2) despite a matching 32-section count. Now carries a runtime parity guard + a pointer to `fidelity_static.mjs`, which is the check that catches same-count drift. | gated ✓ 2026-07-27 — static fidelity 32/32 identical to live, rebuild 32/32, re-export diff clean, `AxisLeft` warning gone and graph verified rendering ~5 ticks at 2M |
| build_tsmo2_workzones_v2.mjs | tsmo2/workzones_v2 2182386 — **§04 "Active work zones" REMOVED 2026-07-28 (#180, out of scope): 34→28 sections, 8→7 groups, group indices re-sequenced. Parity proven before the edit (34==34 byte-identical), the 6 live placeholders deleted so the runtime guard could pass honestly (28==28), rebuilt 28/28.** · **2026-07-31 (row 2197900): §01 graph unit fix — SECTIONS[9] `round(sum(vehicle_delay)/1e6,1) as delay_mvh` → raw `round(sum(vehicle_delay)) as delay_vh`, `yAxis.tickSpacing`/`domainMin` dropped, `margin.left` 44→64; SECTIONS[8] label/kicker follow. Patched-and-re-ran (fidelity 28/28 before AND after). See the builder's header block — it records which graph_new yAxis keys are real.** | generated — **REGENERATED from live 2026-07-27** after drifting 28 vs live 34 (it had been marked "STALE — DO NOT RUN" since it regressed the drafts on 2026-07-15/16). Now carries a **runtime parity guard** instead of that hand-written throw: it compares the live draft section count to `SECTIONS.length` *before* wiping and refuses if they differ. | gated ✓ 2026-07-27 — static fidelity (all 34 exported sections byte-identical to live) + rebuild 34/34 + re-export diff clean + guard verified to refuse a deliberately drifted copy without touching the page |
| build_tsmo2_incident_view.mjs | tsmo2/incident_view 2182470 — **2026-07-28 (row 2196812): event-POINT layer added over source 956/view 1947 (always-on, serverSide event_id, `layer-type` MUST stay `""` — `"categories"` triggers a 66k-value domain fetch), hideIfNull notice Card, statewide initialBounds. Backported a live `height` edit ("fill"→"1/3") that fidelity_static caught. Guard gained an `ALLOW_SECTION_COUNT_CHANGE=1` opt-in for intentional structural changes.** | generated | gated ✓ |
| build_tsmo2_corridor_view.mjs | tsmo2/corridor_view 2182912 | generated + hand-fix 2026-07-27 (#103/#159/#181: corridor strip map added to the empty "Compare & map" group — Map section over source 582/view 984, serverSide county/road/direction/**year** filters) | gated ✓ · rebuilt 24/24 2026-07-27 |
| build_freightatlas2_freight_atlas.mjs | ~~freightatlas2/freight_atlas 1411761~~ **STALE — owner retired 1411761 2026-07-16** | generated (460KB map symbology — payloads via temp files) | The sitemgmt `freightatlas2` surface now tracks the SANDBOX pattern 2175436 → page **2189762** / map section **2189767** (config row 2186151). That page is NOT build-owned; its symbologies are edited directly (see `qa-fix-map-symbology-tickets.md`). TODO: regenerate this build against 2189762 (or retire it) so the tracked page is build-owned again. |
| build_npmrds2_map_21.mjs | npmrds2/map_21 1473731 | generated | gated ✓ |
| build_npmrds_macro.mjs | ~~npmrds_sub/macro 2101931 below-fold bands~~ **⛔ RETIRED 2026-08-17 — exits non-zero unless `--force`.** Alex: *"this page should remove everything below the map. Its intended to be a full page map page, the stuff below it was just for design notes."* The three sections it owned (`npmrds-macro-01-head`/`-01-table`/`-footer`) were deleted from the draft and the `Measure reference` + `Footer` bands removed, leaving page 2101931's draft as the **Map section alone** in the single `Map workbench` band (`workbench` + `full_width:'show'`). This **corrects** `npmrds-category-design-set.md` and the mockup header, which classed the below-fold § 01 measure reference as "real page content" — it is design documentation of the panels, exactly like § 02/§ 03. Nothing was lost: the measure vocabulary lives in `measures.js` and renders in the right-hand context panel. Deleted rows backed up at `scratchpad/npmrdsv5-dev2/backups/macro_fullpage_2026-08-17/`. The published `sections` still hold the old 4 — **the owner must publish** for the live page to become map-only. | hand-written, ADDITIVE (never wiped by page id — the Map section's element-data is a 23KB symbology snapshot) | last built 2026-08-12; retired unrun 2026-08-17 |
| build_npmrds_home.mjs | **npmrds_sub/home 2211341** (created 2026-08-12) — the NPMRDS category front door from `dms_design_system_v2/pages/npmrds-home.html`. 4 bands / 28 sections; 7 data Cards: spine extent+observations (CH **583/982**, `fetchMode:'force'`), spine TMC network (CH **582/983**, GROUP BY `year` + `sort desc` + `pageSize 1` so the vintage TRACKS the newest map instead of a literal year), § 02 saved-routes count (DMS-internal **2107426/2107427**, `count(distinct data->>'route_id')` — the catalogue is ~2.26× duplicated on route_id), and the four PM3 KPI cards **cloned verbatim from the live MAP-21 page sections 2173919/20/21/22** (2001/3394 ⋈ 2027/3460) so the two surfaces can never disagree. Also sets **pattern 2100394**'s `theme.navOptions.secondaryNav` to the contract's five flat items (idempotent: backs the row up, writes only on a real diff; never touches `sideNav.size`). Task: `planning/transportny/tasks/current/npmrds-home-page-build.md`. Draft-only; **awaiting publish.** **2026-08-13: § 01 "go straight to a measure" and both § 02 ready-made cards converted from lexical sections to Cards** (still 28 sections) — the mockup's column alignment needs ONE grid shared by all rows, which a lexical paragraph (and a per-row `layout-container`, each its own grid) cannot give. Their cells are `origin:'static'`; row parts carry `isLink` + `valueFontStyle` (**a lexical cell CANNOT be a link** — `CompWrapper` bypasses the ViewComp and prints the raw JSON), and § 02 keeps two true `type:'lexical'` cells each (icon+text eyebrow, chevron). Each such card carries a `selectOnly` `count()` **seed column**: an all-static card otherwise sends an EMPTY attribute list and the server errors on `SELECT data AS data`. **2026-08-13 (2nd pass): vertical-rhythm parity on § 01 / § 02** (still 28 sections). Band heights are a design decision — the mockup's siblings are natural-height-matched by construction — and live descriptions were wrapping onto lines the mockup does not have, which surfaced as blank strips under the doorway CTAs. § 01's band went 328 → 273.7px (mockup 264.5), § 02's 528 → 443px (mockup 408.4). Fixed with **five additive `themev2.js` tokens, no core change**: `proseSMClamp1`/`proseXSClamp2` (`line-clamp-N` — **`truncate` cannot be used in a Card cell**, its `white-space:nowrap` blows the cell out of a `minmax(0,1fr)` track; and **never write `block` beside `line-clamp`**, `display:block` beats `-webkit-box` and the clamp silently no-ops), `proseRowSM`, `kickerXS`, `buttonRow` (kills the phantom `<br>` line lexical emits after a button-only paragraph), and `iconStyles.rowChevron`. Root cause worth remembering: **a link cell's `valueFontStyle` lands on an inline `<a>`, so its `leading` never sizes the line box — the value div's inherited 24px strut does.** ⚠ `cellsVerticalAlign:'stretch'` is a TRAP here: with no definite grid height it sizes every row to the tallest (made a § 02 card 751px), so § 01's row rhythm is set with `cellPadding` instead. **2026-08-14 (3rd pass): horizontal parity on § 01 / § 02** (still 28 sections). Two width defects the vertical pass exposed. (a) **The doorway cards overflowed their own box**: the links row's `md:grid-cols-[max-content_max-content_1fr]` had a 269px min-content in a 209px content box (a grid item's automatic minimum size IS its min-content, so a bare `max-content` track cannot shrink), so the prose was clipped at the card edge and the CTA rail hung 30.4px into the band gutter. Fixed with `minmax(0,max-content)` tracks, dropping the empty `1fr` filler item (3 columns cost TWO `gap-x-4`s), and an additive **`linkMonoXS`** button style at the mockup's own 10px/tracking-wider — the pair then measures 188px instead of 237px and fits on one line. (b) **Description track 105.5 → 118px, ellipsised rows 7 of 8 → 4 of 8**, bought inside the SAME 508.7px grid by cutting the name track 76 → 66px (76 was the mockup's 64px `w-16` box scaled to the 12.5px token; longest name is 41px) and an additive **`unitXS`** at the design-exact 9px (the unit sits in a shared `max-content` track, so `TONS/YR` sized it for all four rows of its half). Two more additive tokens: **`proseSMTrunc1`** = `line-clamp-1 break-all`, which puts the ellipsis MID-WORD like the mockup's `truncate` (`line-clamp` alone breaks at word boundaries and left 44px of empty track), and `buttonRow` reused on the badge + link paragraphs — ⚠ an inline-flex atom under `leading-[0]` **collapses** unless it carries its own line-height. **The mockup's `2 of 8` is NOT reachable by width**: its unit span is per-row `shrink-0` where a Card's is one shared track, so the clip count is a step function of the uniform track (`≥131 → 1 · [130,131) → 2 · [125,130) → 3 · [118,125) → 4`) and even the mockup's own 850px content column lands on 1 of 8. The 802-vs-850 column is `pages.sectionGroup` styles[0] (`w-[302px]` + `gap-10` vs the design's 262 + gap-8) — a shared brand-wide key, escalated, not touched. **2026-08-14 (4th pass): § 04 MAP-21 PM3 rebuilt as ONE combined panel — 28 → 23 sections**, the only run so far that legitimately needed `ALLOW_SECTION_COUNT_CHANGE=1` (used once, knowingly; the guard baseline is now 23 and the three follow-up runs found 23 == 23). § 04 was four *clones of the MAP-21 report page's KPI cards* as four 4-col sections plus a spacer plus a "go to" lexical section: right for the DATA (the surfaces must not disagree), wrong for the PRESENTATION — a report card carries a status pill, a 44px figure, a captioned bar, a delta AND a margin sentence, and **four sections can never be the mockup's one `col-span-8` box**. It is now ONE Card whose cells grid IS the 2x2: `cellsGridSize: 4`, `cellsTracksTemplate: "minmax(0,1fr) minmax(0,max-content) minmax(0,1fr) minmax(0,max-content)"`, two tracks per measure, the bar spanning both, `cellBorderRight`/`cellBorderBottom` drawing the mockup's internal rules, plus a `cellSpan:2` header strip and a `cellSpan:4` lexical footer link row. **Bindings unchanged and provably so**: `kpiSql(cardId, alias, newAlias)` lifts each expression out of the cloned cards BY ALIAS and renames only the SQL alias. The applicable targets are bound too — a `selectOnly` column with an explicit **`normalName`** read via `target_bar`'s **`targetColumn`**, so the tick can't go stale like the report cards' static `targetValue:"75"` (⚠ a `targetColumn` that misses fails GREEN and SILENT: no marker, `meets` defaults true — check the DOM for the marker's `left:%`). Load-bearing CSS fact: **a grid item spanning a FLEXIBLE track contributes nothing to intrinsic track sizing**, which is why the header strip and footer row can span without inflating the figure column. Three additive `themev2.js` tokens: **`labelMD`** (textSettings + dataCard mirror — closes the long-standing measure-name escalation), **`labelMDTrunc1`**, **`statUnitSM`** (⚠ `stat_value` resolves BOTH `valueFontStyle` and `unitFontStyle` against **textSettings**, not the dataCard mirror). PHED: figure bound at the design's M-scale (same `sum("phed")`), **no bar/tick because 2027/3460 has no phed target column at all**, and its delta now BOUND (`lag() over (order by ds.year_record)`) — the mockup's invented red bar/tick was removed and its `—` delta corrected. Band 260.6px vs the corrected mockup's 258. New offline flag: **`SECTIONS_DUMP=<index>`** prints one section's element-data and exits without touching the live page. **2026-08-14 (5th pass): the § 02 cards' trailing white strip, fixed in CORE (23 sections, unchanged).** The two ready-made Cards now carry **`cellsVerticalAlign: "stretch"`**, whose meaning changed the same day: `Card.layout.js` `resolveCellsGridStyle` emits **`align-content: stretch`** (CSS Grid §12.9 — free space divided equally among `auto`-max tracks) instead of `gridAutoRows: minmax(max-content,1fr)`, which *equalized* every row to the tallest row's max-content and was what took this card to 751px. Not a section-sizing bug: the two section boxes are already equal at every width and the cells grid already fills the card — only the row-axis distribution was wrong. Footer-to-card-bottom gap **43px → ≤1 at all of 1280/1366/1440/1480/1560/1600/1680/1760/1800/1920/2048, with card heights identical at every width**. ⚠ It is **deliberately NOT set on § 01 or § 04**: § 04 has zero slack at every width, and on § 01 the equal share comes out of the 49px header strip (→ 56px at 1440, 60.5 → 80.1px at 1280) — that panel needs the still-unbuilt "one row absorbs it all" (`mt-auto`) knob. Unset is byte-identical, so no other Card anywhere moved. **2026-08-14 (6th pass): two more CORE fixes, and § 01 re-authored on them (23 sections, unchanged).** (a) **Link cells now honour their token's `leading`** — the root cause recorded in the 2nd pass above is fixed rather than worked around: `Card.jsx` blockifies the `<a>`/`<Link>` (`resolveLinkAnchorStyle`) **unless the token declares its own display**, so `btnPrimary`/`btnOutline`/`toggleOff` keep `inline-flex` and `line-clamp-*` keeps clamping, while a `text-[12.5px]! leading-[1.55]!` link cell finally measures **19.4px/line instead of 24**. § 01's rows 55 → 50.4. ⚠ `inline-block` does NOT fix this (an inline-block is still inline-level, so the containing block's strut still wins — measured 24 → 24) and it *breaks* clamped cells. ⚠ Second-order effect worth knowing site-wide: the old 24px strut was silently giving every part of a row the SAME line box, which was aligning differently-sized cells by accident — `measureCells()` now re-centres its three parts with explicit top padding (+1.5 name, +3 unit). (b) **§ 01's slack is absorbed without inflating its header strip** via the new **`display.cellsRowsTemplate`** (raw `grid-template-rows`, the row-axis peer of `cellsTracksTemplate`): `'max-content'` makes row 1 an explicit content-sized track, which CSS Grid §12.9 skips, so `cellsVerticalAlign:'stretch'` spreads the leftover over the measure rows only. Header **60.5/60.5/49/49** at 1280/1366/1440/1480 (unmoved; plain `stretch` inflated it to 83.8/·/59.7/52.8), rows land at 55.2 vs the mockup's 55.9, slack **0** at every width. `'max-content … 1fr'` writes the mockups' `mt-auto` with the same key. ⚠ Adding a display key takes TWO edits — `Card.jsx` re-assembles a *curated* `display` literal for `resolveCellsGridStyle`, so a key that is only added to the resolver reads `undefined` forever and ships inert with no error (this one did, once); `cardLayout.test.js` now has a source-scan guard for it. The clamp tokens were re-measured and **kept** — their vertical job is gone, their horizontal one is not (4 of 8 § 01 descriptions still wrap without them). **2026-08-14 (8th pass): § 01's row content is CENTRED inside its rows, in core (23 sections, unchanged).** The 6th pass left two residuals that were one missing primitive — a stretched cell top-anchors its content, so a row that absorbed slack held 16.3px of text with **15px above and 47.4 below** at 1280, and the three parts of a row (tokens 16.25/19.4/13.5) no longer shared a line box, which `measureCells()` was patching with two hand-computed top-padding bumps (+1.5 name, +3 unit). Both are now `display.cellsContentVAlign: 'center'` (new key, per-column peer `cellContentVAlign`, values top|center|bottom): **15/47.4 → 30.7/31.7** at 1280, **15/23.0 → 18.5/19.5** at 1480, nothing at 390 (no slack to spend), part midlines **−1.6/0/−3.0 → 0.0/0/0.0**, and **both padding bumps deleted**. It emits **no `align-self`**, which is the whole point: the rules stay **5 continuous lines** (`align-self:center` collapses the cells 81.7 → 47.3/50.4/44.5 and yields 13 stubs), and the row template, band height, tracks and cell count are byte-identical — the content moved inside the rows and no box changed size. ⚠ The property is picked from the cell's ACTUAL flex direction (`resolveCellFlexRow`), NOT from `isRowLayout`: § 01 leaves `headerValueLayout` unset and transportnyv2's `headerValueWrapper` bakes `flex flex-col`, so `isRowLayout`'s "unset ⇒ row" would have emitted `align-items`, which measured top-anchored AND horizontally shrink-wrapped (42 → 29px). **2026-08-14 (7th pass): the four doorway cards converted from lexical sections to Cards, and the rail's two cards squared up (23 sections, unchanged).** The mockup's doorway CTA is a **sibling** of the `flex-1` content block, so it is pinned to the card's bottom edge and runs full-bleed into the card's rounded corners; a lexical section has ONE top-anchored flow, so live the rail sat wherever the copy ended (16px above the bottom edge on § 01/§ 03, 96.4 on § 02, 42.7 on § 04) and 209.3px wide inside a 243.3px card. As a Card it is 5 cells (7 on § 02) in a **one-track** cells grid with **`cellsRowsTemplate: 'max-content … 1fr max-content'`** — the row above the CTA absorbs the leftover height (the mockup's `mt-auto`), the CTA row stays `h-11`. **CTA gap to the card's bottom edge: 0px at every width 390→2048**, on all four. Full bleed = `cellPadding: 0` + four additive `dataCard` tokens **`ctaRail`/`ctaRailBlue`/`ctaRailSlate`/`ctaRailGold`** (cell peers of the `button` styles `rail*`) carrying `w-[calc(100%+2px)] -mx-px -mb-px rounded-b-[7px]`: the +2px/negative margins bleed over the **always-on `border border-transparent`** every v1 Card cell ships, and the 7px is the card's INNER radius (8px border box − 1px border), so the rail's corners follow the card's without any `overflow-hidden`. ⚠ **The explicit width is not optional**: a cell's value div carries `justify-items-start` (theme `justifyTextLeft`) and current Chromium implements Box Alignment in **block** layout, so `justify-self` reaches every block-level child and shrink-wraps it — a bare `<div>` injected into a value div measures **8px** inside a 239.3px box, and the CTA anchor rendered 152.2px until the width was added. (Same cause as `chip`'s long-standing `w-full!`.) Cells kept lexical: the tab badge (inline pill atom), icon shield + title, the deep-link row, § 02's chip row — prose and CTA are plain static cells (**`proseSMInk`**, a fifth additive token, because the dataCard mirror's `proseSM` is slate-500 where the design and textSettings are slate-600). § 02's `hr` + eyebrow + chips became a `cellBorderTop` eyebrow cell + a chip cell; ⚠ a cell border is a border-BOX edge, so that rule is full-bleed where the mockup insets it by `p-5`. **Heights, § 8.1 with § 8.2's font correction applied** (the mockup renders in Source Sans 3, the live app in Proxima Nova — measured **890 vs 940px** natural on § 01's prose, so the mockup's own 227.3px box would take 5 lines, not the 4 it draws): § 01 **281.7 vs 283.9** (−2.2), § 03 281.7 vs 283.9 (−2.2), § 04 260.6 vs 258 (+2.6), § 02 443 vs 408.4 (+34.6, the pre-existing § 02 width residual, untouched by this pass). Against the mockup as-drawn § 01 reads +17.2 — quote the corrected number and show the derivation. Also fixed here: **the two rail cards are now the same width**. `InPageNav` is not a section so it paints straight onto the 294px rail column, while the Documentation card IS one and also paid the section wrapper's `p-3` — 270 vs 294. One `padding: { left: '0', right: '0' }` on that section (**x only** — the y padding is the gap between the two rail cards) makes both 294 at every width ≥1280. **2026-08-18 (9th pass): § 01's eight measure rows are DEEP LINKS (23 sections, unchanged).** Each row's three cells carry `isLink` + `searchParams:"none"` + `location`, and `location` is now per-measure: `/macro?measure=<key>` for the five measures pm3 computes, plain `/macro` for the three it does not (Freeflow · CO₂ · Network). With `searchParams:"none"` the cell's `location` IS the href and nothing is appended, so a query string passes through intact (`reference_dms_link_searchparamscol` — the same mechanism build_tsmo2_congestion_v2 uses to build hrefs in SQL). **The availability flag is imported, not restated:** the builder reads `MEASURES` from `src/themes/transportny/components/macroview/measures.js` (import-free by design, so a node script can read the same record the plugin's measure select reads) and a preflight REFUSES to build if § 01 names a measure key the plugin doesn't have — a typo would otherwise ship eight identical `/macro` links that look correct. Every run now prints the eight hrefs. The URL contract itself is owned by the plugin (`macroview/urlState.js` + page 2101931's `filters` registry) — see `creating-a-map-section.md` § 7c. | hand-written (this lineage's first script that ships its OWN parity guard: `existing.length !== SECTIONS.length` ⇒ refuse, `ALLOW_SECTION_COUNT_CHANGE=1` to override) | built + re-run clean 2026-08-12 (28 → wipe 28 → 28; guard exercised on the second run); re-run 4× on 2026-08-13 for the Card conversion and 4× more for the vertical-rhythm pass, and 4× on 2026-08-14 for the horizontal-parity pass, always 28 → 28 (guard never overridden). **2026-08-14 § 04 pass: 28 → 23** — `ALLOW_SECTION_COUNT_CHANGE=1` set ONCE for the deliberate structural drop, then 23 → 23 on three further runs; **3 more runs 2026-08-14 for the 6th pass, 23 → 23, `(23 deleted, 0 orphaned)` each**; **1 run 2026-08-14 for the 7th (doorway-Card) pass, 23 → 23, `(23 deleted, 0 orphaned)`**; **1 run 2026-08-14 for the 8th (content-v-align) pass, 23 → 23, `(23 deleted, 0 orphaned)`**; **1 run 2026-08-18 for the 9th (§ 01 deep-link) pass, 23 → 23, `(23 deleted, 0 orphaned)`, published `sections` 2213575–2213597 byte-identical before/after**; every run printed `(N deleted, 0 orphaned)` with `DMS_AUTH_TOKEN` exported. `fidelity_static.mjs` does **not** apply — no `const SECTIONS = [...]` *literal* (the array is composed from helpers), so use the migrated-builder parity recipe below, or the offline harness the task doc records. |
| build_npmrds_reports.mjs | **npmrds_sub/converted_reports 2188366** — the NPMRDS Reports page from `dms_design_system_v2/pages/npmrds-reports.html`, now **Revision 3 (2026-09-02)**: the page is the templates band and nothing below it — header (title · `Templates | All reports` view toggle · `ChooseReportButton` search bar · `CreateReportButton` · New route) → § 01 templates → footer. **3 bands / 17 sections.** Task: `planning/transportny/tasks/current/npmrds-reports-page-rev3.md` (revision 2's build + measurements: `npmrds-reports-page-build.md`). **REV 3 (2026-09-02):** § 02 Your reports (5), § 03 Worked examples (2), § 04 The finder (2), the § 01 route CTA and the header's freshness card + search-count Card are gone; added the view toggle (a Card of two static cells in a gap-0 max-content cells grid, `viewTabOn`/`viewTabOff` additive tokens; "All reports" INERT until the list page exists — decision D2) and the `ChooseReportButton` section at size 4 (it reads `?search=` off pageState.filters and binds its own counts via a UDA length request). Header sizes 2+2+4+2+2, budgeted from the mockup at real Oswald metrics (toggle 175px · New report 112 · New route 108.7): the toggle is size 2 only with both x-gutters zeroed. New route → `/route_creation` (2216258) with the additive `btnOutlineLG` (h-10). All-static Cards carry a `selectOnly` `count(1)` seed. **The rev-2 find-a-report modal section group (Filter + result Card + foot Card behind `isModal`/`find`) is REMOVED** at the owner's request the same day — nothing published its action param once the search bar opened ReportPickerModal, and in /edit it rendered inline as three dead sections under the footer. Also from that round: `CreateReportButton.theme.js`'s wrapper is now `flex w-full flex-col items-end` (the button right-aligns in its section, site-wide — 2208581 verified), and the title section zeroes its top gutter so the 26px title centres on the 40px controls row. The builder now PINS `PAGE_ID = "2188366"` and refuses rather than creates when the slug lookup does not resolve to it: `dms page list` defaults to 50 rows and `npmrds_sub` crossed 50 pages on 2026-09-02, at which point the old find-or-create would have minted a duplicate page (`page_to_build.mjs` and `cr_sync.mjs` got the same `--limit 1000`). Rev-2 description kept below for the bands that remain. **THE TEMPLATE SHELF** re-houses 2208581's five `reports_snap_2` Cards (**2177438/2177440**, `env npmrdsv5+reports_snap_2`, `srcEnv npmrdsv5+datasets`, isDms) **with their `tags` filters lifted verbatim** — before_after · floating_car · events · change_over_time · behavioral — as 12 template cards. The mockup draws the three narrow groups' sub-heads INSIDE the group box; a Card's static cells repeat per record, so a shared group head is not a cell — they are **sibling sections instead**, three heads on one row (3+6+3) over three Cards on the next, which lands the columns in the same place with nothing nested. The two wide groups are a full-width lexical head over ONE Card at `cardsGridSize: 4`. **THE MODAL** is a `draft_section_groups` entry with `isModal: true` + `modalParamKey: 'find'` + `modalSize: '4xl'` holding a `Filter` (the `like` search control) + a result Card + a foot Card; the header trigger is a Card static cell with a `click_publish` provider on the same key. **Verified in view mode 2026-08-19** (the page was published from the admin UI by the owner mid-build, which is what made view-mode verification possible): closed ⇒ 0 overlays, trigger click ⇒ 1 `fixed inset-0 z-50 … bg-black/50` overlay holding the control + rows, ✕ ⇒ 0, backdrop click ⇒ 0. `?search=bridge` cold-loads with the control pre-filled, the list filtered and the CLOSED trigger reporting **130 matches** (the count Card carries the same OR-group filter tree — the design's answer to "action params never reach the URL"). **Five gotchas encoded in comments, all measured here.** (1) **A `notempty` leaf + `usePageFilters` defaults ON, not off**: `applyPageFilters` (buildUdaConfig.js:475) early-returns when the page-variable map is empty and it is the ONLY pass that disables a unary leaf, so two "off" facet chips silently cut the finder from 1,574 rows to **19**. The two facets were dropped; a STATIC `notempty` leaf (no `usePageFilters`) is safe and is what excludes the 71 name-less rows. (2) **A `notempty` column in a `Filter` section renders as a NUMBER INPUT**, not a toggle. (3) **A calc column's string literals must not contain " as "** any more than a comma — the alias parser splits on the LAST occurrence, so `else 'rebuilt as a page' end as rebuilt_state` came back keyed on the expression with the alias eaten and the cell rendered blank (same dodge as build_fa_gallery_about's `chr(32)`). (4) **An `isLink` cell ALWAYS renders its anchor and the href is `location \|\| value`** — binding `page_path` straight through emitted `…/undefined` on every one of the 1,560 legacy rows; the fix is a `case when … then '' else … end` calc so the cell renders nothing when there is no page. (5) **Sorting `updated_at` desc puts NULLs FIRST** (Postgres' default for DESC) — 171 rows have none and two store a serialised weekday object — so the finder's default state was six blank cards; the sort key is now a regex-gated calc. **Parity after P7 (2026-08-20)**: § 8.1 § 01 band **954.9 vs 950.4 (−4.5)**, header 81 vs 85.8, footer **51 vs 60.5 (+9.5, was +23)**, § 04 410.4 vs 395.8; § 8.2 band content column **1144 vs 1144 exact** AND every full-width section's content box now **1144 too** (was 1120 — see the band-edge pass below), § 01's 3-card row spanning the mockup's own x 304 → 1448; hOverflow 0 at 1480 and 390. The § 01 card is `cardStyle: 'tile'` — the theme already carries the mockup's exact shell (`rounded-[8px] border border-zinc-950/10 bg-white shadow-sm … hover:border-[#37576B]`) AND zeroes `theme.value`'s `px-3 pb-3`, which was charging every card 60px of dead padding; `display.cardBorder` alone paints a border with NO radius because this theme's `subWrapperCompactView` is deliberately empty. **Eight additive `themev2.js` tokens, no core change**: `displayMDCaps` (26px uppercase — the ladder had no rung between displayLG 38 and displayMD 28), `proseSMClamp2`, `metaXSLink`, `plateEmpty` (patterns.html §14's "no preview" tile), `pill.chip_meta`, plus `btnPrimary`/`btnOutline`/`displayMDCaps` parity entries in the dataCard mirror, and from P7 `textSettings.kickerSM` (the mockup's group-head numeral WITH an explicit leading — `kicker` has none, and a lexical section's wrapper sets an ABSOLUTE `leading-[22.4px]` that inherits, so a one-line kicker measured a 22.4px line box against 15.75) and `dataCard.proseSMClamp1` (`proseSMClamp2`'s one-line sibling — the mockup gives the search trigger's prompt `truncate` and a Card value cell has no truncation knob, only `wrapText`), and `button.linkMonoRow` (`linkMono` + `mr-6` — the footer's link row must stay inline siblings in ONE paragraph because that is the only primitive that reflows like the mockup's `flex flex-wrap`; a layout-container item per link cost +65.9px at 390 — and a lexical button node has no margin knob, so the 24px gutter lives in the button style. NO `last:mr-0`: the ButtonNode wraps each button in its own element, so `last:` matches every one). **P6 (2026-08-20) — the real `CreateReportButton`.** The header is FIVE sections (title 2 · trigger 4 · freshness 2 · **CreateReportButton** 2 · New route 2) instead of three: the component is a theme `pageComponent`, i.e. an element type (`element-type: "CreateReportButton"`, `element-data: "{}"`), not a Card cell, so it needs its own section — which is why the count went 28 → 30. It replaces the static "New report" link that pointed at 2208581. That indirection existed because core's `newPage` derives the new page's parent from `item.parent` with no override and 2188366's `parent` is `''`; the component now falls back to the host page's own id, which is byte-identical wherever `item.parent` is set (`newPage` reads `item` at exactly one place). Verified by creating a report from it — `parent: "2188366"`, four sections cloned from template **2187021** — then deleting the page and, because `page delete` does not cascade on this server, its four section rows too. **P7 (2026-08-20) — the BAND-EDGE GUTTER PASS is the one structural thing to know here**: on a `gap-0` band grid the section wrapper's padding IS the gutter, so the theme's 12px-per-side default also pads each ROW'S OUTER EDGES — every full-width section measured 1120px inside the band's own 1144px column. A derived pass at the bottom of `SECTIONS` zeroes only the outer side of each row's first/last section (rows are computed from the sizes, so a later size change cannot leave an edge padded); internal gutters stay 24px, matching the mockup's grid gap. The modal band is excluded. Also from P7: `stat_value`'s built-in unit class is a RELATIVE `text-[0.4em]` and rendered three units at **4.2px** — always set `unitFontStyle` beside `valueFontStyle`; and the footer's `plain` button style is 13px Proxima on a fixed `h-9`, which is what cost the footer band +23px (`linkMono` is the no-chrome mono variant). **NOT built, all escalated in the task doc**: the preview plate (stored thumbnails are 50×50px; the layout-derived shape needs a new column type — the cards keep the 1/4-width tile column so a plate cell is a one-column swap), § 03's four worked examples (**pages 2194949/2192364/2192451/2191095 no longer exist** — the band states the gap rather than shipping four dead links), the whole-card anchor, the routes CTA's href-less "New route" button, and `cardsGridSize`'s non-responsiveness at 390. | hand-written (carries the parity guard: `existing.length !== SECTIONS.length` ⇒ refuse, `ALLOW_SECTION_COUNT_CHANGE=1` to override; plus offline guards for nested layout-containers, lexical cell docs, and commas in isDms calcs) | built 2026-08-19, extended 2026-08-20 (P6/P7); 10 runs, every one `(N deleted, 0 orphaned)` with `DMS_AUTH_TOKEN` exported; `ALLOW_SECTION_COUNT_CHANGE=1` used twice — the deliberate 29 → 28 drop when the facet Filter was removed, and the 28 → 30 header split in P6. Guard baseline was **30** through rev 2. **2026-09-02 rev-3 run:** the pre-run CONTENT diff (offline harness — the script up to `// APPLY` printing `{GROUPS, SECTIONS}` — against a `page_to_build.mjs` export) found live 30 == 30 but **27 of 30 sections differing**: the live draft had been re-minted from the published set (P7's band-edge gutters, `kickerSM`, `metaSM` units, gold period, `linkMonoRow`/`metaMD` footer all absent) and carried three 2026-08-25 hand edits (a `ChooseReportButton` section, New route re-pointed to `/route_creation`, `height:"fill"` on CreateReportButton). Classified and recorded in the task file; the hand edits were folded into the script, P7 restored by the rebuild. Then `ALLOW_SECTION_COUNT_CHANGE=1` for the deliberate 30 → 20 drop: `(30 deleted, 0 orphaned)`, `created 20`, re-export vs script **20/20 sections + 4/4 groups identical** (JSON key order aside); and once more the same day for 20 → 17 when the modal group was dropped: `(20 deleted, 0 orphaned)`, `created 17`, re-export vs script **17/17 + 3/3 identical**, re-checked again after the /edit screenshot pass. Guard baseline is now **17**. `fidelity_static.mjs` does not apply (no `const SECTIONS` literal — the array is composed from helpers). |

**⚠ EXPORT `DMS_AUTH_TOKEN` BEFORE RE-RUNNING ANY BUILDER THAT WIPES (2026-08-14).**
Reads are anonymous, but `dms section delete` 500s with *"Authentication required to delete
items"*. The wipe step is best-effort, so **the run still succeeds and the page is still
correct** — it just prints `wiped N draft sections (0 deleted, N orphaned)` and leaves the old
rows in the pattern. Six tokenless re-runs during the 2026-08-14 npmrds-home pass added **168**
orphaned `npmrds_sub|component` rows (ids 2212248–2212421; the live draft is 2212450–2212477 and the published 28 are 2211448–2211475, both untouched) to a pattern that already carried
~3,082 from earlier rebuilds; the run with the token printed `(28 deleted, 0 orphaned)`.
The CLI reads `DMS_AUTH_TOKEN` (`cli/src/config.js`); mint one with `cli/bin/mint-token.mjs`
(no `--out`/`--origin` — those are for Playwright storageState):

```bash
DMS_AUTH_TOKEN=$(node src/dms/packages/dms/cli/bin/mint-token.mjs \
  --host https://dmsserver.availabs.org --project <app> \
  --email availabs@gmail.com --password test123) \
DMS_HOST=https://dmsserver.availabs.org DMS_APP=<app> DMS_TYPE=<type> node <builder>.mjs
```

**Always read the wipe line.** `(N deleted, 0 orphaned)` is healthy; `(0 deleted, N orphaned)`
means you were anonymous. Note the tension with the drift note below: orphaned rows are also the
*recovery path* for live edits a rebuild clobbered, so don't bulk-delete a pattern's orphans
without checking that nothing in them is worth lifting.

**⚠ LIVE-EDIT DRIFT — check CONTENT, not section counts (2026-07-27 incident).**
`build_tsmo2_congestion_v2.mjs` was generated 07-20; two fixes (#156, #158) were applied on 07-21 by
editing the **live sections**; re-running the script on 07-27 silently reverted both. Section counts
matched (20 vs 20) because the drift was *inside* existing sections' column configs, so the count-parity
check passed. Restored 07-28 from the orphaned 07-21 published rows.

Before re-running ANY script here: re-export live (`node ../page_to_build.mjs --pattern <p> --slug <s>
--out /tmp/x.mjs`) and **diff against the committed script**, and check the ticket table for fixes whose
`resolved_date` is after the script's generation date on that `page_key`. If it happens anyway: the
pre-rebuild published section rows survive as orphans — `dms raw list "<app>+<pattern>|component"
--limit 2000` (plus `--offset`), grep for the lost config, lift `element-data` verbatim, fold it into
the script.

**Armed right now:** `build_tsmo_reliability.mjs` (migrated, ungated) predates **#165**'s 07-21 live fix
(map legend title no longer truncated to "W..") — re-running it as-is will revert that. Diff before use.

**MIGRATED builders can't use `fidelity_static.mjs` either — but they can still be parity-proven.**
The 9 migrated scripts have no `const SECTIONS` literal, so the tool matches the first `[…]` in the
file (a comment) and dies with `SyntaxError: Unexpected token 'B', "[BACKFILL→Graph]"`. **That crash is
not a licence to run the builder blind.** Recipe used for `build_fa_home.mjs` on 2026-07-31: copy the
script's source up to its `// ── apply` marker into a scratch harness, append the *same* payload
construction the apply loop does (`{size, group, title, element:{…}, border?, height?}`) and print it as
JSON — i.e. build what the script WOULD create without creating it — then diff each payload against the
live `draft_sections[i]` row using `fidelity_static.mjs`'s own `strip`/`denoise` normalisation, plus two
tweaks the migrated shape needs: parse `element-data` (a JSON *string*) before comparing so
re-serialisation whitespace doesn't false-alarm, and compare `group` as a **bijection** (exported uuid →
live uuid) rather than literally, since migrated scripts `randomUUID()` their band names on every run.
Same verdict semantics: all sections identical ⇒ running the script is provably content-neutral.

**GENERATIVE builders can't use `fidelity_static.mjs`.** It parses a `const SECTIONS = [...]` literal, so
it only works on scripts produced by `page_to_build.mjs`. The four `build_cr_*.mjs` scripts *compose*
their sections from live data, so use a **before/after payload diff** instead: dump the draft sections,
run the builder, dump again, and compare payloads grouped by their distinguishing key (for the overview,
the `surface` filter value). Pre-existing groups must come back byte-identical; anything else is either
an intended data-driven counter change or a regression. Worked example — the 2026-07-29 landing
enrolment: `tsmo2`/`freightatlas2`/`npmrds2` blocks byte-identical, only the header counter text moved
(`3 patterns · 19 pages` → `4 patterns · 20 pages`) plus the new `landing` block. **That diff earned its
keep**: it caught the overview's identity card printing `t.pattern` — which is the raw pattern **id**
`2175436` for the Freight Atlas row — so a client-facing card would have read "2175436 · 6 pages".
Fixed to key on `t.surface`. Two `sitemgmt_pages` reads also silently returned NOTHING until the CLI got
`DMS_AUTH_TOKEN`: the `sitemgmt` pattern is auth-gated, and an unauthenticated `raw get` on its pages
returns empty rather than erroring — always pass the token when touching control-room pages.

**⚠ VIEWING A PAGE IN `/edit` MUTATES ITS SECTIONS — it is itself a drift source (2026-07-28).**
Opening `/edit/<slug>` on a build-owned page makes the editor re-save the sections it renders, so a
builder that was byte-identical to live can fail `fidelity_static.mjs` minutes later with nobody
having authored anything. Two flavours, and only the second matters:

- *Harmless, now normalised by `fidelity_static.mjs`*: an unset page-variable leaf's `"value": []`
  comes back as `"value": [""]` (behaviourally identical — `buildUdaConfig` drops all-empty
  filter/exclude leaves, which is also what stops them compiling to `IN ('')` and blanking the
  section), and an empty `join: {"sources": {}}` is added.
- *Real config drift, still reported*: `useDataSource`'s runtime reconcile rewrites
  `externalSource` — `baseUrl` blanked, `isEditable` added, and every column def reshaped
  (`display_name` → `display: ""`, `"INTEGER"` → `"integer"`). Observed on incidents_v2 sections
  2196881/2196886 after a verification pass. The page still renders, but the committed builder and
  live have diverged — the same condition that let the 07-16 graph fix get reverted.

Practical rules: **verify page changes on the PUBLISHED view, or via the CLI, not `/edit`**, and if
you must open `/edit`, re-run the builder afterwards to restore parity (it is content-neutral when
fidelity passes) and re-check. Open question worth deciding once: adopt the editor-normalised
`externalSource` shape into the builders (regenerate, so the drift stops recurring) or keep
re-running to restore the authored shape — nobody has established whether the blanked `baseUrl`
matters.

**Fidelity gate** (mandatory before a script's FIRST fix-loop rebuild if flagged above): baseline
`qa_assess.mjs` + section count → run the script → section count unchanged, no new findings,
`page_to_build.mjs` re-export diff clean.

**Add a STATIC fidelity check before the first run of a regenerated script.** The two gate steps above
both require *running* the script, which is the destructive part — that is how workzones_v2 lost 6
authored sections on 2026-07-15/16. Cheaper and safer: export with `page_to_build.mjs --out <scratch>`,
then compare each exported `SECTIONS[i]` payload against the corresponding live `draft_sections[i]`
row's `data` (ignoring `id`/timestamps). If every section is byte-identical, running the script is
*provably* content-neutral and the gate becomes a formality.
(Worked example: `fidelity_static.mjs`, used for the 2026-07-27 workzones_v2 regeneration.)

**⚠ A matching section COUNT does not mean a builder is safe to run.** `build_tsmo2_incidents_v2.mjs`
was 32-for-32 with its live page and still silently reverted a fix when re-run (2026-07-27): the
2026-07-16 pass had changed the "Attributed delay by year" graph from millions-of-vehicle-hours to raw
vehicle-hours on the live draft — `display.yAxis.tickSpacing` `2` → `2000000` — and never backported
it. The rebuild restored `tickSpacing: 2` against a 0–9.79M domain, so `AxisLeft` began logging
*"ignoring tickSpacing 2 — it would produce 4893180 ticks (max 200)"* and falling back on every render.
Nothing failed loudly; the section count was identical and the page still drew.
**So: run `fidelity_static.mjs` before re-running any builder on a page that has been authored since
the builder was generated** — it is the only check that catches a same-count content drift. Recovery
came from the published rows, which a draft-only rebuild never touches.

**Every generated script now carries a runtime parity guard — `page_to_build.mjs` emits it** (as of
2026-07-28), so a regeneration no longer drops it and you don't hand-add it. It sits right after
`const existing = …draft_sections` and *before* the delete loop, refuses when
`existing.length !== SECTIONS.length`, and prints the exact `fidelity_static.mjs` command for that
page (real page id baked in). It replaces the old idiom of a hand-written `STALE — DO NOT RUN` throw
at the top of the file, which needed a human both to notice the drift and to remember to remove the
throw afterwards.

Verified by running a deliberately drifted copy (`SECTIONS.pop()`): it refuses with
*"REFUSING TO WIPE workzones_v2: the live draft has 34 sections but this builder carries 33"* and the
page is untouched — tested on both an emitted guard (workzones_v2) and a backfilled one (methodology).

**Coverage: 9 of 18 builders — every GENERATED one.** The guard was backfilled into the 7 generated
scripts that predate the generator emitting it (a pure code addition: it only takes effect on the next
run, so no rebuild and no page was touched to apply it). The **9 MIGRATED builders have no guard** —
they don't keep a `const SECTIONS` array (they create sections another way), so `SECTIONS.length` has
no meaning there and each needs its own "how many sections will I create" expression. Add it per
script, or regenerate them (which flattens hand-authored structure to a verbatim export — a trade to
make deliberately, not by default):
`build_cr_{design,overview,page,tickets}.mjs`, `build_fa_{home,gallery_about}.mjs`,
`build_tsmo_{home,incident_search,reliability}.mjs`.

**Two scripts remain stale** — `build_tsmo_home.mjs` still carries a `STALE` throw, and
`build_freightatlas2_freight_atlas.mjs` targets retired page 1411761 (the tracked page is 2189762).
Both want the same regenerate-and-gate treatment.

> **build_tsmo_home.mjs, 2026-08-24.** The throw is now load-bearing in a second way: the draft has
> moved 14 sections ahead of the script (client-feedback batch 2026-08-21, tickets 2214470-2214499),
> including one section-TYPE change. The regenerate half of "regenerate and gate" is already done —
> `scratchpad/build_tsmo_home.EXPORT-2026-08-24.mjs`. Only the gate is outstanding, and the gate is
> destructive to section ids that live tickets and the feedback doc both reference. Publish the draft
> first, then decide whether to adopt the export as owner.

**Not owned here** (out of scope): the 7 no-design tracked pages (pending prune decision);
`build_map21_lottr.mjs` (npmrds_sub report page), `build_emp_overview.mjs`,
`build_avlgraph_trends.mjs`, `build_s02/s03` (non-tracked pages) — still in scratchpad.

**Large payloads**: `dms section create --data` accepts a FILE PATH or `-` (stdin) as well as
inline JSON (CLI patched 2026-07-07) — generated scripts always write payloads to temp files;
migrated scripts using inline JSON are fine below ~100KB.

**Editable Card cells need an explicit `type` (2026-07-16 gotcha)**: making a Card column
editable-in-view takes BOTH `allowEditInView` (section + column) AND an explicit editable
columnType. A bare `col(name, label, {...})` sets **no `type`**, so the cell falls to `Card.jsx`'s
read-only `DefaultComp` and silently won't edit even with `allowEditInView` on (it still renders
the value in view, so the bug is invisible until you try to edit). Use `type: "textarea"` for
prose/multi-paragraph fields (multi-line box; `type: "text"` is a single-line `<input>` — wrong for
prose), `status_pill`/`select` for dropdowns. Worked example: `build_cr_tickets.mjs`'s `tacol`
helper + the Details-rail `efld` helper. Full explanation in `src/dms/skills/card-layout.md`
("Defaults that bite").
