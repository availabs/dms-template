# DMS Template Completed Tasks

Grouped by project, same hierarchy as [`todo.md`](./todo.md). See
[`planning-rules.md`](./planning-rules.md) for the project registry.

## TransportNY

### themes

- [Closure-density point-selection performance](./transportny/tasks/completed/closure-density-performance.md) - `data-types/routing/densitySearchPool.js` + `graphSearchWorker.js`, a worker_threads pool (`min(cpus-1, 8)` workers) sharing the routing graph via `SharedArrayBuffer`. Both point-selection validation and the route-tally step dispatch their independent per-pair searches to the pool in one combined batch instead of sequentially - same exact `bidirectionalDijkstra` algorithm (exported from `memoryGraph.js` so the worker calls the identical function), just parallelized. Verified live: point selection ~60s+ -> ~18s, route tally -> ~3-5s. App-level timeout bypass for the two density routes stays as uncommitted, local-only `src/dms` submodule state - needs re-applying after any fresh submodule checkout. (2026-08-24)
- [Closure-density route comparison tab](./transportny/tasks/completed/closure-density-route-comparison-tab.md) - new "Distance cost" / "Time cost" tabs in the density results panel, showing a 5-bucket histogram of per-pair open-vs-closed route deltas (miles/minutes) - reuses one `RouteComparisonBarChart.jsx` component via a `metric` prop, plain divs (no chart library). Backend extended `computeClosureDensityFromPoints` to compute both the open and closed route per pair through the same worker pool. Along the way, fixed three unrelated live-tested bugs: the testing-only pick-pair route staying visible after its toggle was switched off, a single-direction detour rendering solid instead of dashed when only one direction had a route, and simple detour mode's start/end points landing only one hop from the closed segment (now walks up to 200m/15 hops along the same road, fixing nonsensical huge-loop routes near interchanges). Also renamed "(testing)" UI labels to "(beta)". (2026-08-24)
- [Converter redesign: stop replicating redundant old route_comps](./transportny/tasks/completed/converter-route-comp-redesign.md) - added a merge/dedup pass so old `route_comp`s sharing (routeId, calendar date range, resolved tmc_array) collapse into one route entry instead of one-per-old-comp — Design Push #2 already moved peak/weekday/resolution slicing onto each graph's own `_measurePick`, making those separate route entries redundant. Merged entry gets a generic (never peak-labeled) name; a graph whose title never had a `{name}` slot picks up the distinction via a new suffix. For `--template-id` (Dynamic Report) conversions specifically, route slot names/graph titles are now fully genericized (measure/peak/dow/date only, never a specific old route name — a viewer can fill the slot with any route later) via a new `generic_comp_label` helper. Two real bugs found and fixed during live verification: (1) a template slot leaking a literal old route name/duplicated text when compTitle had no substitution tokens to signal the route-specific portion; (2) `useGraphPublish.js`'s route-resolution map broke for any graph fed by a comp merged away from its route entry's representative — fixed by carrying the full absorbed-comp-id list (`route_comp_ids`) through to runtime. Also deleted 68 stale pre-existing `converted_reports` pages + 52 orphaned `reports_snap_2` rows per Ryan's request, and reconverted all 12 catalog templates (3 rounds, one per fix). Live-verified via `report_probe.mjs` on the catalog page and Ryan's own real-routes repro URL. Runtime seriesLabel/title-from-picked-route (the "ideally" follow-up) is tracked separately as Round 4 in the DMS library's `dynamic-report-nongraph-section-binding.md`. (2026-08-07)
- [Reports page — § 01 template catalog](./transportny/tasks/completed/reports-page-template-catalog.md) - built the 12-card/5-category template catalog band from the `npmrds-reports.html` design mockup, data-driven off `reports_snap_2` extended with new `tags`/`graph_count`/`page_path`/`difficulty`/`counts_label` columns (no new dataset — extending the existing per-report-routes dataset was verified safe end-to-end via the actual write path, a shallow top-level JSONB merge). Converted the 9 remaining homepage templates (3 of 12 already existed) via the existing `convert_old_reports.py --template-id` pipeline. New page created at `/reports`, live-verified in both draft/edit and public view (0 console/page errors). Real gotchas hit along the way: a `dms raw update` CLI positional-args form that silently no-ops (superseding an older memory's blanket "raw update no-ops on split rows" advice — fixed by the since-added `--row-type` flag), a Lexical leaf-node shape that must be complete or renders nothing, and `Card` `isLink` cells needing an absolute (not relative) href. (2026-08-06)
- [ReportRouteList: UX polish, add-flow, and persistence history](./transportny/tasks/current/reportroutelist.md) - hooks/subcomponent refactor (`useReportRow`, `useGraphPublish`, `RouteRow`), visual polish (route count, unassigned badges, TMC truncation), and the add-route flow's evolution through three rounds (2026-07-21 banner-to-top, 2026-07-24 add-by-DMS-id fix, 2026-07-29 full inline search/add rebuild). Merged 2026-07-30 from two task files (`reportroutelist-ux-polish.md`, `add-route-flow-improvements.md`) — full history in `reportroutelist-archive.md`. (2026-07-21 through 2026-07-29)
- [AVL Graph quick controls — inline Measure/Comparison Mode pills in the card header](./transportny/tasks/current/report-page-redesign.md) - report-page redesign Gap 01, Variant A: refactored `MeasurePicker` to share its apply-logic (`applyMeasurePick`/`isReportPage`) with a new header-row `QuickControls` component (Measure dropdown pill + Comparison Mode toggle pill), registered via the new `sectionHeaderExtensions` library primitive. Fully live-verified — pill/drawer state sync, non-report-section gating, and multi-card independence all confirmed on real report pages. (A claim made during this task, that the View-mode section Settings trigger is `display:none` at all desktop widths, was investigated further the same day and retracted — not a real bug.) (2026-07-22; merged 2026-07-30 with Gaps 02/03 into `report-page-redesign.md`, see that file's own status)
- [RRL: relative-dates edit gate + missing graph count](./transportny/tasks/completed/rrl-relative-dates-and-graph-count.md) - fixed the two original RRL bug reports: a derived-date route had NO way to re-enter edit mode (the pencil was gated `!isDerivedDate`, hiding a fully-built Fixed/Derived UI that already worked correctly once reachable), and route rows never showed how many graphs they feed (`useGraphPublish`'s already-computed `graphs` return value was discarded; wired it into a live, always-correct per-route count instead of the stale write-once `graphIds` field). Caught and reverted one accidental live-page reorder mid-investigation via a scoped DB write. (2026-08-07)
- [Compact SideNav — RRL header bar white-sliver gap](./transportny/tasks/completed/compact-sidenav-margin-bug.md) - the "blank space to the left of the RRL" Ryan flagged turned out to be `ReportRouteList`'s own dark "ROUTES" header bar sitting inside the generic per-section `p-3` grid-cell padding — visible only because the header's dark bg nearly matches the rail's, not a SideNav/Layout margin bug (present identically on compact AND default expanded rails). Fixed with `-mt-3 -mx-3` on `ReportRouteList.theme.js`'s `panelHead`. A wrong first theory (Layout wrapper2 margin vs. sidenav.activeStyle decoupling) was chased, implemented, found to be an actual regression (a real double-margin gap on the 2 pages that had `sideNav.activeStyle:1` set), and fully reverted before the real cause was found — see the task file's "Correction" section. (2026-08-07)
- [Catalog page slugs collide with old-system names](./transportny/tasks/completed/catalog-page-slug-naming-fix.md) - slug/title swap fixed ("Snapshot" ↔ "Weekly Average"/`rochester_inner_loop`, both catalog card link and on-page `<h1>`) via a `--title` override added to `--template-id` conversions; all 12 catalog templates re-run + catalog metadata reapplied. The one item left open (header `purpose`/`metaLine` placeholder text still showing the master template's editor-instruction copy on all 12 pages) was folded into `dynamic-reports-and-route-tags.md`'s open questions on 2026-08-18 rather than left stranded here. (2026-08-07; moved to completed 2026-08-18)
- [Client request → report: the skill arc](./transportny/tasks/completed/client-request-to-report-skill.md) - made Claude able to turn a literal client request into a real, showable report end-to-end via the CLI (`route_build.py`/`report_build.mjs`) — spec-on-the-row storage, an intake posture (guess-and-flag), a rules library, Route Map/Info Box/Route Compare wiring, peak-hour sub-windows. All four original gaps plus every follow-on wiring gap DONE; two real client-request case studies (NY-9D Beacon, Poughkeepsie) exercised it against real AVAIL-shipped reports. Full history in `client-request-to-report-skill-archive.md`. 5 small residual open items (`--ui-guide` generator, Route Map `color_range` default, `--verify-routing`, `--from-page` route-field drift, measure-queryable-for-year check) folded into `dynamic-reports-and-route-tags.md`'s open questions on 2026-08-18 rather than left stranded here. (2026-07-27 through 2026-07-30; moved to completed 2026-08-18)
- [Report spec + `report_build.mjs`](./transportny/tasks/completed/report-spec-and-build-script.md) - declarative NPMRDS report authoring: one JSON spec → live report page, composed through the *same* `applyMeasurePick` the UI's Measure Picker calls, so CLI and UI output are byte-identical by construction. Phases A (skill split into `creating-reports.md`/`creating-routes.md`) + B (spec format + script) DONE 2026-07-27; the "Follow-on: Dynamic Report spec support" section (all 12 catalog templates spec-built) DONE 2026-08-11; difference-graph color polarity DONE 2026-07-30. Phase C (UI-parity gaps) was always tracked separately in `report-route-ui-parity-gaps.md`, never duplicated here. The one item spun off rather than blocking this file's completion: a `minutes_seconds` duration y-axis ValueFormat, tracked in the DMS library's own `duration-value-format-mm-ss.md` (still not started, though a reusable `duration_mmss` formatter landed 2026-08-17 as a side effect of unrelated tooltip work). (2026-07-27 through 2026-08-11; moved to completed 2026-08-18)
- [NPMRDS reports/routes planning-doc consolidation](./transportny/tasks/completed/reports-docs-consolidation.md) - full inventory of ~18 overlapping task files, then all 7 fix steps: deleted a dead-duplicate file, fixed a broken cross-reference and `todo.md`'s own malformed duplicate preamble, folded small "still open" tables out of 3 files into `dynamic-reports-and-route-tags.md` before moving them to `completed/`, split `npmrds-design-v2-implementation.md` and `dynamic-reports-and-route-tags.md` each into a trimmed live file + a full-history archive (mirroring the project's existing live+archive convention), refreshed a drifted golden-corpus inventory table against its real git-tracked manifest, and added a pointer for an orphaned finding in `port-transportny-map-plugins.md`. Surfaced and fixed 2 real staleness bugs along the way that weren't part of the original plan (a stale "9 templates need rebuilding" claim that was actually down to 3; 4 open items buried in dated narrative that had never been promoted to the live doc's own tracking section). (2026-08-18)
- [Port transportNY map plugins into dms-template via theme](./transportny/tasks/completed/port-transportny-map-plugins.md) - map plugins (`routecreation`, `macroview`) now load via a new `theme.mapPlugins` key instead of existing only in transportNY. Both ported to `src/themes/transportny/components/{routecreation,macroview}/` and live-verified 2026-07-29 (routecreation: full click-to-select pipeline including real falcor data fetch; macroview: crash fixed + map/legend/filters all render with real data). `rerouter` explicitly deferred until a coworker/client asks; `pointselector`/`routing` excluded (dead/unconfirmed usage). Known gaps: routecreation's marker/auto-route mode and save/load untested; macroview's Data Downloader (needs a `MapEditorContext.DAMA_HOST` upstream) and MapEditor-side controls untested. **This closes the last reason routes/reports development ever needed the transportNY repo** — see the update notes added the same day to `creating-routes.md`, `creating-reports.md`, and `research/npmrds-reports/reportroutelist-cross-repo-sync.md`.

## MitigateNY

### themes

- [MNY design-system `reports/` folder + Duplicate & Boilerplate action reports](./mitigateny/tasks/completed/mny-actions-analysis-reports.md) - introduced a `reports/` area in the MitigateNY design system for HTML analysis outputs that are *not* meant to migrate to DMS, moved the existing Actions QA report there, and added two data-backed reports (Duplicate Actions, Boilerplate Actions) plus their analysis scripts in `references/actions/`. (2026-07-16)
- [MNY — Capability Inventory + Capabilities-vs-Capacity data reports](./mitigateny/tasks/completed/mny-capability-capacity-reports.md) - reports 5 and 6 in the `reports/` series, analyzing the combined 7-county capability CSV (2,354 rows / 169 jurisdictions): `capability-inventory.html` (coverage, breadth, county comparison, vocabulary gaps) and `capabilities-vs-capacity.html` (82.1% of rows are "pure checkbox" with no utilization/integration narrative — the case for a separate capacity assessment). Nav widget updated on all 6 report pages. Open: narrow-viewport layout unverified. (completion date not recorded in the task file; indexed 2026-08-05)
- [MNY — State Capability Catalog data report](./mitigateny/tasks/completed/mny-state-capability-catalog-report.md) - report 7, analyzing `capcat.csv` (649 rows) — the statewide capability *catalog* as distinct from county self-reports. Confirms the "mostly state level" framing for the 402 real programs, but surfaces that 245 rows (38%) are bare placeholders with zero data, 3 live test/junk rows remain, and the 2.0 taxonomy migration is mid-stream (the next tier of new fields is 100% empty). Nav widget updated on all 7 pages. Open: desktop-width screenshot and mobile layout unverified. (completion date not recorded in the task file; indexed 2026-08-05)
- [MNY — Capacity Assessment Data Architecture report](./mitigateny/tasks/completed/mny-capacity-assessment-architecture-report.md) - report 8, answering whether the planned capacity assessment should be extra columns on the capability dataset or its own dataset. Recommends **two datasets joined by a pinned (jurisdiction, capability/capacity-type) composite key**, grounded in DMS's real source/view/join mechanics per `live-cross-view-joined-section.md` — capability data is sparse/event-shaped, capacity needs a dense jurisdiction × ~30-item grid. Testing checklist fully closed. Implementing the dataset is future work if the recommendation is accepted. (completion date not recorded in the task file; indexed 2026-08-05)

## Landbank

_(none yet)_

## Tessera

_(none yet)_

## Shared

_(none yet)_
