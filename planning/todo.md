# DMS Template Todo

## themes

- [x] ~~See [`research/npmrds-reports/reportroutelist-cross-repo-sync.md`](../research/npmrds-reports/reportroutelist-cross-repo-sync.md) — `ReportRouteList` manual cross-repo sync~~ — obsolete as of 2026-07-29: `routecreation`/`macroview` (the reason transportNY needed its own theme copy at all) are now ported natively into dms-template via `theme.mapPlugins` ([`tasks/completed/port-transportny-map-plugins.md`](./tasks/completed/port-transportny-map-plugins.md)), so routes/reports work no longer touches transportNY. `RouteComparison` (reverse-drift, transportNY-only) is still flagged as a separate, unrelated item in the research doc — not a report/route dependency.
- [ ] [MNY — Action Prioritize (list view) redesign in the design system](./tasks/current/mny-actions-prioritize-list-design.md) — new `pages/actions-prioritize.html` + DS additions (tier pills, linked stat strip, filter bar). Flags two platform gaps needed for a later live build: an active-when-search-param-matches cell hint, and an is-empty filter leaf.
- [ ] [MNY — build "Prioritize Actions (List)" live in the county-template pattern](./tasks/current/mny-action-prioritize-v2-live-build.md) — 3-phase: (1) working interactive page next to `action_prioritize` from existing primitives, (2) style via mny theme `activeStyle`s, (3) new platform features (active stat cell, empty-filter op, tier-pill column type, progress metric — escalated to `src/dms/planning/`).
- [ ] [Report page redesign — all 3 gaps](./tasks/current/report-page-redesign.md) — Gaps 01 (quick controls, DONE), 02 (route color assignment, implemented + live-verified end-to-end but with open testing-checklist items: auto-assigned color on a fresh `addRoute`, cross-graph color consistency, Bar/Pie/Treemap and GridGraph/SunburstGraph regression all untested), and 03 (card visual/density polish, settled 2026-07-23 after a scope review — shadow knob kept/restyled, attribution divider kept, legend/tooltip precision kept; header-uppercase and footer-whitespace fixes both fully reverted at user request). Merged 2026-07-30 from three separate task files (`avl-graph-quick-controls.md`, `report-route-color-assignment.md`, `report-card-visual-density-polish.md`) — full history in `report-page-redesign-archive.md`.
- [ ] [ReportRouteList — UX, add-flow, and persistence history](./tasks/current/reportroutelist.md) — see "Cross-repo note" in that file; hooks refactor, visual polish, add-route flow's 3-round evolution. Open items: stray duplicate DB rows need cleanup authorization, publish-path not separately re-tested for the graphIds fix.
- [ ] [Route creation tool — current status](./tasks/current/route-creation-tool.md) — orientation pointer tying together the port (DONE 2026-07-29), the marker-placement/auto-routing phases (Phase 2 done, Phases 1/3 not started), and known-closed loose ends. Phase 1 (dms-server routing proxy) is the next real blocker; Phase 3's year selector needs the routable-year-range re-verified beyond one test location first.

- [ ] [Report spec + `report_build.mjs`](./tasks/current/report-spec-and-build-script.md) — declarative NPMRDS report authoring: one JSON spec → live report page, composed through the *same* `applyMeasurePick` the UI's Measure Picker calls, so CLI and UI output are byte-identical by construction and UI parity becomes "does a control exist for each spec field?". Also carries the client-request→spec inference layer (a main feature per user 2026-07-27). Phases A (skill split into `creating-reports.md`/`creating-routes.md`) + B (spec format + script) DONE 2026-07-27; Phase C (UI-parity gaps) remains.
- [ ] [Client request → report: the skill arc](./tasks/current/client-request-to-report-skill.md) — all four original gaps plus every follow-on wiring gap (Route Map/Info Box/Route Compare, route-date cleanup, peak-hour sub-window) DONE as of 2026-07-29. Split 2026-07-30 into this live status/reference doc + `client-request-to-report-skill-archive.md` for full history. Open: `--ui-guide` generator, Route Map's `color_range` default, `--verify-routing` (flagged non-functional/wrong-oracle), a `--from-page` route-field drift gap, and an unconfirmed Route Map travelTime regression (see next item).
- [ ] **Check Route Map's travelTime choropleth for the same live regression Route Compare had** — flagged 2026-07-29 while fixing Route Compare's travelTime delta query (see `report-spec.md`'s "Route Compare graphs" section and this task file's item 10 writeup), NOT YET VERIFIED — deliberately stopped before running the check (user: "mark this down as a TODO, i dont want to do it now anymore"). `TRAVEL_TIME_VALUE_EXPR` (used by `ensure_route_map_traveltime_template`, `convert_old_reports.py:3298`) derives from the same shared, now-bare-column `TRAVEL_TIME_EXPR`, and the template carries the same `table1` join Route Compare's did — structurally similar, but Route Map's failure mode may differ (no `__ANCHOR__` involved, so the specific "aggregate found inside aggregate" error may not reproduce; a silent ambiguous-column resolution or wrong values is also possible and needs checking, not just a rerun of the same repro). To check: `python3 scripts/npmrds-reports/convert_old_reports.py --route-map-section --measure travelTime --year 2026 --tmcs '[...]' --start-date <YYYY-MM-DD> --end-date <YYYY-MM-DD>` with real TMCs (e.g. NY-9D's `120+29713`/`120-29713` etc., known-good from this session) and see whether it errors or bakes real values.
- [ ] [NPMRDS route/report UI parity gaps](./tasks/current/report-route-ui-parity-gaps.md) — Phase C of the report-spec arc above. Ranked list of route-creation (map tool) and report-building (RRL/Measure Picker) UI gaps the spec format now makes enumerable. 5 of 12 gaps fixed and live-verified so far (`route_id` overwrite labeling, TMC search-to-add — both 2026-07-27; peak-hour filtering — 2026-07-28; `weekdays` day-mask UI, difference-graph anchor UI — both 2026-07-30); next cheapest is gap #7 (RRL rename control, needs the existing input-commit bug root-caused first).

## data-types

## deployment

## content

- [ ] [Set up tessera.so landing, features, and docs pages on design_system_v6](./tasks/current/tessera-v6-landing-pages.md) — all 3 pages built as drafts on `test|pages:pattern` (`app=tessera-test`, id 3) with `tessera_v6` theme applied, verified live in both light and dark. Left unchecked pending human review + `dms page publish`; also flags a docs SideNav follow-up and 2-3 minor content-fidelity gaps (editor illustration, invite form, one CTA-button-row layout nit).
