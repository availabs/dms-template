# DMS Template Todo

Every item belongs to a project section. See [`planning-rules.md`](./planning-rules.md) for the
project registry and the rules for this shared directory.

Projects: [TransportNY](#transportny) · [MitigateNY](#mitigateny) · [Landbank](#landbank) ·
[Tessera](#tessera) · [Shared](#shared)
## TransportNY

### themes

- [ ] [Routes/Reports/Users mesh — picker modals, curated+user data merge, ownership filtering](./transportny/tasks/current/routes-reports-users-mesh.md) — **current priority as of 2026-08-25**, per Ryan's direction. Workstreams A (picker-modal mockup, `npmrds-picker-modals.html`, live-verified) and C (auto-gen route audit: 731 duplicate-name collisions found+fixed, 1,617/8,660 single-TMC routes flagged for de-prioritization) both DONE 2026-08-25. B (old-reports reconversion classification, tracked in `src/dms/planning/tasks/current/old-reports-conversion.md`) also ran 2026-08-25 — surfaced a real live regression: old-report-converted graphs have silently reverted to "all days, all hours" since a 2026-08-14 rendering-contract change the converter never picked up. Route Creation Plugin redesign explicitly last, not started.
- [x] ~~[Report Authoring UX Overhaul — 9 items + gap #16](./transportny/tasks/current/report-authoring-ux-overhaul.md)~~ — **Tiers 1-10 all DONE, live-verified 2026-08-24.** Full build record archived 2026-08-25 to `report-authoring-ux-overhaul-archive.md` (file had grown past 2500 lines). Only 3 explicitly-deferred items remain open (Publish/Discard for RRL, multi-window-per-route UI, a unified Add Report Section picker) — see the file's own Parked section. No longer the active focus; superseded by the mesh item above.
- [ ] [Point-to-point routing plugin — new map plugin + backend routing service](./transportny/tasks/current/point-to-point-routing-plugin.md) — new, separate from `routecreation` (that's a named-route authoring tool, this is a focused 2-point shortest-path query). Turn-restriction-aware routing backend + data (persisted relations, confirmed `pgr_trsp` approach) already built/validated in the sibling `conflation` repo (2026-08-03) — see that repo's `routing/ROUTING_API_TASKS.md`. Blocked first on `git submodule update` (src/dms is stale, `theme.mapPlugins` wiring missing). Open: backend location (fix avail-falcor's buggy `dama/routes/data_types/pgr/` vs. build fresh), target theme, point-placement UX.
- [ ] [NPMRDS Design System v2 → report page & report library — STALE/SUPERSEDED](./transportny/tasks/current/npmrds-design-v2-implementation.md) — **marked stale 2026-08-24 (Ryan's call).** Item 1 ("Map graph-type support, genuinely blocked") is now moot — Map creation UI + choropleth-by-measure (5 measures + all 4 CO2 variants) shipped in `report-authoring-ux-overhaul.md`. Items 2-4 (report library blocked on Alex's design, tabled post-ship gap review, small Phase 4 theme tokens) not re-checked this pass — read `report-authoring-ux-overhaul.md` for current state before trusting this file.
- [x] ~~[Reports page — § 01 template catalog](./transportny/tasks/completed/reports-page-template-catalog.md)~~ — **DONE, live-verified 2026-08-06**. Built the 12-card/5-category template catalog band, data-driven off `reports_snap_2` extended with `name`/`description`/`tags`/`graph_count`/`page_path`/`difficulty`/`counts_label` columns (no new dataset, per Ryan's steer). Converted the 9 remaining homepage templates. New page at `/reports`. See task file for gotchas hit (a `dms raw update` CLI positional-args bug, Lexical leaf-node shape, relative-link paths). **Follow-up bugs found same day, split into their own task files** — see the 3 items below plus a `src/dms/planning/` library task.
- [ ] [Live-verification testing structure: `report_probe.mjs --expect` + golden-corpus batch probe](./transportny/tasks/current/report-probe-expect-and-golden-corpus.md) — **PRIORITIZED for next session, 2026-08-07.** `--expect <spec.json>` DONE and live-verified (found + fixed 3 real `report_build.mjs`/`report_probe.mjs` bugs along the way — every spec-built graph since 2026-08-06 had rendered blank, plus two separate sidebar-layout gaps). 5 real golden-corpus candidate pages built/confirmed live with working URLs (4 spec-built graph types + 1 real Dynamic Report page covering Bar Graph Summary). Remaining, now the actual next step: build the real test plan/script — baseline capture + diff-on-rerun — that wires these pieces together into something repeatable. Part of a 2026-08-06 testing-structure ask.
- [ ] [Unit tests for the converter's pure vocabulary/date/expression logic](./transportny/tasks/current/converter-vocabulary-unit-tests.md) — new pytest suite for `convert_old_reports_lib/dates.py`/`expressions.py`/color-scale logic (currently zero test coverage despite being where most of the 69-round history's bugs lived). Part of the same 2026-08-06 testing-structure ask. Explicitly NOT in scope: fixing `dms-server`'s partially-wired `npm test` script (Ryan: triaging that is out of scope).
- [ ] [Dynamic Reports, Route Tags & Add-Route Flow — next-phase scoping](./transportny/tasks/current/dynamic-reports-and-route-tags.md) — **takes priority over every other item in this section as of 2026-07-31**, per Ryan's direction. Three related items, core architecture now decided (all same-day, 2026-07-31, across three rounds): (1) Add Route Flow and (3) Dynamic Reports' route-slot-fill share one tag-folder-browsing picker modal, not two separate builds, and the Add-Graph-via-modal idea is part of this arc too; (2) Route Tags — routes get a `multiselect` tags column (system-applied + user-addable in `SaveRouteModal.jsx`), filtered via DMS's existing generic `array_contains` UDA filter (no new engineering needed there), starting taxonomy to be inferred from the old DB's folder structure (not yet done); (3) Dynamic Reports are one shared DMS page per template (not a new row per use), route slots filled via URL param on the existing page-variable system, with the old tool's 216 templates to be ported as real content while keeping them dynamic. Two standing scope decisions were explicitly reversed to get here (folders back in scope for route organization; old templates to be ported after all) — both amended into the referenced memories. Remaining opens are sequencing/scheduling only — see the file. **2026-08-24: Ryan reviewed the "Open questions for triage" tail (stashed sidebar-width lever, 3 templates needing `--publish`, multi-year query-size limit, route/comp relative-date labels) and confirmed it's all done or no longer relevant** — not independently re-verified. Route Tags' TMC-linear auto-generation is still only done for 2024 (10 other years not yet generated) — untouched by this review.
- [x] ~~[NPMRDS reports/routes planning-doc consolidation](./transportny/tasks/completed/reports-docs-consolidation.md)~~ — **DONE 2026-08-18.** ~18 overlapping task files across this section had drifted; all 7 steps closed: 3 concrete bugs fixed (a dead duplicate file, a broken cross-reference, this file's own malformed duplicate preamble), `client-request-to-report-skill.md`/`catalog-page-slug-naming-fix.md`/`report-spec-and-build-script.md` folded/moved to `completed/`, `npmrds-design-v2-implementation.md` and `dynamic-reports-and-route-tags.md` both split live+archive, the golden-corpus table refreshed, a pointer added for `port-transportny-map-plugins.md`'s untested surface. Only open thread left is Alex's report-library design status, tracked in `npmrds-design-v2-implementation.md` itself, not a doc-structure issue.
- [ ] [Report page redesign — all 3 gaps](./transportny/tasks/current/report-page-redesign.md) — Gaps 01 (quick controls, DONE), 02 (route color assignment, implemented + live-verified end-to-end — remaining testing-checklist items confirmed working by Ryan 2026-08-24: auto-assigned color on a fresh `addRoute`, cross-graph color consistency, Bar/Pie/Treemap and GridGraph/SunburstGraph regression), and 03 (card visual/density polish, settled 2026-07-23 after a scope review — shadow knob kept/restyled, attribution divider kept, legend/tooltip precision kept; header-uppercase and footer-whitespace fixes both fully reverted at user request). Merged 2026-07-30 from three separate task files (`avl-graph-quick-controls.md`, `report-route-color-assignment.md`, `report-card-visual-density-polish.md`) — full history in `report-page-redesign-archive.md`.
- [ ] [ReportRouteList — UX, add-flow, and persistence history](./transportny/tasks/current/reportroutelist.md) — see "Cross-repo note" in that file; hooks refactor, visual polish, add-route flow's 3-round evolution. **Stray duplicate DB rows cleaned up 2026-08-24** (701 rows across 666 report_ids, far more than expected — see the file for the full record). Still open: publish-path not separately re-tested for the graphIds fix.
- [ ] [Route creation tool — current status](./transportny/tasks/current/route-creation-tool.md) — orientation pointer tying together the port (DONE 2026-07-29), the marker-placement/auto-routing phases (Phase 2 done, Phases 1/3 not started), and known-closed loose ends. Phase 1 (dms-server routing proxy) is the next real blocker; Phase 3's year selector needs the routable-year-range re-verified beyond one test location first.
- [ ] [Report Page template — editorial slots + Callout Stat (REOPENED)](./transportny/tasks/current/report-page-template-editorial-slots.md) — original rounds 1+2 still live; reopened 2026-07-31 same day: RRL pill labels now distinguish "Stat N" (Card) from "Graph N" (everything else) in `useGraphPublish.js`, DONE and live-verified. The hero-stat menu's misleading "Speed already selected" display (a `DEFAULT_PICK` fallback with no real state backing it, same latent pattern also present in `MeasurePicker`) is still open — an attempted DB-content fix reverses this task's own prior explicit "ships unconfigured" decision and its effect on newly-created pages is unconfirmed/inconsistent (a materialization step appears to reset Card sections to a generic default independent of template content; not root-caused). Real fix likely belongs in the menu's display logic, not template data. A pre-filled prose field next to the stat (a separate ask) was descoped, not attempted.
- [ ] [NPMRDS route/report UI parity gaps](./transportny/tasks/current/report-route-ui-parity-gaps.md) — Phase C of the report-spec arc above. Ranked list of route-creation (map tool) and report-building (RRL/Measure Picker) UI gaps the spec format now makes enumerable. 5 of 14 gaps fixed and live-verified so far (`route_id` overwrite labeling, TMC search-to-add — both 2026-07-27; peak-hour filtering — 2026-07-28; `weekdays` day-mask UI, difference-graph anchor UI — both 2026-07-30); next cheapest is gap #7 (RRL rename control, needs the existing input-commit bug root-caused first) — **deprioritized 2026-08-24 (Ryan): not worth chasing proactively, revisit only if it comes up again.** Two new gaps (#13/#14) added 2026-07-31, a different "cold-open" axis (first-60-seconds friction, not measure/graph parity) — see [`research/npmrds-reports/cold-open-ux-findings.md`](../research/npmrds-reports/cold-open-ux-findings.md) for the full diagnosis of why the tool feels less complete than the closing gap count suggests.
- [x] ~~See [`research/npmrds-reports/reportroutelist-cross-repo-sync.md`](../research/npmrds-reports/reportroutelist-cross-repo-sync.md) — `ReportRouteList` manual cross-repo sync~~ — obsolete as of 2026-07-29: `routecreation`/`macroview` (the reason transportNY needed its own theme copy at all) are now ported natively into dms-template via `theme.mapPlugins` ([`transportny/tasks/completed/port-transportny-map-plugins.md`](./transportny/tasks/completed/port-transportny-map-plugins.md)), so routes/reports work no longer touches transportNY. `RouteComparison` (reverse-drift, transportNY-only) is still flagged as a separate, unrelated item in the research doc — not a report/route dependency.
- [x] ~~Check Route Map's travelTime choropleth for the same live regression Route Compare had~~ — **VERIFIED 2026-07-30: no regression, no fix needed.** Ran the exact repro the item specified (`convert_old_reports.py --route-map-section --dry-run --measure travelTime --year 2026` with NY-9D's real TMCs `120+29713/+29712/+29714/-29713/-29711/-29712`, a real 2025-01 date range) end to end — no error, real quantile breaks baked (1.0–3.0 min range). Also directly diffed `TRAVEL_TIME_VALUE_EXPR` (bare-column) against a `ds.`-qualified control version of the same expression, same LEFT JOIN, same TMCs/dates, straight against ClickHouse via `dbq.py ch` — byte-identical values on all 6 TMCs, ruling out the "silent wrong value" possibility too. Two reasons this differs from Route Compare's failure: (1) Route Compare's "aggregate found inside aggregate" error came specifically from wrapping the bare expression in dms-server's `__ANCHOR__(...)` delta composition — the Map choropleth's CH-join path (`tile-join-clickhouse-source.md`) has no such wrapping, the CH subquery is just `query.columns`+`groupBy`+`join` run directly and merged in via `jsonb_to_recordset`; (2) the join predicate is exactly `ds.tmc = table1.tmc`, so any ambiguous resolution of the bare `tmc` reference between the two tables can't diverge in value, and `travel_time_all_vehicles` only exists on the `ds` side (the TMC-identification join table doesn't carry it) so there's no ambiguity there either.

## MitigateNY

### themes

- [ ] [MNY — State Actions Dashboard design page](./mitigateny/tasks/current/mny-state-actions-dashboard-design.md) — **BUILT 2026-08-19, pending owner visual pass.** `pages/county-actions/state-dashboard.html`, the statewide sibling of the county dashboard, every number real from **Actions Cleaned (source 12453 / view 13192)**: county choropleth (new `ny_counties_actions.geojson` asset), statewide hazard + normalized-priority mixes, and four county×variable cuts (progress, priority, hazard heatmap, location quality) + an all-counties leaderboard. Rebuild via `references/actions/scripts/18|19_*.mjs`. Flags live-build platform gaps (heatmap + 100%-stacked primitives, choropleth Map feed).
- [ ] [MNY — Action Prioritize (list view) redesign in the design system](./mitigateny/tasks/current/mny-actions-prioritize-list-design.md) — new `pages/actions-prioritize.html` + DS additions (tier pills, linked stat strip, filter bar). Flags two platform gaps needed for a later live build: an active-when-search-param-matches cell hint, and an is-empty filter leaf.
- [ ] [MNY — build "Prioritize Actions (List)" live in the county-template pattern](./mitigateny/tasks/current/mny-action-prioritize-v2-live-build.md) — 3-phase: (1) working interactive page next to `action_prioritize` from existing primitives, (2) style via mny theme `activeStyle`s, (3) new platform features (active stat cell, empty-filter op, tier-pill column type, progress metric — escalated to `src/dms/planning/`).
- [ ] [MNY — Jurisdiction Prioritization page in the County Actions Workflow](./mitigateny/tasks/current/mny-county-actions-jurisdiction-prioritization.md) — DONE pending a human visual pass. Adds `pages/county-actions/jurisdiction-prioritization.html` (local priority + the moved "Needs your attention" band) and narrows `workspace.html` to county priority as its only editable column; `jurisdictions.html`'s 23 tiles now enter the new page. Two open items: a pixel-level review (this session could only verify DOM/text), and the local-priority fill counts, which are placeholders until refreshed from source `1029065` / view `1074456` — every other number on the six pages is real Sullivan data.
- [ ] [MNY — build "Jurisdiction Prioritization" live in the county-template pattern](./mitigateny/tasks/current/mny-jurisdiction-prioritization-live-build.md) — converts the approved design page into a real DMS page (app `mitigat-ny-prod`, type `prod`, pgEnv `hazmit_dama`), bound only to Actions-Revised (source 1029065 / view 1074456). Five sections specified with their bindings; the county sibling (2262755) already shipped most of the library features this reuses. Two pre-flight blockers: **three of the four editable column names are unverified** against the source, and `local_priority`'s distribution is unprofiled. Needs 4 mny-theme additions (the `headerValueLayout:'row'` prerequisite is load-bearing) + 4 small additive dms library changes, incl. a `toggleSearchParam` Card flag without which the needs-attention tiles can't switch off.
- [ ] [Action Prioritize (List) v3 — close the design-fidelity gaps](./mitigateny/tasks/current/mny-action-prioritize-v3-design-fidelity.md) — **PLAN, awaiting sign-off (no implementation yet).** Six remaining gaps between the live v2 page (2262755, DRAFT) and the approved mockup, each mapped to the smallest platform enrichment. Three of them are genuine `@availabs/dms` library changes (filter style mechanism, `edit_publish` provider, `county_badge` columnType) and get their own docs under `src/dms/planning/`.
- [ ] [Action Prioritize (List) v3 — implementation plan](./mitigateny/tasks/current/mny-action-prioritize-v3-implementation-plan.md) — the task-by-task execution plan for the spec above (checkbox steps, per-gap sequencing, subagent split).
- [ ] [MNY Admin Panel — current-state analysis & direction reports](./mitigateny/tasks/current/mny-admin-panel-directions.md) — **the standing admin-panel question as of 2026-08-06; blocks the redesign task below.** Three-report series shipped in `src/themes/mny/design/reports/` (`admin-workflow-current-state.html` + Direction A `admin-direction-consolidate.html` + Direction B `admin-direction-dissolve.html`, recommended B1: all six planner forms move into the plan experience on shared `subdomain:*` patterns, `/admin` becomes a ~12-page DHSES console; both directions include the owner-required Plan Status page). Open: owner direction decision; four no-regret fixes (7 dead LHMP_IA "Add Data" rows + stale card snapshots, guide link sweep — 59/122 broken, archive legacy trees, Capabilities-vs-Capacities schema). **Plan Status mockups DONE 2026-08-07** — both directions, real Sullivan data, new "LHMP Admin" category at `src/themes/mny/design/pages/lhmp-admin/` (`plan-status-admin.html` + `plan-status-plan.html`); HoC-worklist mockup is next. Raw pulls + fact ledger in `references/admin IA/` (git-ignored).
- [ ] [MNY Admin Panel redesign — design mockups](./mitigateny/tasks/current/mny-admin-panel-redesign.md) — re-shapes live pattern `566466` (`prod|admin:pattern`, `/admin`) as HTML mockups in `src/themes/mny/design/pages/` across 7 datasets. Phase 1 (Actions, 15 files across State/County/Jurisdiction tiers) + the IA report and Admin Panel Home are DONE (2026-07-29); Phase 2+ (Capabilities, Hazards of Concern, High Hazard Dams, Participation, Capacities, Roles) not started. Note the superseded framing at the top: tiers were decided to be roles/scope *within* one page per page-type, not three duplicated page sets — Phase 1's 15 files are left unconsolidated and Phase 2+ should build directly in the collapsed shape. **ON HOLD as of 2026-08-06** — do not start Phase 2+ until the direction decision in the task above lands; if Direction B is chosen, Phase 2+ builds happen in plan context instead.

### content

- [ ] [MNY Admin Panel — page-by-page status report + county_template link audit](./mitigateny/tasks/current/mny-admin-panel-status-report.md) — **report DONE 2026-08-12, fixes not started.** `src/themes/mny/design/reports/admin-panel-status.html` documents all 74 pages of admin pattern `566466` (IA diagram with per-page status, the 18-family analogy matrix, context per page type) and every county_template (`1300890`) link into `/admin`. Findings: **47 rendered buttons point at slugs that don't exist** across 27 pages, 17 pages are reachable only by typing the URL, 23 published sections never render (18 on admin Home), and 5 of the 10 LHMP_IA "add data" doors are broken with 48 stale cached copies across 30 county pages. Ships a 9-type fix list; types 1–2 (missing `other_forms/` segment, unifying `create`) clear 30 of the 47. Nothing deleted; delete buttons untested per client direction. **v2 DONE 2026-08-13** — `admin-panel-status-v2.html` + `admin-panel-fix-list-v2.csv` narrow the same audit to the planner-facing panel (7 families + Home/Forms, everything under `other_forms` dropped), assume **no page naming convention**, and check the create-as-modal decision: 29 pages, **19 live dead links on 17 pages**, 1 unreachable page, **50 tracked fixes**. **16 of the 19 dead links are the CREATE button**, and four of the seven families render none at all. Key finding: the create modals (`modalParamKey: 'create'` + add form → `single_edit`) are already authored on 19 pages, but **nothing publishes the `create` action param, so none can open** — masked only because the deployed bundle predates modal section groups and renders the forms inline. The 5 trigger items must ship with the next deploy or create breaks.
- [ ] [Schenectady County HMP → MitigateNY 2.0 transcription](./mitigateny/tasks/current/schenectady-hmp-migration.md) — transcribe the Schenectady County Hazard Mitigation Plan into county-template pattern 2275239 (`mitigat-ny-prod`, geoid 36093), filling empty Annotation (Local Context) lexical slots verbatim; `draft_sections` only, never publish. Batch phases done; jurisdictional annexes split out into the task below.
- [ ] [Schenectady jurisdictional annexes → Jurisdictions dataset](./mitigateny/tasks/current/schenectady-jurisdictional-annexes.md) — the deferred "9 annexes" piece of the migration, re-mechanized: annex content lives in dataset row columns (source 1346449 / view 1346450) driving the filtered Jurisdictional Annex template page 2304232. Open: owner visual verification per jurisdiction + a decision on unmapped sections/hazards.

### data

- [ ] [Nassau County HMP → MitigateNY 2.0 annex transcription](./mitigateny/tasks/current/nassau-hmp-annex-transcription.md) — **Phases 1–4 DONE (crosswalk 2026-08-20, owner decisions 2026-08-21); Phase 5 next.** Nassau's 2020 plan is **Hagerty Consulting**, not Tetra Tech — the second consultant, so this also generalised the transcription skill (new [`profiles/hagerty.md`](./mitigateny/skills/profiles/hagerty.md), a new Phase-0 question in Layer 1, corrected Nassau attribution in `profiles/tetratech.md`). Glen Cove crosswalked to **163 field-level mappings** across Actions/Capabilities/Roles/HOC/Participation/Jurisdictions ([report](./mitigateny/skills/worked-examples/nassau-annex-crosswalk-report.md)); corpus pre-flight over all 52 annex folders confirms a uniform 12-heading spine and measures **234 proposed + 284 prior actions + 139 action worksheets**. **All Phase-4 decisions resolved 2026-08-21 (and revised same day) with no schema additions** — `general_vulnerability` is filled from Table 2's impact categories as a self-declaring derived sentence, `Mitigation Connection` is skipped, and Action Type is now **inferred** by a standing consultant-invariant tier/guardrail rule ([`action-type-tiers.csv`](./mitigateny/skills/action-type-tiers.csv)) validated against the live Actions source. `hazard_of_concern` follows the **Suffolk silence = No** rule, so all 17 hazards resolve to Yes or No — Layer 1 now states it once, for every county. Zero `gap-no-target` rows remain and only **five** fields have no home at all, tracked in a generated `omissions-register.csv`. **The most portable finding: the workbook is not the schema** — three declared gaps turned out to be real live columns missing from the workbook tabs, and Layer 1 has a new subsection plus a one-line `dms raw get` recipe for checking. **Phase 5 mostly done too:** identity resolved (Glen Cove → 3629113, Rockville Centre), and both the **file manifest** and the **jurisdiction alias table** are built — 52 folders → 52 geoids, zero collisions, every file choice reasoned. **Finding: Freeport is not a Hagerty annex** — its "Jurisdictional Annex.pdf" is the Village's own standalone 177-page hazard mitigation plan and needs its own crosswalk. Also settled: the 18 attendance-matrix jurisdictions with no annex are **all incorporated Villages marked Withdrawn — zero CDPs** — so Roles + Participation load for all **70** jurisdictions and everything else for the 52; the alias table is now 70 rows. **Phase 5 fully closed 2026-08-21** — all five live schemas read, pre-load flags 70/70 ready, HOC verified as 884 updates + 0 inserts. Also corrected a stale claim I had been repeating: the flat-dataset load path is **not** an open question (closed by the Suffolk load 2026-08-17 — `dms raw create` + `dms dataset update`, no workbook needed). Next: batch-extract — no consultant actions workbook exists, so all 234 proposed + 284 prior actions and 143 worksheets are ours.
- [ ] [MNY actions — cleaned + located derived source (`actions_cleaned`)](./mitigateny/tasks/current/mny-actions-cleaned-source.md) — **BUILT + PUBLISHED 2026-08-14: source `12453` / view `13192`** (`gis_datasets.s12453_v13192_actions_cleaned`, 16,948 rows, all checklist items verified). New `data-types/mny/actions_cleaned/` plugin applying every recommended transform (schema hygiene, keep-richer dedup −1,960 rows, boilerplate flag, priority normalization + companions, 671 recovered-coordinate overlays — low-confidence named roads excluded per owner) and joining `wkb_geometry` + `precision` from Actions Location 12463. **Non-destructive** — sources 1029065 and 11725 untouched. Open: owner deploy (server route + Create page), and 530 newer actions have NULL precision until `actions_location` re-runs and this re-publishes against the fresh view.
- [ ] [MNY actions — `actions_cleaned` absorbs the location waterfall (one unified source)](./mitigateny/tasks/current/mny-actions-cleaned-unify-locations.md) — **SHIPPED 2026-08-24: view `13272` published + verified.** The waterfall (now at `data-types/mny/_shared/location/`, one resolver shared by both plugins) runs in-process in the `actions_cleaned` worker; the 530 NULL-precision rows resolved (501 p3 / 15 p4 / 14 p0) with **zero content diffs** on previously-coded rows; tiles verified live. Consumer audit: **zero live bindings** to source `11725` → recommendation freeze-don't-delete, plugin kept diagnostic-only. Unblocks the county Actions Dashboard build (binds `12453`/`13272`). Remaining owner calls: 11725 fate, publish cadence, and the server/frontend deploy (descriptor changed: `locationsView` → `jurisdictionsView`+`countiesView`).
- [ ] [MNY actions — recover site coordinates from text (backfill)](./mitigateny/tasks/current/mny-actions-location-recovery.md) — IN PROGRESS (2026-07-16). Extract/geocode site coordinates from each action's text and write them into the production actions source in `dms-mercury-3` (source 1029065 / view 1074456). Implements the `reports/location-from-text.html` findings. **2026-08-14:** the `--apply`-to-production step is parked — the recovered CSV is now consumed non-destructively by the `actions_cleaned` task above.

## Landbank

### themes

- [ ] [Landbank admin panel — design mockups (Phase 1)](./landbank/tasks/current/landbank-admin-panel/README.md) — flat HTML mockups (dashboard · spreadsheet-edit table · new-property address→modal form · single-property view card · edit form) in `src/themes/landbank/design_system/pages/`, re-theming the Fable‑5 `dashboard.html` into the current landbank DS, using the real ACLB dataset (DAMA source 3). Interlink public pages + admin via a user menu. Phase 2 (live data/filter/edit wiring) deferred.

### content

- [ ] [Landbank admin dashboard → live DMS page](./landbank/tasks/current/admin-dashboard-dms-page.md) — build `admin-dashboard.html` as a real DMS page in `app=landbank`, `type=dev2`, bound to the single ACLB properties source, including a create-new-property modal section. Component-by-component data bindings + platform/theme gaps are enumerated in the task file. **As of 2026-08-13 the page mirrors the design across every band**; what remains on it is the neighborhood eyebrow/table header strip, the owner-decision items (6-bucket status regroup, section-title rule) and the 🔴 G12 DDL blocker on add-parcel.
- [ ] [Landbank admin panel — the three remaining live pages (view · edit · table)](./landbank/tasks/current/admin-panel-detail-pages.md) — owner direction 2026-08-13, immediately after the dashboard reached design parity. Phase 1 mockups already exist for all three. ⚠ **Two slugs are already committed in two places each** — the dashboard table's row actions point at `/admin/property-view?id=<ogc_fid>` and `/admin/property-edit?id=<ogc_fid>`, and the add-parcel modal navigates to `/admin/property-edit?id=<newId>` on save. Build order: view (proves the `?id=` plumbing) → edit (adds the write path) → table.

## Tessera

### content

- [ ] [Set up tessera.so landing, features, and docs pages on design_system_v6](./tessera/tasks/current/tessera-v6-landing-pages.md) — all 3 pages built as drafts on `test|pages:pattern` (`app=tessera-test`, id 3) with `tessera_v6` theme applied, verified live in both light and dark. Left unchecked pending human review + `dms page publish`; also flags a docs SideNav follow-up and 2-3 minor content-fidelity gaps (editor illustration, invite form, one CTA-button-row layout nit).

## Shared

_(no active items — platform-level dms-template work that isn't tied to a single project goes here:
shared theme infrastructure, `data-types/`, deployment/build config, CLI/tooling.)_
