# NPMRDS Design System v2 → report page & report library implementation

**Project:** TransportNY · **Topic:** themes · **Status:** IN PROGRESS — Phase 1 (rail-width mechanism) DONE, Phase 2 (`ReportPageHeader` component) DONE, Phase 3 (RRL restructure, first design push) 6.5/7 sub-items DONE — only Map graph-type support remains, genuinely blocked (no reference example to compose against yet). Template row `2187021` propagation gap (Phases 1-2 weren't reaching new reports) also DONE. **Design push #2 (Quick Controls + storage-model rework, superseding parts of Phase 3) — ALL 8 SUB-PHASES DONE AND LIVE-VERIFIED, 2026-08-06** — see "Design push #2 — IMPLEMENTATION COMPLETE" below. All dated 2026-08-06. Plan confirmed for **the single report page** (`npmrds-report.html`, build now) and **the report library** (`npmrds-reports.html`, Ryan's eventually — Alex's design is ~90% done). Ryan's active/eventual scope, narrowed 2026-08-06. This is the top-priority reports/routes item, superseding `dynamic-reports-and-route-tags.md`. The other 4 surfaces in the design set (`npmrds-home.html`, `npmrds-macro.html`, `route-comparison.html`, the MAP-21 family) are **Alex's purview for now** (status unknown to Ryan) — kept below as reference research only, not part of the active plan. · **Started:** 2026-08-06

**2026-08-06, later same day — a second design push landed** (commit `deabb6e`, superseding the version Phase 3's route-rail/Add-Graph-modal tables below were scoped against). Moves several route-rail controls onto a per-graph "Quick Controls" surface and requires a real storage/data-flow change, not just a component reshuffle — see **"Design push #2 — Quick Controls + storage model rework"** below (after "Next steps") for the full comparison and the resolved storage-model plan. **Graph/chart visual fidelity is explicitly out of scope for this round** (Ryan, 2026-08-06) — stays tracked under `report-page-redesign.md` gap 03 / this doc's existing graph-card-contract note, not part of this push.

**2026-08-06, same day, third pass — phase breakdown drafted, not yet built.** Confirmed the previously-flagged `pageState`/`sectionHeaderExtensions` wiring gap is real (re-read `section.jsx` directly). Read the actual current source (`QuickControls`, `useGraphPublish`, `useReportRow`, `RouteRow`, `AddGraphModal`, `useAddGraphSection`, `composeMeasureConfig`) and the mockup's own JS (not just this file's earlier summary) to ground an 8-step lettered phase breakdown (a-h) — see "Phase breakdown (drafted 2026-08-06, not yet built)" under Design push #2 below. Key finding: `applyMeasurePickToState` already persists the FULL pick verbatim (`state.display._measurePick = pick`), so the new `weekdays`/`start`/`end`/`routeIds` fields need no new storage mechanism — the real work is entirely in what reads/writes/consumes them. Not yet implemented — awaiting go-ahead to start (a).

**2026-08-06, same day, fourth pass — ALL 8 PHASES (a-h) BUILT AND LIVE-VERIFIED.** Ryan: "it doesn't matter if the report is functional at each step... otherwise, you can just implement the whole plan, looks good to me" — implemented straight through without pausing between phases. Full detail in "Design push #2 — IMPLEMENTATION COMPLETE" below (replaces the phase-breakdown-only status). Two real bugs found and fixed during live verification (not caught by code review alone): Quick Controls rendering on unrelated Spreadsheets, and the Mode pill's text overflowing its icon-sized box. The core value proposition — the same route answering two different questions for two different graph cards — is verified via an actual live network capture (identical route/TMCs, two different epoch ranges), not just a code-review claim.

## Objective

Bring two pages of Alex's redesign onto the real, already-built (or, for the library, not-yet-built) surfaces:
1. `npmrds-report.html` + `npmrds-report.js` — the individual report canvas — onto `ReportRouteList`/`RouteRow`/`RouteTagBrowserModal`/`AddGraphModal`, plus a new second custom component (a report page header). **Active now.**
2. `npmrds-reports.html` — the report library — a net-new page with no live equivalent, reversing a prior "permanently out of scope" ruling. **Ryan's eventually, blocked on Alex finishing the design** (per Ryan, 2026-08-06: "~90% done").

Both are genuine second-pass UX work — not reskins — built, per Ryan (2026-08-06), with the current live implementation in mind.

**Scope note (Ryan, 2026-08-06, in two messages):** *"I am focused only on the reports page / single report page. The map-21 stuff, other design files, I am not sure the status of. Those fall under Alex's purview for now."* Then, clarifying further: *"reports lib I think is mine eventually, Alex is only like 90% done with that design. But yeah, the map21, macroview, all that is Alex for now."* Net effect: the report canvas and the report library are both Ryan's (this doc), at different points of readiness; `npmrds-home.html`, `npmrds-macro.html`, `route-comparison.html`, and the MAP-21 family stay with Alex. Those four were already researched in this doc's first pass (2026-08-06, before this narrowing) and are kept at the bottom under "Reference: the rest of the design set" so that work isn't lost — but none of it is part of the active plan, decisions, or phasing below.

## Sources

- Design: `src/themes/transportny/TransportNY Design System/dms_design_system_v2/pages/npmrds-report.html` + `npmrds-report.js` (the one interactive mockup in the set), plus `theme/theme.js` for the shared token/style additions it needs. Parent folder's `README.md` documents the cross-page contract this page still has to honor (nav, freshness strip, container width) even though the other pages aren't Ryan's concern right now.
- Critique: `research/npmrds-category-design/critique-round-1.md` (2026-07-29) — the must-fix items that named `npmrds-report.html` specifically (1600px→1480px container, compound-card gap, inert sticky rail) are still relevant; its Phase 8b tightening pass (same day) applied most of them, but wasn't re-verified against the current file in this pass since the report-canvas page itself went through a *later* redesign (Phases 9-14, 2026-08-05).
- **Missing:** `planning/transportny/tasks/current/npmrds-category-design-set.md` — referenced 3× as the authoritative task file (real cross-page contract, an Escalations table, verified numbers, Design notes, Phase 6/8 history). Confirmed absent from the whole repo. Ryan is asking Alex directly (Decision 0 below) — it likely has report-canvas-specific decision history too, not just cross-page material.
- Live components: `src/themes/transportny/components/{ReportRouteList,RouteTagBrowserModal,AddGraphModal,MeasurePicker}/`
- Related in-flight task files: [`dynamic-reports-and-route-tags.md`](./dynamic-reports-and-route-tags.md) (built RRL/modals through 2026-08-05, explicitly anticipated this design work arriving "later"), [`report-page-redesign.md`](./report-page-redesign.md) (gaps 01-03, card visual/density polish — continuous with this doc's graph-card-contract items), [`reportroutelist.md`](./reportroutelist.md), [`report-route-ui-parity-gaps.md`](./report-route-ui-parity-gaps.md) (its "permanently out of scope" ruling on report discovery was amended 2026-08-06 — see the reference section below, not relevant to the active plan here).

---

## The single report page — active scope

`npmrds-report.html`/`.js` is a genuine second-pass redesign (Phases 9-14, all dated 2026-08-05) of `ReportRouteList` / `RouteRow` / `RouteTagBrowserModal` / `AddGraphModal` — real UX changes, not a reskin. The mockup is itself a transcription of a real live page (`npmrds_sub` pattern, page `2195810`, "NY-9D Beacon Signal Study"), stress-tested with extra cards borrowed from two other real reports.

### Layout
- Rail becomes a **fixed 340px flex sibling** of the canvas column (not a grid column — `col-span-3`/`col-span-9` was explicitly rejected in the mockup's own comments), `sticky top-0 h-svh`, no padding, full tab height. Theme key `pages.sectionGroup` needs a second named style ("flush") beyond the current single 302px style. **Decision 2 (below): use the design's named-style approach** — Ryan's own stashed core change (`sidebarGroup.width` on `sectionGroup.jsx`, prototyped the same day, 2026-08-05) is not being merged.
- No breadcrumb band on this page (deliberate — makes the rail's `sticky top-0 h-svh` math exact; a breadcrumb strip would need to offset both `top` and height).
- Page header is **not** a full-bleed band — it's a card-styled section inside the content column: kicker+meta (region/county/agency, a "published" pill) → h1+purpose → an action stack (Data/Share/Print/Edit) → a full-width freshness footline. Dynamic enough (edit-mode-aware action, multi-sibling-field kicker meta, a computed freshness line) to justify a **second custom component**, per Ryan relaying Alex's suggestion (2026-08-06). Proposed name: `ReportPageHeader` (exact name TBD), alongside `ReportRouteList` as this page's two custom components.
- Compact 64px SideNav on this page (340px rail + 256px expanded nav would starve the canvas).
- Container width: `max-w-[1480px]` (an earlier draft used 1600px — a critique must-fix normalized it back to the catalogue's standard 1480; port 1480).
- Graph-card contract (header shape, legend-below-plot not floating, one-line attribution, size-4 tick/legend collapse) is real polish work, continuous with `report-page-redesign.md`'s existing gap 03 (card visual/density polish) — track as a continuation there, not duplicated here.

### Route rail — per-route controls (`RouteRow.jsx`)

**SUPERSEDED 2026-08-06 (later same day) by the "Design push #2" section below** — the date/time-facet row and the graph-assignment row in this table describe the *first* mockup version (weekdays/time-of-day/graph-assignment still route-owned). The second design push moves those off the route entirely. Table kept for history; don't implement date/time/graph-assignment against this version.

| Item | Live today | Design wants | Gap size |
|---|---|---|---|
| Row disclosures | 4 independent `useState`s (dates/deps/color/menu) + row expand | Same shape, restructured chrome | small |
| Kebab menu | "⋮" menu, 2 items (Rename, Remove) | No kebab — same 2 actions (pencil+trash) always visible inline | trivial |
| Color control | Inline: expand row → click "Appearance" → `ColorPicker` mounts inline in the open-out | Popover anchored directly on the row's color dot, portalled (the rail's scroll region is `overflow-hidden`, needs a portal like the shared `Popup` primitive) | small-medium (new portal pattern for this control) |
| Date/time editing | 2-field framing ("Start date+time / End date+time") in the UI, even though internally already 3 facets (day-range loop + weekday mask + epoch range) | Explicit 3-facet UI: **Dates** (live "N of M days" count + **shift ±1yr** pills) → **Days** (weekday mask + Weekdays/Weekends/All — already live) → **Time of day** (5 presets, hours printed on the pill — already live) | UI restructure surfacing the real mental model; **shift ±1yr is net-new** (today only reachable indirectly via Derived-mode's period options, never for a route's own fixed dates) |
| Copy/paste window | **absent** — grepped, no hits anywhere | Per-row copy icon + paste icon (disabled until a copy exists) + a rail-top "paste into all (N)" strip; derived-date routes always skipped | **net-new feature**, moderate build |
| "Base for N routes" | Live: `dependentsRow`, a standing one-liner + pill list (`RouteRow.jsx:481-493`) | **Not drawn anywhere in the mockup** (its only worked example is a 1:1 derive relationship) | Confirm with Alex this wasn't meant to be dropped — likely an under-tested example, not a real removal ask |
| Derived-date mode | `Switch` + `relativeDatePresets.js` (Offset/Same-period/Advanced) | Same mechanism, single-hop only (already enforced live) | matches |

### Add Route modal (`RouteTagBrowserModal`)
- Live: search+recent list, then 5 **equal-weight** category tiles (County/Region/Agency/Auto-generated/Other tags); result rows show name + TMC count only, no tags.
- Design: same search-first shape, but only **3** tag axes as header pills (County/Region/Agency), with Auto-generated/Other-tags demoted to plain text links; every result row shows its tags inline as chips ("name + TMC count can't separate 'NY-9D NB (Beacon)' from 'NY-9D NB · Main St to Verplanck'").
- Gap: small restructure (5 tiles → 3 pills + 2 links) + inline tag chips on result rows (currently absent). The already-added-routes-hidden-only-in-the-unscoped-recent-list nuance **already matches live** (fixed 2026-07-31, same rule the design states).

### Add Graph modal (`AddGraphModal`)

**SUPERSEDED 2026-08-06 (later same day) by the "Design push #2" section below** — the second design push inserts a new "When" step and renumbers Routes; shape cards/measure dropdown described below are still accurate.

- Live: exactly **3** graph-type options (Bar/Line/Grid), all producing an "AVL Graph" chart section only — `useAddGraphSection.js:7` hardcodes the element type, confirmed no Spreadsheet/Map branch exists anywhere in this modal. Flat 9-item measure select (no grouping). 3 inline SVG glyphs already exist (`BarGraphGlyph`/`LineGraphGlyph`/`GridGraphGlyph`).
- Design: **5** shape cards — adds **Table → Spreadsheet section** and **Map → Route Map section** — plus a grouped measure `<optgroup>` (Speed/Travel time/Delay/Emissions), default pick changed to Line·TravelTime·Hour (from Bar·speed·5min), picks persisting across repeated modal opens.
- Gap: **the single largest net-new-capability item in this plan.** `composeMeasureConfig`'s compose-a-fresh-section-from-a-pick mechanism is chart(AVL-Graph)-specific; the Table/Map cards need a fresh `Spreadsheet` defaultState and a fresh `Map` defaultState composed the same principled way — no existing analog to reuse verbatim the way the chart path had. Everything else (grouping, default pick, persistence-across-opens) is small config-level work.
- Note: a **second, older** measure-editing surface still exists for already-created graphs (an amber edit-bar "measure" button opening 4 flat `<select>`s via `toggleMeasurePicker`, sharing one global `measurePick` state rather than per-card). A separate, smaller pre-existing inconsistency the mockup doesn't address — flagged, not scoped for this pass unless it blocks something.

### Constraints any redesign must respect (from `ReportRouteList/README.md`, all still binding)
- RRL never writes into a graph section's row (read-only discovery via `findSelfBoundGraphs`)
- key graph/route identity by `trackingId`, not `id` (the orphan-cleanup race fix, 2026-08-03)
- never set the generic `hideInView` flag on this component — it self-hides via its own `if (!isEdit) return routeSelectionModal`
- two edit-gates required (`editPageMode` AND the section's own `isEdit`), not either alone
- route content lives in the report's own `reports_snap_2` row — no Publish/Discard semantics, ever
- route names must stay unique (comparison-series' sole series discriminator)
- derived dates are single-hop only (a derived route can't itself be a base)

---

## The report library — active scope, blocked on Alex's design

`npmrds-reports.html` is a "TEMPLATE SHELF" (12 real templates in 5 typed groups, each card a 4:5 SVG "preview plate" derived from the template's own `graph_comps[].layout`), "your reports" (3 real + empty state), "worked examples" (4 real converted pages), and a **real, working** search dialog (57-row live sample, `<mark>` highlighting, URL-bound query/facets, 3 working facets + 4 disabled-with-tooltip facets explaining exactly why each isn't wired). Per the critique's A/B ruling (already applied): this inline/URL-bound treatment shipped; a command-palette alternative was deleted and logged as an escalation, not built.

**Live:** does not exist. A reports-library/browsing page had been **explicitly out of scope, permanently** — `report-route-ui-parity-gaps.md`: *"Not in scope, permanently: folders, report discovery/browsing, permissions... per user direction 2026-07-27"* — reaffirmed 2026-07-31.

**Decided 2026-08-06:** reopened — this is Ryan's page eventually, not Alex's to build. *"we dont have one built yet but we will want one"*, then *"reports lib I think is mine eventually, Alex is only like 90% done with that design."* Amended in `report-route-ui-parity-gaps.md` and `dynamic-reports-and-route-tags.md` (permissions/ACL remains the one still-standing exclusion from the original ruling).

**Status: blocked on Alex's design reaching 100%.** Don't start building against `npmrds-reports.html` yet — check with Ryan/Alex on design completion first. When it's ready:
- Needs a real queryable index of report metadata (869 reports, with denormalized route chips per report per the critique's Honesty §5) as a data-shape prerequisite, not just UI work.
- Sequenced after the report-canvas work in the phased plan below, both because of that prerequisite and because the design isn't finished.

---

## Cross-cutting findings (scoped to the report page)

### A. Theme additions this page needs
- `pages.sectionGroup` needs a second named style ("flush", 340px, no padding, `sticky top-0 h-svh`) — both live theme files today have a single fixed-302px style, no style-selection mechanism at all. **Decision 2: add it directly to `theme.js`/`themev2.js`, don't use the stashed core mechanism.**
- Micro-type tokens the page's attribution lines/quick-control pills/kicker labels actually use, per the critique's must-fix #8: `chromeLabel` (10px/400 mono uppercase) and `chromeTick` (9px/400 mono) cover this page's 9/9.5/10/10.5px cluster; `chromeNav` (13px/500) covers the compact SideNav this page also uses. Declare-to-match, no page edits needed beyond declaring them.

### B. The missing `npmrds-category-design-set.md`
See Decision 0. May contain report-canvas-specific decisions beyond the cross-page contract — fold anything recovered back into this section.

### C. Two custom components in scope
1. **`ReportRouteList`** (exists) — restructure per the route rail diff table above; net-new sub-features (copy/paste window, color popover, Table/Map graph types).
2. **Report Header** (net-new, per Ryan 2026-08-06 relaying Alex's suggestion) — the page-header card described above. Justified as a real component (not pure Card config) per `src/themes/CLAUDE.md`'s bar: edit-mode-aware dynamic behavior, multiple-sibling-field reads, computed content a `formatFn` can't express.

---

## Decisions (Ryan, 2026-08-06)

**0. Missing task file — Ask Alex.** Ryan will ask Alex directly whether he has `npmrds-category-design-set.md` (his machine, a different branch). Not reconstructed here.

**Assessed 2026-08-06: not blocking for the report canvas, doesn't gate starting the phased plan.** Checked what the file actually held (via its 3 citations + how extensively `critique-round-1.md` quotes it): the cross-page contract is duplicated verbatim in `npmrds-home.html`'s header comment (already captured above); all 5 Escalations-table items are about `npmrds-macro.html`/`npmrds-reports.html`, none about the report canvas; the verified-numbers ledger covers aggregate stats the report canvas doesn't show; of the 7 Design notes only #1 (gap-6 compound-card bug) and #3 (type debt, generic) touch this page, and both are already quoted in full in the critique doc. The report canvas's *own* most recent redesign (Phases 9-14, 2026-08-05, a week after the critique) isn't in the missing file at all — it's self-documented directly in `npmrds-report.html`'s own comments (control inventory, design rules, "try this" walkthrough), already fully read. The one place recovery could still help is a provenance question (the "Base for N routes" mockup-omission, above) — better resolved by asking Alex directly than by finding the doc.

**1. Reports library — reopened, Ryan's eventually.** See "The report library" section above. Blocked on Alex finishing the design (~90% done as of 2026-08-06); don't start building yet.

**2. Rail-width mechanism — use the design's style only.** Add "flush" directly to `pages.sectionGroup` in the theme files. Ryan's stashed core `sectionGroup.jsx` change is not being merged — it's still sitting in a local git stash; worth a `git stash drop` (or `git stash list` to confirm which one) once Ryan's ready to let it go. Not done here since dropping a stash is a destructive, easily-lost action.

**DONE 2026-08-06.** Resolved the one open question Decision 2 left unstated — how a page opts into the new style, since `pages.sectionGroup` had zero selection mechanism (flat object, no `styles[]`). Confirmed via the mockup's own HTML comment (`npmrds-report.html` ~line 338: `pages.sectionGroup styles[1] "flush"`) that a real `styles[]` array was always the intent. Implementation:
- `src/dms/packages/dms/src/patterns/page/components/sections/sectionGroup.jsx` (core, one line): `getComponentTheme(theme, 'pages.sectionGroup')` → `getComponentTheme(theme, 'pages.sectionGroup', group.theme)`. Reuses the band's existing `group.theme` field — already used one line above to select the band's `layoutGroup` style — as the *same* selector for the rail style. No new field, no new authoring surface.
- `src/themes/transportny/themev2.js` `pages.sectionGroup`: restructured from a flat object into `{ options: { activeStyle: 0 }, styles: [{name:'default', ...byte-identical to the old flat object...}, {name:'flush', ...340px/no-padding/sticky top-0 h-svh/gap-0 contentRow, per the mockup's rail markup...}] }`.
- `theme.js` (v1) was **not** touched — it has no live consumer for a flush rail (no `contentRow`/`contentCol`/nav* keys defined there at all today; it relies entirely on `sectionGroupTheme`'s core fallback), and inventing untested values with nothing to verify them against was judged worse than leaving it alone. Revisit only if a v1 (non-v2) page ever needs this.
- Safety: `getComponentTheme` falls back to `styles[0]` for any unmatched/undefined selector (confirmed by reading its implementation), so every existing band (`group.theme` unset, `'default'`, `'content'`, etc.) is unaffected — only a band explicitly set to `group.theme === 'flush'` gets the new style. `InPageNav.jsx` still reads the key with no selector (always `styles[0]`), but it returns `null` before using any style value whenever a band has no `navLabel`'d sections — true for a flush rail band, so this doesn't need to change for Phase 1.
- Verified: dev stack up (vite :5173, dms-server :3001); `report_probe.mjs` against `converted_reports/page_13_13` (a live page using the default-style rail) — 0 page errors, screenshot confirms the "On this page" rail renders unchanged (302px, right side, nav card + Related card).
- **Live demo (2026-08-06):** flipped the `default` band's `theme` to `'flush'` on **`converted_reports/ny9d_beacon_spec_test`** (page 2195822) via `dms page update --data` — **draft-only** (`draft_section_groups` changed, `section_groups`/published untouched, so the public URL is unaffected). Verified with a Playwright `getBoundingClientRect`/`getComputedStyle` probe against the real rendered DOM, not just a screenshot: the rail wrapper is exactly `w-[340px] shrink-0 hidden xl:block bg-white border-r border-zinc-950/10` (`width: 340`), its inner wrapper is `sticky top-0 h-svh overflow-hidden` (`position: sticky`, `top: 0px`, `height: 1000px` = the full `window.innerHeight`) — matches the mockup's spec exactly. To see it live: mint a dev token (`scratchpad/npmrds-sub/mint_token.sh`), log in, visit `http://npmrds.localhost:5173/edit/converted_reports/ny9d_beacon_spec_test`. Left as-is (draft-only, reversible by flipping the band's `theme` back to `'content'`) so Ryan can look at it directly; not reverted automatically.
- **Follow-up fix, same session:** Ryan caught (looking at the live demo) that the rail still sat ~48px off the SideNav — a second, independent padding source. Root cause: `group.theme` selects a style in *two* theme trees (`pages.sectionGroup` for the rail, and `theme.layoutGroup` for the band's own wrapper — pre-existing, one line above my sectionGroup.jsx edit), and only the first got a `'flush'` entry. `theme.layoutGroup.styles` had no `'flush'` name, so the band fell back to `styles[0]` (`'content'`), which still carries `pl-12 pr-8`. Fixed by adding a `layoutGroup.styles` `'flush'` entry (`wrapper1: 'w-full bg-[#ECEEF2]'`, `wrapper2: 'w-full'`, `wrapper3: ''` — no cap, no gutter) and moving the inset onto `pages.sectionGroup` styles[1]'s `contentCol` (`px-8 py-8`, per the mockup's own content-column div) so the rail stays flush at the true edge while the graph cards keep a clean inset. Re-verified on the same demo page (no DB change needed, theme-only): rail now measures `left: 256` (exactly the SideNav's width, zero gap), band wrapper padding `0px`. Screenshot re-sent to Ryan.

**4. Priority — yes, top priority.** The report-canvas + report-library work above supersedes `dynamic-reports-and-route-tags.md` as the top-priority reports/routes item (noted in that file and in `todo.md`).

*(3 — MAP-21 consolidation — was never actually asked, and is now moot for this doc: MAP-21 is Alex's purview, see the reference section below.)*

---

## Phased plan

1. **DONE 2026-08-06 — Rail-width mechanism** — added the "flush" named style to `pages.sectionGroup` in `themev2.js` (not `theme.js`/v1 — see Decision 2). See Decision 2 for the full implementation note. Unblocks the rail work below.
2. **DONE 2026-08-06 — `ReportPageHeader` component.** New section-component directory `src/themes/transportny/components/ReportPageHeader/` (`ReportPageHeader.jsx`, `.theme.js`, `index.jsx` registry entry — same 3-file split as `ReportRouteList`), registered in `themev2.js`'s `pageComponents`. Built per `src/dms/skills/creating-page-section-components.md`'s recipe (`useDataSource: true, useDataWrapper: true`, `EditComp === ViewComp` with inline `isEdit`-conditional fields, same two-gate `editPageMode && sectionEditorOpen` convention as `ReportRouteList`).
   - **h1 and the published/draft pill read the page's own `item.title`/`item.published` directly** (real page fields, not duplicated into section state) — everything else (kicker label, meta line, purpose paragraph, freshness footline, an optional Data-button link) is this component's own authored `state.display.*`, edited **inline in place** (no separate config-panel controls) when the section's own pencil is open.
   - **Scope calls made without asking** (per "guess and flag, don't gate" — flagging here, not blocking): (1) freshness footline is **author-typed text fields**, not a live query against the underlying data source's actual date coverage — computing it live would be a materially bigger, separate effort and wasn't asked for; (2) the "Data" button only renders when an author has set a `dataHref` (or is in edit mode, to set one) — there's no unified "download this report's data" endpoint to wire it to yet, so it doesn't fabricate one; (3) Share = copy current URL to clipboard, Print = `window.print()` — both simple, real, working actions rather than placeholders; (4) the Edit button navigates between `/edit/<slug>` and `/<slug>` (`editPageMode` is route-determined, confirmed via `siteConfig.jsx`'s `edit/*` route — there's no separate in-page toggle to hook instead).
   - Note: the mockup's later Phase-12 commentary (in `npmrds-report.html`) says the *rail's own* edit toggle was removed so route controls are "always live," independent of page edit mode — that's a Phase 3 (RRL) question, not touched here, and is a real tension with `ReportRouteList/README.md`'s documented two-gate constraint worth resolving explicitly when Phase 3 starts (flagging per "flag standing-decision reversals, don't reconcile quietly" — not resolved silently either way).
   - **Two real bugs found by Ryan live-checking this, both fixed 2026-08-06:**
     1. **Draft/published pill always showed "draft."** My check was `item?.published === 'published'` — a value that never occurs anywhere in the codebase. Read the actual state machine in `editFunctions.jsx`: `published: 'draft'` means pending unpublished changes (set at page creation); `published: ''` (empty) means clean/published — set by both `publish()` and `discardChanges()`. Fixed to `item?.published !== 'draft'`, matching the exact idiom already used elsewhere (`editPane/index.jsx`, `pagesPane.jsx`: `item.published === 'draft'` for `hasChanges`). Re-verified live: pill now correctly reads PUBLISHED, bottom toolbar shows "NO CHANGES."
     2. **Empty rail space in view mode** — a real, pre-existing gap Ryan found, unrelated to the Phase-1 mechanism itself but directly exposed by it (the new 340px white flush rail made empty space much more visible than the old subtle style). Root cause: `sectionGroup.jsx`'s `showRail` was computed purely from `item.sidebar` being truthy, with zero awareness of whether the sidebar group's content (`ReportRouteList`) actually renders anything — RRL's own recent fix (`if (!isEdit) return routeSelectionModal`, documented in `dynamic-reports-and-route-tags.md`'s "View-mode visibility" section) returns `null` in view mode for a normal report, but the rail's fixed-width wrapper doesn't know that and renders anyway.
        - **First attempt (WRONG, caught by Ryan same session): `showRail = ... && (edit || !item?.sidebarHideInView)`.** This gates the rail at the JS/React level — when the flag is on and we're outside edit mode, the entire rail (including `SectionArrayComp`, which is what mounts `ReportRouteList`) never renders at all. That silently swallows RRL's Dynamic-Report entry-gate modal exactly like the *original* generic `hideInView` bug already fixed once (same doc, same section) — RRL never gets a chance to decide "actually I need to show the picker." **Ryan caught this live**: toggled the NY-9D Beacon report into a Dynamic Report, published, viewed — no modal. Confirmed by direct test (below) that this was a real regression from my flag, not a "converting an existing report" artifact.
        - **Fix: collapse via CSS `:has(...:empty)`, not a JS-level gate.** `showRail` is back to the original `Boolean(item?.sidebar) && group.name === railGroupName` (rail — and therefore RRL — always mounts). A new `collapseRailIfEmpty = Boolean(item?.sidebarHideInView) && !edit` instead adds `has-[.dms-rail-content:empty]:hidden` to the rail wrapper's className, and `sideNavContainer3` gets a `dms-rail-content` marker class. This only visually collapses the rail's width/chrome once RRL has actually rendered nothing (a real empty DOM, checked after render) — RRL still always mounts, so it can still render its entry-gate modal when needed. That modal is a `createPortal(..., document.body)` (`ui/components/Modal.jsx`), so it displays fully regardless of the (possibly CSS-collapsed) rail wrapper's own visibility.
        - **Verified live, both cases, on the demo page** (temporarily added/removed a `routeSlots`-typed `item.filters` entry to switch it between Dynamic/normal — the exact mechanism `ReportRouteList.jsx`'s "Dynamic Report" toggle itself writes, then restored the page's original filter state after): (1) Dynamic Report, no `?routes=` → the "Add Routes" blocking modal now shows correctly. (2) Normal report (or resolved Dynamic Report) → rail fully collapses, content reflows full-width, no empty space. (3) `/edit/...` → full rail, unaffected, in both cases.
        - Also added `template.sidebarHideInView` to `newPage()`'s template-copy block (`editFunctions.jsx`) so pages created from a template with this flag inherit it automatically, and set `sidebarHideInView: true` on the actual **Report Page template (id 2187021)** — every future report page gets it.
        - **Two more real bugs found by Ryan re-testing live, both fixed same session:**
          1. **`:has(...:empty)` doesn't actually work.** Confirmed by DOM inspection on Ryan's exact failing URL (a *resolved* Dynamic Report — routes provided, no picker needed): `SectionArrayComp`/`dataWrapper` always emit real wrapper markup (a 12-col grid div, a per-section padding div, dataWrapper's own inner divs) around whatever a section returns — so the rail's DOM is never actually `:empty` even when `ReportRouteList` itself returns bare `null` deep inside all that scaffolding. **Fix:** RRL now renders an explicit, deliberately-invisible `<span className="hidden dms-rail-collapsed" />` marker instead of bare `null` when it has nothing to show (`ReportRouteList.jsx`); `sectionGroup.jsx`'s rail wrapper looks for `.dms-rail-collapsed` via `:has()`, which finds it at any depth regardless of the scaffolding in between (unlike `:empty`, which requires zero children on the checked element itself).
          2. **The `has-[...]:hidden` class had zero effect even once the marker was found** — root cause was a *different* one-off Tailwind cascade fight (`hidden xl:block`, already on the wrapper, won at equal specificity depending on generated rule order) — fixed with Tailwind v4's postfix `!` important modifier: `has-[.dms-rail-collapsed]:hidden!`.
          3. **Even after both of those, the flag still had no effect on the real public URL** — `item.sidebarHideInView` was reading as `undefined` client-side despite being `true` in the DB. Root cause: `siteConfig.jsx`'s `PageView` route (the public `/*` route) fetches an **explicit Falcor field allowlist** (`filter.attributes`), and a brand-new field invented this session obviously wasn't in it — silently dropped, no error. Added `'sidebarHideInView'` to that list, next to the existing `'sidebar'` entry. This is *why* the very first "verified" pass looked correct (edit mode has no such filter and always worked) while the public route silently never had the flag at all.
        - **Fully re-verified, all four states, on Ryan's actual URL and the demo page**: (1) Dynamic Report, resolved (`?routes=...` matching, Ryan's exact URL) → rail fully collapses, no blank space. (2) Dynamic Report, unresolved (no `?routes=`) → "Add Routes" blocking modal shows. (3) Normal report → rail fully collapses. (4) `/edit/...` → full rail, unaffected. DOM-level checks (not just screenshots) confirm the wrapper's `display`/width in each case.
        - Files: `src/dms/packages/dms/src/patterns/page/components/sections/sectionGroup.jsx`, `.../pages/edit/editFunctions.jsx`, `.../patterns/page/siteConfig.jsx`, `src/themes/transportny/components/ReportRouteList/ReportRouteList.jsx` (all core submodule except the last).
   - **Verified live** on the same demo page (`converted_reports/ny9d_beacon_spec_test`, page 2195822): created a new `ReportPageHeader` component row (id 2208223) seeded with the mockup's own NY-9D copy, spliced into `draft_sections` (draft-only). Screenshot + 0 console/page errors confirm: kicker/meta/draft-pill render, `item.title` renders correctly as h1, purpose paragraph renders, Share/Print/Done buttons render (Done because `editPageMode` is true on `/edit/...`), freshness footline renders. Settings popup confirms `Type: ReportPageHeader` (proves registry wiring + section-type-picker visibility). **Not verified live:** the inline-edit fields actually appearing/writing when the section's *own* pencil (not just page edit mode) is opened — attempted via Playwright native-click automation and hit the exact hidden-gear-button flakiness `traversing-dms-pages.md` already documents (non-deterministic which hidden button is "first"); gave up after several attempts rather than keep forcing it. Structural confidence is high since the gating logic is a direct copy of `ReportRouteList`'s already-proven pattern, but this one path wants an actual click-through (by Ryan, or a future session) before calling it fully confirmed.
3. **RRL restructure — 6.5/7 sub-items DONE 2026-08-06, only Map (part of g) remains.** In this sub-order:
   - a. **DONE 2026-08-06** — Kebab → inline 2-icon (trivial). Removed the `menuOpen` dropdown (`kebabWrapper`/`kebabMenu`/`kebabMenuItem*`) from `RouteRow.jsx`, replaced with two always-visible `Button`s (pencil "Rename" → `onStartEditName`, trash "Remove from report" → `onRemove`) next to the existing reorder chevrons — same `isEdit &&` gate as before, unchanged. Removed the now-unused theme keys from `ReportRouteList.theme.js`. **Decision (Ryan, 2026-08-06): keep the current two-gate edit model** (`editPageMode && sectionEditorOpen`) for all of Phase 3 — the mockup's Phase-14 commentary calling for "always-live" controls (no page-edit-mode gate at all) is explicitly NOT being adopted; flagged rather than silently reconciled. Verified live (temporarily forced `isEdit={true}` in `ReportRouteList.jsx` to reach the row's true edit state around the known sidebar-Settings-trigger positioning bug — see item (b) below — then reverted immediately): inline pencil+trash render correctly, no dropdown.
   - b. **DONE 2026-08-06** — Date/time 3-facet UI restructure. Reorganized `RouteRow.jsx`'s date-editing block into three explicitly labeled facets in the engine's own order — **Dates** (start/end date+time, a live "N of M days" count, new **shift ±1yr** pills) → **Days** (weekday mask — already live, just re-labeled/reordered to come before Time of day, matching the engine's real order) → **Time of day** (the 5 existing peak presets, now printing their hours directly on the pill, e.g. "AM Peak · 6a–10a", instead of hover-only). Extracted `parseYMD`/`generateDateRange`/`generateEpochRange` out of `useGraphPublish.js`'s private closure into `utils.js` as shared exports (so the day-count preview enumerates identically to what the real query engine builds — no duplicated day-loop that could drift). New `facetWrapper`/`facetLabel`/`dateFacetFooter`/`dayCountText`/`shiftYearGroup` theme keys added; removed the now-unused `peakPresetLabel` key. **Verified live** (same temporary `isEdit={true}` forcing, reverted immediately after): "59 of 59 days" count correct for Jan 1–Feb 28 2025 (31+28, no exclusions), −1yr/+1yr pills present, Days facet (Su–Sa + Weekdays/Weekends/All Days) renders above Time of day (5 presets, hours printed), no console/page errors.
   - **Found along the way, not yet fixed:** the documented sidebar-group Settings-trigger positioning bug (`traversing-dms-pages.md` §2) is real and still open — spent significant effort trying to reach RRL's true edit mode via browser automation (multiple selector strategies, a full click-sweep of every on-page Settings trigger) and never found RRL's own trigger at all via the documented selector; worked around it by temporarily forcing `isEdit={true}` in source for verification, not a real fix. Whoever picks up `sectionGroup.jsx`'s rail rendering next (per that doc's own note) should look at this — it will keep making sidebar-group components annoying to browser-test.
   - c. **DONE 2026-08-06** — Color popover-on-dot. Replaced the "expand row → click Appearance → ColorPicker mounts inline" flow with `UI.Popup` (existing shared primitive, `ui/components/Popup.jsx` — portals to `document.body`, auto-positions around its trigger, closes on outside-click) anchored directly on the row header's color dot. The dot is now a real `<button>` (`colorDotButton`) when `isEdit && ColorPicker && Popup`; a read-only viewer still gets the old plain, non-interactive dot. Removed the old `colorOpen` state and the whole `colorSection`/`colorSwatchToggle`/`colorPickerBody` block and theme keys (replaced by `colorDotButton`/`colorPopoverBody`). `Popup` threaded through as a new prop, `ReportRouteList.jsx` → `RouteRow.jsx`, same as `ColorPicker`. **Verified live** (same temporary-`isEdit={true}`-then-revert technique as (a)/(b)): clicking the dot opens the full swatch/gradient/hue picker anchored right on the dot, fully visible outside the rail's `overflow-hidden` scroll region — confirms the portal actually solves the clipping problem it was meant to solve, not just visually similar to before.
   - d. **DONE 2026-08-06** — Add Route modal: 3-pill+2-link restructure + inline tag chips (`RouteTagBrowserModal.jsx`/`.theme.js`/`tagCategories.js`). Replaced the 5-tile `categoryGrid` with a `categoryPillRow` (County/Region/Agency — the 3 real, fixed/enumerable browse axes, from `TAG_CATEGORIES`) plus a `categoryLinkRow` below it (Auto-generated, Other tags — demoted to plain text links, same click handlers as before). Added `parseTags`/`tagToLabel` to `tagCategories.js` (a small local JSON-array parser mirroring `ReportRouteList/utils.js`'s `parseTmcArray` — kept separate rather than shared, since route tags and TMC arrays are different concerns that happen to share a parsing shape; `tagToLabel` maps a raw stored tag like `county:Dutchess`/`auto_generated` to its human label via `TAG_CATEGORIES`, falling back to the raw string for free-text/project tags with no fixed vocabulary). Every result row now shows its tags as small inline chips below the name/TMC-count line — the exact gap called out in the mockup ("name + TMC count can't separate 'NY-9D NB (Beacon)' from 'NY-9D NB · Main St to Verplanck'"). **Verified live** (same temporary-force-then-revert technique — this time forcing `canMutate` itself in `ReportRouteList.jsx`, since the Add Route button/modal is gated on it directly, not on `RouteRow`'s `isEdit` prop): screenshot shows every result row with real chips ("Auto-generated", "Region 11 - New York City", "Queens"/"Richmond"); DOM check confirms County/Region/Agency pills and both links render.
   - e. **DONE 2026-08-06 — folded into (b) above.** Shift ±1yr naturally belongs inside the "Dates" facet's own restructure (it's the same JSX/state as the day count), so it was built together with (b) rather than as a separate pass — see (b)'s note above for the implementation.
   - f. **DONE 2026-08-06** — Copy/paste window (net-new). Copies/pastes the whole window (dates + weekday mask + time-of-day) as one unit — matches the mockup's own reasoning ("the three facets only mean anything as a set... two buttons per row buys nothing" refers to NOT fragmenting copy/paste per-facet, not to omitting per-row buttons). Built:
     - `clipboard` state in `ReportRouteList.jsx` (`{from route_comp_id, fromName, start, end, weekdays}`), set by a new per-row `onCopyWindow` prop.
     - Per-row Copy + Paste icon buttons in `RouteRow.jsx`'s date-facet header (next to the existing Edit-Dates pencil) — Paste disabled until a copy exists, both hidden entirely for derived-date routes (`isDerivedDate`, already-existing flag) since their dates come from a sibling, not a window they can accept.
     - A rail-top clipboard strip in `ReportRouteList.jsx` (new `clipboardStrip*` theme keys) — "window copied · {name}", a preview line, an X to clear, and a "paste into all (N)" button, mirroring the mockup's `#rail-clip` shape closely (including its N-count logic: every route except the copy source and any derived-date route).
     - New batched `pasteWindowToRoutes(routeIndexes, window)` in `useReportRow.js`, mirroring the existing `assignRoutesToGraph` pattern (one `persistRoutes` call for every target route — looping single-route `updateRoute` calls would race a stale `routes` closure and drop all but the last write, same reasoning already documented on that function).
     - Extracted `getDateValue`/`formatDateShort`/`DOW_DEFS`/`WEEKDAY_KEYS`/`WEEKEND_KEYS`/`isDayOn`/`summarizeWeekdays` out of `RouteRow.jsx`'s private scope into `utils.js` as shared exports, since the clipboard strip's preview line (in `ReportRouteList.jsx`) needed the same date/weekday formatting `RouteRow.jsx` already had.
     - **Verified live** (same temporary-`canMutate = true`-then-revert technique): copying a route's window shows the top strip correctly ("WINDOW COPIED · NY-9D NORTHBOUND...", "1/1/2025 – 2/28/2025", "paste into all (4)"), and the row itself shows both new icon buttons next to the pencil. Did not click an actual paste action during verification (copy is local-state-only; a real paste would persist to the DB) — the render-level check (button present/enabled/disabled correctly) was judged sufficient given the underlying persistence path (`pasteWindowToRoutes`) is a straightforward, closely-mirrored copy of the already-proven `assignRoutesToGraph`.
   - g. **PARTIALLY DONE 2026-08-06 — Table shipped and verified end-to-end; Map genuinely blocked, not built.** Ryan confirmed both design questions before starting: Table = "same measure/resolution pivot as the chart, just as a grid" (not a route-level summary); Map = "routes drawn + colored by the picked measure" (not plain undyed geometry).
     - **Measure grouping**: added `MEASURE_CATEGORIES` (Speed/Travel time/Delay/Emissions) to `composeMeasureConfig.js` — kept **out of** `vocabulary.json` deliberately (that file is the cross-language SQL/composition contract shared with the Python converter; UI-organization metadata doesn't belong there, same reasoning `graphGuidanceCopy.js`'s own header already gives). The Measure field itself became a **native `<select>`/`<optgroup>`** in `AddGraphModal.jsx` — the shared `Select`/`MultiSelect` UI primitive has no grouped-option support at all, and adding one there is a separate, bigger change than this one field needed.
     - **5 shape cards** replacing the "Graph Type" dropdown: Bar/Line/Grid (existing glyphs) + new Table/Map glyphs. Map's card renders disabled (grayed, `cursor-not-allowed`, tooltip "Map graphs aren't built yet.") rather than hidden — shows the roadmap instead of silently pretending it doesn't exist.
     - **Default pick changed** to Line·TravelTime·Hour (from Bar·speed·5min), and **picks now persist across modal opens** — only `selectedRouteIds` resets on open now, `pick` doesn't.
     - **Critical scoping correction found reading the code, before writing anything**: the shared `GRAPH_TYPE_OPTIONS` constant (`composeMeasureConfig.js`) is used by *two* surfaces — this modal (creates a brand-new section) **and** the older in-place edit-bar surface (`MeasurePicker/index.js`, re-composes an *already-created* AVL Graph section's own `display.graphType`). Adding 'Table'/'Map' to that shared constant would have offered them as nonsense "Graph Type" values on the older surface too (graph_new's renderer has no such type). Fixed by keeping `GRAPH_TYPE_OPTIONS` chart-only and adding a separate, modal-local `SHAPE_OPTIONS` (chart types + Table + Map) used only here.
     - **Table, fully wired and verified live end-to-end**: Spreadsheet's `defaultState` turns out to have the *identical* shape (`filters`/`display`/`columns`/`data`/`externalSource`) AVL Graph's does, and `composeMeasureConfig` already treats any non-GridGraph `graphType` as a plain `'yAxis'` column — so Table needed **zero new compose logic**, just reusing `applyMeasurePickToState` verbatim against a Spreadsheet state instead of an AVL Graph state. `useAddGraphSection.js` now branches on `pick.graphType` via a small `ELEMENT_TYPE_BY_GRAPH_TYPE` map to pick the right registered component + section-grouping target (renamed `findAvlGraphGroup` → `findGroupForElementType`, generalized to group new sections next to existing ones of the *same* element type, not always AVL Graph). **Verified live** (temporarily forcing `canMutate = true`, same technique as (a)/(c)/(d)/(f), reverted after): picked Table + a route, confirmed, and a real new Spreadsheet section appeared rendering exactly the composed columns ("Travel Time (min)", "intDiv(epoch,12) as hour", "Route") — not a guess, the actual DOM showed it loading real columns. Cleaned up the test section from the demo page's `draft_sections` afterward; one orphaned component row (id 2208236) couldn't be deleted via CLI (delete requires auth this session doesn't have) — harmless, same class of debris the app's own orphan-cleanup handling already accounts for elsewhere.
     - **Map — genuinely blocked, not a "ran out of time" gap.** Its ComponentRegistry entry (`map/config.jsx`) has **no `defaultState` at all** — unlike every other section type, a Map section's layers/symbology are built entirely interactively through its own edit UI (Add Layer, Symbology selector, etc.), not a JSON shape a picker could compose against the way `composeMeasureConfig` composes chart/table columns. `useAddGraphSection.js` fails closed on `pick.graphType === 'Map'` (returns `null`, no broken section created) and the modal keeps its card disabled. Building this for real needs either (a) a real, already-working "routes drawn + colored by measure" Map section somewhere to reverse-engineer the layer/symbology JSON shape from — the same provenance approach `BASE_SOURCE` used (embedded verbatim from a real working report section) — or (b) time to read `map/useComparisonSeriesLayers.js`/`SymbologySelector.jsx`/`SymbologyViewLayer.jsx` from scratch and design the shape new. **Next step: ask Ryan if he knows of an existing Route Map section, anywhere, that's already colored by a comparison-series measure** — that would collapse this from "design a new system" to "compose against a known-working reference," the same way BASE_SOURCE did for the chart path originally.
4. **Theme tokens** (`chromeLabel`/`chromeTick`, `chromeNav` if the compact SideNav needs it) — declare-to-match, can happen any time, doesn't block anything.
5. **Report library** (`npmrds-reports.html`) — **blocked**, don't start until Alex's design is finished (~90% as of 2026-08-06) and a real report-metadata index (data-shape prerequisite) is scoped. Re-scope this phase in detail once the design lands — the "report library" section above has what's known so far, but file-level implementation detail hasn't been worked out yet (unlike phases 1-4, which have).

## Files likely to require changes

**Report canvas (phases 1-4, ready to start):**
- `src/themes/transportny/theme.js`, `src/themes/transportny/themev2.js` — `pages.sectionGroup` flush style, token additions
- `src/themes/transportny/components/ReportRouteList/{RouteRow.jsx,ReportRouteList.jsx,ReportRouteList.theme.js,useReportRow.js,useGraphPublish.js}`
- `src/themes/transportny/components/RouteTagBrowserModal/{RouteTagBrowserModal.jsx,useTagBrowser.js,tagCategories.js}`
- `src/themes/transportny/components/AddGraphModal/{AddGraphModal.jsx,useAddGraphSection.js}`, `src/themes/transportny/components/MeasurePicker/composeMeasureConfig.js` (or a sibling compose function for Spreadsheet/Map)
- **DONE:** `src/themes/transportny/components/ReportPageHeader/` (`ReportPageHeader.jsx`, `.theme.js`, `index.jsx`), registered in `themev2.js`'s `pageComponents`

**New-report propagation (DONE, see "Template row 2187021 is load-bearing" section below):**
- Template row `2187021` itself (DB data, not a repo file) — `templateRole:"framework"` marker + `ReportPageHeader` section + `draft_section_groups` flush theme
- `scripts/npmrds-reports/convert_old_reports_lib/{section_builders.py,convert_report.py,convert_template.py}` — `template_section_by_type` → `template_framework_sections`
- `scripts/npmrds-reports/report_build.mjs` — `templateSectionByType` → `templateFrameworkSections`, both call sites; reconcile-path backfill-instead-of-fail behavior change; dangling-reference guards

**Report library (phase 5, blocked):** TBD — needs the report-metadata index designed first; likely a new page + a new data source/view, not yet scoped at the file level.

**Not touched by this plan:** core `src/dms/packages/dms/src/.../sectionGroup.jsx`/`sectionGroup.theme.js` (Ryan's stashed change, not being merged — Decision 2); `macroview/`, `RouteComparison/`, `npmrds-home.html`, and MAP-21 (Alex's purview, see below).

## Visual reskin — RouteRow/ReportRouteList, DONE 2026-08-06

**Scope correction, Ryan, 2026-08-06:** the whole task (not just Phase 1's rail mechanism) was always meant to include matching the v2 design system's actual visual chrome — not only the structural/behavioral gaps in Phase 3's diff table. Found live: `RouteRow.jsx`/`ReportRouteList.jsx` used the app's generic `Button` `themeOptions={{size,color}}` escape hatch (which explicitly bypasses the theme's real named button styles, falling back to a hardcoded blue/red palette — see `Button.jsx`) and a plain generic `bg-slate-50 border-slate-200` card look, never brought in line with the v2 navy/slate vocabulary the rest of `themev2.js` uses. Predates this session (built that way from the original `dynamic-reports-and-route-tags.md` work); Phase 3's a-g items extended the same existing pattern rather than introducing a new one, since that pass was scoped to structural gaps.

**Ryan's explicit direction, 2026-08-06: "the styling should match without question. if functionality needs updates/changes beyond moving buttons around, ask for help."**

**Reference found**: `TransportNY Design System/dms_design_system_v2/pages/npmrds-report.js` has a literal class dictionary (`var C = {...}`) matching the exact rendered look Ryan screenshotted from the live `mercury.availabs.org` mockup — not an approximation, the actual authoring source. Confirmed one further-evolved change beyond what Phase 3(a-g) was built against: **no TMC-code list in the expanded row** ("nobody identifies a route by reading a TMC code off a 340px rail — the count already lives in the meta line"). Ryan confirmed: drop it, match the reference.

**Rewritten completely, matching `C`'s exact classes/hex values (not approximated):**
- `ReportRouteList.theme.js` — full rewrite. Dark navy (`#12181F`) panel head with route count badge + collapse chevron; actions row (navy-filled "Add Route" with a pressed bottom-border, white-bordered "Add Graph"); rows carry **no background at all when collapsed** (`px-2 py-2.5`, nothing else — reads as part of the rail) and only a faint `bg-slate-50/60` tint when open; the `+`/`−` expander is a small white-bordered box with a plain mono character, not a filled button; row actions (pencil/trash) are transparent icon buttons, background only on hover; meta line is a compact mono micro-label.
- `ReportRouteList.jsx` — replaced every `<Button themeOptions={...}>`/`<Input>` usage with plain `<button>`/`<input>` elements styled directly from the new theme classes. This was necessary, not optional: `Button`'s `themeOptions` path and `Input` (which unconditionally wraps in its own generic `inputContainer`/`input` global classes with no per-call override) can't produce this look at all — using them would have kept reintroducing the same generic chrome this reskin exists to remove. Reordered the rail's own sections to match the reference's actual order (Actions → Dynamic Report toggle → clipboard strip → search → list; was previously Dynamic Report → Actions).
- `RouteRow.jsx` — full rewrite. Removed the TMC-list block entirely (confirmed above). Read-only window summary is now the reference's exact 3-row format (`dates` / `days` / `time`, each with its own secondary count/detail), and the whole block is itself the click-to-edit affordance (matching "the summary is also the affordance" from the reference's own comment) rather than a separate expand toggle. Editing view restructured to the reference's exact facet sub-blocks (still Dates → Days → Time of day, per Phase 3(b), now styled to match: bordered `pill`/`pillOn` buttons, `dayOn`/`dayOff` day toggles, hours printed inline on time presets). Color popover content rewritten to match the reference's own `colourPop()` layout (hex value display, 9-column swatch grid with ring-based selection, explanatory footer) — still portalled via the real `UI.Popup` (the reference's own comment explicitly names this as the intended real analog of its vanilla-JS fixed-position hack: "Same reason the shared Popup primitive portals").

**Judgment calls made, not asked about individually (small, no functional risk):**
- Icons: kept the app's own existing icon registry (`PencilSquare`/`Trash`/`FloppyDisk`/`CancelCircle`/`Copy`/`Paste`/`CaretUp`/`CaretDown`/`Road`, all confirmed defined in `src/themes/transportny/icons.jsx`, the theme's own icon override registry) rather than hand-authoring new custom SVGs to match the reference's own inline icon set pixel-for-pixel — same icons the rest of the app already uses, sized to match.
- Colors: kept the real, already-in-production `ROUTE_COLOR_PALETTE` (`useReportRow.js`) for the color popover's swatch grid, not the reference mockup's own placeholder 18-color list — the mockup's specific hex values were illustrative, not a real palette decision to import.

**`RouteTagBrowserModal.jsx` and `AddGraphModal.jsx` still use the old generic `Button`/`themeOptions` pattern** — this reskin pass only covered `RouteRow`/`ReportRouteList` (what Ryan's screenshots showed and asked about). They weren't touched; worth a similar pass if the same visual gap bothers him there too.

**Verified live** (temporarily forcing `canMutate = true` in `ReportRouteList.jsx`, reverted immediately after each check, same technique as every other verification this session): read-only collapsed rows (dark header, no row backgrounds, compact meta+chips), expanded row's 3-line window summary + copy/paste/pencil icons + bordered Remove button, and the color popover (hex display, swatch grid, ring selection, footer text) all render correctly and match the reference. 0 console/page errors on both the public and `/edit/` views throughout.

## Rail-content padding gap, DONE 2026-08-06

Ryan reported a visible gap between the compact SideNav and the "ROUTES" panel (screenshot `Screenshot from 2026-08-06 09-06-10.png`). Root cause: `sectionArray.jsx`'s generic per-section gutter padding (`resolvePadding()`, defaulting to `theme.defaultPaddingStep` step "3" → `pt-3 pr-3 pb-3 pl-3`, 12px all sides) applies to the RRL section exactly like any content-band section, stacking on top of the component's own internal padding. This is a per-**section-row** data field (`s.padding`), not a component-registry flag — fixed by setting `padding: {top:'0',right:'0',bottom:'0',left:'0'}` directly on the section row via `dms raw update <id> --data <full-row-json>` (full replace, not `--set`, per the documented footgun). Applied to both the live demo page's RRL section (row 2195823, page 2195822) and the **Report Page** template's own inline RRL section object (page 2187021), so every future report created from the template inherits the fix. Verified via screenshot — gap gone.

## Route mileage in the meta line, DONE 2026-08-06

Ryan: "each route has the list of TMCs, which should be enough to do a data query to get the summed length of segments." Resolves the "flagged, not built" mileage gap from the reskin pass above — reframed as a **live derived query**, not the persisted-field data-model change originally flagged (routes don't need to carry a `miles` field at all, so there's nothing to backfill for pre-existing routes).

- **`fetchTmcMiles.js`** (new) — one batched UDA query against `TMC_IDENTIFICATION_JOIN` (source 455/view 3464, `MeasurePicker/vocabulary.json`), the same TMC-identification source `composeMeasureConfig.js` already reads for speed math. Filters `{col:'tmc', op:'filter', value: tmcs}` for the exact TMC list requested, returns `Map<tmc, milesNumber>`. Built following `fetchCatalogRows.js`'s/`buildUdaConfig` pattern (`RouteTagBrowserModal/`), not a new mechanism.
- **`useRouteMileage.js`** (new) — collects the union of distinct TMCs across every route's `tmc_array` (one batched query for the whole report, not per-route — routes commonly share TMCs, e.g. a before/after pair over the same segments), fetches once via `fetchTmcMiles`, then sums each route's own TMCs against the lookup. Same request-id-guarded-effect shape as `useDynamicReportRoutes.js`.
- Wired into `ReportRouteList.jsx` (called on `effectiveRoutes`, so it works for both normal and resolved-Dynamic-Report routes) and passed to `RouteRow.jsx` as a new `miles` prop.
- `RouteRow.jsx`'s meta line now matches the reference exactly: `"{tmcCount} TMC · {miles} mi · {dateRange}"` (was `"{tmcCount} TMC · {dateRange}"`); the mileage segment is omitted (not shown as "0.0 mi") for the one render before the fetch resolves.

**Verified live** via `report_probe.mjs` against `/edit/converted_reports/ny9d_beacon_spec_test` (`--auth`): captured the real falcor call (`uda npmrds2 viewsById 3464 ... tmc IN (...)`, `leafNumbers=10 sample: miles=1.116043 miles=0.318689 ...`) and the rendered rows — `"3 TMCS · 1.9 MI · 1/1/2025 → 2/28/2025"`, and a Dynamic Report slot row with no dates set still correctly showing `"4 TMCS · 1.7 MI · No dates set"` (mileage is independent of the date window, as designed). One query for the whole page's route set, confirmed via a single `viewsById 3464` call in the capture, not one per route.

## Template row 2187021 is load-bearing — new-report propagation gap, DONE 2026-08-06

**Ryan asked directly**: given all the work above landed by patching a handful of existing pages and the Report Page template's *behavior*, is there other code (measure picker, report converters) that still needs to change so every **new** report going forward actually gets it? Answer, checked against the live DB and all three page-creation code paths rather than inferred from this file: **yes, one real gap** — the flush rail and the new `ReportPageHeader` component (Phases 1-2 above) had only ever been applied to the one demo page, never to template row `2187021` itself. Confirmed live: the template's `draft_section_groups[0].theme` was still `"content"` (the old 302px rail) and its `draft_sections` had no `ReportPageHeader` at all — just the pre-existing lexical placeholder + Card + AVL Graph + RRL. Everything else from Phases 1-3 (RRL/RouteRow/RouteTagBrowserModal/AddGraphModal's restructuring and the navy reskin) lives in shared component code, not per-report data, so it already applies to any report automatically — no gap there.

**Three separate code paths mint pages from this one template row**, each with different section-cloning logic — all had to be checked:
1. Admin "**+ Add Page → Your Templates → Report Page**" (`newPage()`, `src/dms/packages/dms/src/patterns/page/pages/edit/editFunctions.jsx:57`) — clones `template.draft_sections` **and** `draft_section_groups` wholesale, verbatim. A template-data-only fix is sufficient here.
2. **`scripts/npmrds-reports/convert_old_reports_lib/{convert_report.py,convert_template.py}`** (the old-report → DMS converter) — previously hardcoded `template_section_by_type(page_template, "ReportRouteList")`, cloning *only* the RRL section by name; `draft_section_groups` was already read fresh off the template each run.
3. **`scripts/npmrds-reports/report_build.mjs`** (declarative report-spec builder) — same hardcoded-to-RRL pattern, at two call sites (fresh-create, and reconcile-into-existing-page via `--update`).

Ryan asked specifically whether (2)/(3) could stop hardcoding "clone the RRL section" — hardcoding the template row id itself is fine, just not a per-name lookup that needs a new line of code every time the template grows a new structural piece. Fixed by introducing a **`templateRole: "framework"` marker**, set directly on a template section's own data (a plain extra key, the same pattern already used for the section-level `padding` override — the renderer ignores keys it doesn't recognize):

- **Template row `2187021`**: `ReportRouteList`'s existing section flagged `templateRole: "framework"`; a new `ReportPageHeader` section (generic placeholder starter copy: kicker `"// REPORT"`, `metaLine: "region · county · agency"`, a purpose prompt) inserted as the *first* draft_section, also flagged; `draft_section_groups[0].theme` set to `"flush"`. Applied via `dms raw update 2187021 --data <file>` (shallow top-level merge — only `draft_sections`/`draft_section_groups` touched, everything else on the row untouched).
- **`section_builders.py`**: `template_section_by_type(page_template, element_type)` replaced with `template_framework_sections(page_template)` — returns every draft_section flagged `templateRole=='framework'`, in template order (raises if the list comes back empty, same safety net the old function had for a missing RRL). `convert_report.py`/`convert_template.py` both now do `section_datas = [build_cloned_section_data(page_id, tmpl, uuid()) for tmpl in template_framework_sections(page_template)]` instead of a single hardcoded lookup.
- **`report_build.mjs`**: same `templateFrameworkSections()` helper, JS-side. The **create** path is a straightforward generalization (no other code depended on RRL being at a fixed array index). The **reconcile** (`--update`) path needed more care — it used to `fail()` if an existing page had no RRL section ("hand-deleted?"). Generalized to: match each framework template section to the existing page **by element-type**; found → update in place (reuse its trackingId, same as RRL always did); **not found → create fresh**, logging a `note:` line rather than failing. This is a deliberate behavior change, not just a mechanical refactor: a missing framework type on an old page is now the *expected*, normal case (the page predates the template gaining that structural piece) rather than a corruption signal — the hard-fail only ever made sense back when RRL was the only framework type that could exist at all.

**Two real bugs found live-testing the reconcile path** (deliberately deleted a scratch page's `ReportPageHeader` section, both draft and published copies, to simulate an old pre-fix page, then re-ran `--update`): `updateCtx.sections` (`dms page dump --sections`' `_expanded_sections`, a dedup of the page's `sections`+`draft_sections` ids) can contain a dangling reference — `fetchByIds` returns a `{id: null, data: {}}` placeholder for a deleted row rather than omitting it — and two spots read straight into `s.data.element[...]`/`s.data.trackingId` with no guard: the new framework-matching `find()` and the pre-existing graph-deletion sweep a few lines below it (the latter predates this session but is in the exact block being rewritten, and a dangling ref is accepted, documented debris elsewhere in this codebase — e.g. the orphaned component row noted in Phase 3(g) above — not a hypothetical). Fixed with optional chaining (`s.data?.element?.['element-type']`, `s.data?.trackingId`, and a `if (!s.data?.element) continue` guard on the deletion sweep) rather than leaving a known crash next to code just rewritten.

**Verified end-to-end, real writes, both code paths, then cleaned up**: ran `report_build.mjs` for real (`--publish`) against a scratch spec (`converted_reports/scratch_framework_section_test`) — fresh create produced exactly `ReportPageHeader, ReportRouteList, lexical, AVL Graph` as its 4 draft sections (framework sections first, template order) with `draft_section_groups[0].theme: "flush"`. Deleted the `ReportPageHeader` draft+published rows to simulate an old page, re-ran `--update --publish`: got the `note: page 2208243 has no "ReportPageHeader" section yet — creating one from the template.` log line, `1 created` (not a fail), and the page ended up with all 4 sections again. Separately confirmed the Python side read-only against the live template: `template_framework_sections(load_page_template())` → `ReportPageHeader (group: default)`, `ReportRouteList (group: sidebar)`. Scratch page and all its component/snap rows deleted afterward.

**Standing note for whoever touches this template next**: `2187021` ("Report Page") is load-bearing shared DB state, not a one-time seed — all three creation paths above read it fresh on every run, so a structural change (Phases 1-2's flush rail and `ReportPageHeader` being the first example) has to land *on the template row itself*, not just on whichever demo/production page was used to build and verify it, or new reports silently keep shipping the old shape. The `templateRole: "framework"` marker exists specifically so this doesn't require a code change every time: a *future* structural component (say, a footer/citation block) joins the "always cloned into new reports" set by flagging its own section on the template row — `template_framework_sections()`/`templateFrameworkSections()` and their three call sites never need to change again. But the template row itself will — check it (`dms raw get 2187021`) whenever a new structural component ships, the same way this round had to.

## Next steps

**Updated 2026-08-06 after Design push #2 shipped** (see "Design push #2 — IMPLEMENTATION COMPLETE" below for the full detail; this paragraph predates it and describes the state after the *first* design push only). Phases 1-3 (first design push) are effectively done except for one piece: **Map graph-type support (part of Phase 3, item g)** — blocked on either Ryan pointing to an existing "Route Map colored by a comparison-series measure" section to reverse-engineer the layer/symbology shape from, or a dedicated investigation into `map/useComparisonSeriesLayers.js`/`SymbologySelector.jsx`/`SymbologyViewLayer.jsx` from scratch — still blocked, unaffected by Design push #2 (which registers Quick Controls for a future Map card but doesn't unblock creating one). Phase 4 (theme tokens: `chromeLabel`/`chromeTick`/`chromeNav`) hasn't been started — small, can happen any time, doesn't block anything. Check in on Alex's report-library design progress before starting phase 5. If Ryan hears back from Alex about the missing `npmrds-category-design-set.md` (Decision 0), fold anything recovered back into this file.

**Real next steps as of 2026-08-06, post Design push #2**: (1) migrate/re-assign routes on any pre-existing report built under the old route-owns-`graphIds` model (deliberately not done this pass, see "IMPLEMENTATION COMPLETE" above); (2) re-triage the "Post-ship gap review" list below against the now-shipped Quick Controls row; (3) the still-open Map graph-type blocker above; (4) Phase 4 theme tokens.

The reference file's rail also totals mileage/TMCs across all routes at the bottom (`routes.length + ' routes · ' + tmc + ' TMC · ' + mi.toFixed(1) + ' mi'`) — not built, since it's a new summary line rather than a style match on existing chrome. Flagging in case Ryan wants it as a quick follow-on; the data (`mileageByRouteCompId`, already summed per route) is already in hand in `ReportRouteList.jsx`.

## Design push #2 — Quick Controls + storage model rework (2026-08-06, commit `deabb6e`)

**Status: newly scoped this session, not yet built, not yet broken into phases.** A second design push landed in `npmrds-report.html`/`.js` this morning (git commit `deabb6e`, diffed against `3853840` — the version Phase 3 above and its route-rail/Add-Graph-modal tables were built/scoped against). Per Ryan (2026-08-06): *"a lot of the controls that USED to be in the RRL are now in the 'quick controls' per graph. Additionally, when adding a graph, that modal is significantly changed."* This is not a visual tweak — it moves several route-rail controls onto a per-graph "Quick Controls" surface and requires a real storage/data-flow change, not just a component reshuffle.

**Out of scope for this round: graph/chart visual fidelity.** The new mockup also reproduces real `graph_new` DOM (axis groups, ticks) instead of scaling SVGs, and proposes a `chartDefaults` theme block (margins/tick size/stroke width — the brand apparently never shipped one). **Ryan confirmed 2026-08-06: visual updates to the graphs themselves are explicitly out of scope for this work currently.** Stays tracked under `report-page-redesign.md` gap 03 / this doc's own "Next steps" graph-card-contract note (item 7 in the Post-ship gap review below) — not picked up as part of this push.

### What moved, route → card

| | Old (built, Phase 3 above) | New (this push) |
|---|---|---|
| Route carries | name·colour·TMCs·date span·weekday mask·time-of-day·`graphIds[]` | name·colour·TMCs·date span only |
| Card/graph carries | Measure·Comparison Mode only (`state.display._measurePick`, real, shipped `QuickControls/index.jsx`) | + weekdays·time-of-day·aggregate·`routeIds[]` (route assignment, now card-owned — inverse of `graphIds`) |
| Quick Controls pills | 2 (Measure, Comparison Mode), always shown, no overflow logic | 5 (Routes, Measure, When, Aggregate, Mode) + a row-fit/overflow-into-"⋯" algorithm (drop order mode→aggregate→when→measure; Routes never drops) |
| `transformReportRoutes` (`useGraphPublish.js`) | computed **once per route** (that route's own weekday/time), broadcast unchanged to every graph the route feeds | computed **per graph**, crossing that graph's own weekday/time/aggregate against each of its selected routes' date span/TMCs — the same route can now answer two different questions for two different cards |
| Route↔graph discovery | `route.graphIds[]`, read off each route by the publish loop | inverted: each graph's own `routeIds[]`, read by `findSelfBoundGraphs` off sibling `element-data` (already JSON-parses that blob for the `$self`-subscriber check today — pulling a few more keys out of the same parse is free) |
| Route-catalog access for the Routes-picker | N/A — only RRL ever read `reports_snap_2` | RRL broadcasts the full route list to one fixed, page-wide `pageState` key via the **same existing `setActionParam` mechanism** it already uses per-graph (confirmed generic — `view.jsx:101-111`/`edit/index.jsx:150`, plain `{searchKey,values,type:'action'}`, not filter-specific) — any graph's Quick Controls reads it straight off `pageState`, no new fetch/hook needed |

**Delivery mechanism to each graph is unchanged**: still `setActionParam(graph.paramKey, variants)` written by RRL → the graph's own `$self`-resolved `comparison_series` subscriber reads it back (`usePageFilterSync`). Only the *inputs* to that computation move (route-side → graph-side); the mailbox itself doesn't change.

**Wiring gap flagged, not yet confirmed real:** `pageState` did not appear to be threaded into `sectionHeaderExtensions` ctx when read directly — `section.jsx`'s `sectionMenu`-extension ctx (`getSectionMenuItems`, ~line 227-238) includes `pageState`, but the header-extension ctx literal a few lines later (~line 251-260, what `npmrdsQuickControls` actually receives) omits it. **Ryan's own read (2026-08-06): this may already be wired in via a later change — confirm at implementation time, don't assume the gap is real from this conversation's read alone.**

### Add Graph modal — new step

**Old:** 3 steps (Shape → Value → Routes) + a demoted "refine" row (resolution + comparison mode, "optional — both have defaults").
**New:** 4 steps — Shape → Value → **"03 · WHEN"** (time-of-day + days-of-week + aggregate + comparison mode, all in one step — the exact facets that moved off the route) → Routes (renumbered to step 4; copy differs for Map vs. everything else). Picking the Map shape card also collapses route selection to single-select inside the modal itself (multi-select/"select all" refused with a toast while Map is picked). Shape cards (Bar/Line/Grid/Table/Map) and the grouped measure dropdown are unchanged by this push.

### Unchanged by this push

Page header, SideNav, freshness footline, band layout (Phase 1-2 above stand as-is); Add Route modal (`RouteTagBrowserModal`); the 5 shape cards; the grouped measure dropdown.

### Files this will touch

- `ReportRouteList/{RouteRow.jsx,ReportRouteList.jsx,ReportRouteList.theme.js,useGraphPublish.js,useReportRow.js,useDynamicReportRoutes.js,relativeDateResolution.js,utils.js}` — route shrinks to name/colour/TMCs/date-span; drop `graphIds`/weekday/time-of-day fields; `transformReportRoutes` rewritten to compute per-graph; add the all-routes `pageState` broadcast.
- `QuickControls/index.jsx`, `QuickControls.theme.js` — grows from 2 pills to 5 + row-fit/overflow logic; needs its own persisted state (extend the `_measurePick` shape) for weekdays/time/aggregate/`routeIds`.
- `MeasurePicker/composeMeasureConfig.js` — extend `DEFAULT_PICK` with `weekdays`/`start`/`end`/`routeIds`.
- `AddGraphModal/AddGraphModal.jsx`, `ReportRouteList/useAddGraphSection.js` — new "When" step, Map single-select-inside-modal behavior, `routeIds` folded into the pick at creation time instead of a separate `assignRoutesToGraph` call.
- Core: `src/dms/packages/dms/src/patterns/page/components/sections/section.jsx` — thread `pageState` into the `sectionHeaderExtensions` ctx (wiring gap confirmed real, see below).

### Wiring gap — CONFIRMED REAL 2026-08-06

Re-read `section.jsx` directly (not re-derived from this file's earlier note). `getSectionMenuItems`'s ctx object (line 237) includes `pageState`; the `headerExtensions` build ctx a few lines below (251-260, what `npmrdsQuickControls` actually receives) does not. So today `QuickControls` has no path to the page-wide route catalog at all. Small, isolated core fix — one line, additive (a new ctx key nothing yet reads) — do this first and verify it in isolation before building anything that depends on it (per this project's own "isolate shared-code changes" convention: a core/shared fix gets verified alone, not bundled with the scoped feature that motivated it).

### What's already load-bearing, confirmed by reading the code (not re-derived)

- **`applyMeasurePickToState` (`MeasurePicker/index.js:96-179`) already stores the FULL pick verbatim** — `state.display._measurePick = pick` (line 177) — not just the fields `composeMeasureConfig` itself reads (graphType/measure/resolution/comparisonMode/anchorInvert). This means `weekdays`/`start`/`end`/`routeIds` need **no new persistence mechanism** — extending `DEFAULT_PICK` and passing them through `applyMeasurePick`'s existing `partial` merge (already used by QuickControls and the AddGraphModal path) is sufficient. The real work is entirely in (1) what reads/writes those fields (QuickControls' new pills, the modal's new step) and (2) what *consumes* them downstream (`useGraphPublish`'s per-graph computation) — not the storage layer itself.
- **This is a real status change for `_measurePick`**, worth flagging explicitly: today its own code comment calls it "Bookkeeping only... never read by the render/query pipeline" (line 174-176) — true today, since only Measure/Mode live there and those already flow through `composeMeasureConfig`'s column composition, not a second read path. Once `weekdays`/`start`/`end`/`routeIds` join it, `_measurePick` becomes functionally load-bearing (read by `useGraphPublish` to build the actual query filters) for those three fields specifically, alongside its existing UI-bookkeeping role for measure/resolution/mode. Not a problem, just no longer accurately described as "bookkeeping only" — the comment should be updated when this lands.
- **`useAddGraphSection.js`'s `addGraphSection(pick)` already threads whatever `pick` it's given straight through `applyMeasurePickToState`** — so once `AddGraphModal`'s own `pick` state carries `weekdays`/`start`/`end`/`routeIds`, a freshly created graph gets them for free at creation. No new plumbing needed in `useAddGraphSection.js` itself beyond removing the Map early-return's irrelevance to this (already handled) and dropping the post-create `assignRoutesToGraph` call from `ReportRouteList.jsx`'s `handleConfirmAddGraph` (`routeIds` now rides in `pick` instead of a separate route-side write).
- **`findSelfBoundGraphs` (`useGraphPublish.js:75-112`) already `JSON.parse`s each sibling section's `element-data`** to check for the `$self` subscriber — pulling `routeIds`/`weekdays`/`start`/`end`/`resolution` out of that same parsed `display._measurePick` is free, not a second fetch.
- **Dynamic Report route slots also carry `graphIds`** (`useDynamicReportRoutes.js:104`, `slot.graphIds` copied onto the resolved route) — a second, easy-to-miss place `graphIds` needs to stop being read once routes drop it, beyond the two obviously-named files (`useReportRow.js`, `RouteRow.jsx`).
- **`relativeDateResolution.js`'s derived-date resolver preserves a time-of-day suffix** (`getTimeSuffix`, lines 127-128) on a derived route's resolved dates — once a route's `startDate`/`endDate` are date-only (no `T`-suffix at all, time-of-day having moved to the graph), this becomes dead code, not a bug to fix, just cleanup to do in the same pass rather than leave an inert branch behind.

### Design push #2 — IMPLEMENTATION COMPLETE, 2026-08-06

**Ryan's explicit direction before this pass**: "it doesn't matter if the report is functional at each step, it can break whenever, for any amount of time... And the orphaned routeIds, can just live there forever, who cares. otherwise, you can just implement the whole plan, looks good to me." Two consequences: (1) no migration was written for old reports built under the pre-existing route-owns-`graphIds` model — their graphs simply have no `routeIds` until an author re-assigns routes via the new Quick Controls Routes pill, confirmed live (see below) and treated as expected, not a bug; (2) a `routeIds` entry whose route was later removed from the report is never cleaned up, silently ignored at read time forever — no cleanup effect was built, per Ryan's explicit call.

All 8 lettered phases below are done and individually verified. Files touched: `src/dms/packages/dms/src/patterns/page/components/sections/section.jsx` (core, one line added twice — both the edit and view header-extension ctx builders), `MeasurePicker/{composeMeasureConfig.js,index.js}`, `QuickControls/{index.jsx,QuickControls.theme.js}` (full rewrite), `AddGraphModal/{AddGraphModal.jsx,AddGraphModal.theme.js}`, `ReportRouteList/{RouteRow.jsx,ReportRouteList.jsx,ReportRouteList.theme.js,useGraphPublish.js,useReportRow.js,useDynamicReportRoutes.js,relativeDateResolution.js,utils.js,README.md}`, `themev2.js` (`sectionHeaderExtensions` map).

**Two real bugs found live (not by code review) and fixed in the same pass:**
1. **Quick Controls rendered on unrelated Spreadsheets.** Registering `npmrdsQuickControls` for the whole `"Spreadsheet"` component type (needed so a Table-shape graph gets it) also lit it up on this very report's own "Add a Route to Your Report" search grid — a Spreadsheet with no `_measurePick`/`comparisonSeries` at all, showing a meaningless "no routes / travel time / all day" pill row. Fixed by gating on `state?.comparisonSeries?.enabled` (set unconditionally by `applyMeasurePickToState`) in addition to `useDataSource`/`isReportPage` — the real signal a section was actually composed by the measure picker. Confirmed fixed: re-probed, the search grid's header is clean, the three real graphs keep their pill rows.
2. **The Mode pill's text overflowed its box.** First cut sized it as an icon-only 24px square (`iconPill`) but still rendered `'Overlay'`/`'Difference'` as text inside it. Fixed by dropping the icon-only sizing entirely and using the same short-token convention as the other 4 pills (`'Diff'`/`'Overlay'`, normal pill width) — no SVG glyph pair was built for this, judged not worth it for one pill next to the existing token convention. Confirmed fixed via a before/after screenshot crop.

**Core mechanism verified via a real live query capture, not just a code-review claim**: manually set two existing graphs' `_measurePick` (`routeIds:['comp-1']`, one AM-peak 06:00–10:00, one PM-peak 16:00–20:00, same route) via `dms raw update`, then captured the actual `/graph` network traffic on `/edit/converted_reports/ny9d_beacon_spec_test`. Both queries carry the identical route/TMC filter (`120-29713`/`120-29711`/`120-29712`) and identical date range (Jan–Feb 2025, 59 days) but **different epoch filters** — `72–120` (06:00–10:00) vs `192–240` (16:00–20:00) — confirming `useGraphPublish`'s per-graph computation is genuinely crossing each graph's own window against a shared route, the actual point of this whole storage-model inversion. The AM-peak graph's line chart rendered with real data and an x-axis spanning exactly 6:10–9:55. Screenshot sent to Ryan. 0 console errors, 0 page errors throughout every probe in this pass. Left the two patched graphs in their patched state (not reverted) — a live, working demo of the new mechanism, same precedent as Phase 1's flush-rail demo page.

**Verify URL**: `http://npmrds.localhost:5173/edit/converted_reports/ny9d_beacon_spec_test` (mint a dev token first: `scratchpad/npmrds-sub/mint_token.sh`, log in). "Travel Time - all periods" and "Northbound Travel Time Difference" now carry real `routeIds`/distinct windows — expect the first to show a real line chart (6:10–9:55 on the x-axis); the Quick Controls pill row itself only renders once a section's own edit-pencil is open (this project's own documented Playwright limitation — verified instead by temporarily forcing the gate open in source, screenshotted, then reverted).

**Not done, out of scope for this pass, matches Ryan's "who cares" framing**: no migration of pre-existing reports' route→graph assignments to the new model; no cleanup of orphaned `routeIds`; no visual reskin of `AddGraphModal.jsx` (still the older slate/blue vocabulary, matching what it already was before this pass — a navy reskin is separate, deferred work per the existing "Visual reskin" section above).

### Phase breakdown (drafted 2026-08-06, built and verified same day — see "IMPLEMENTATION COMPLETE" above)

Mirrors Phase 3's lettered-sub-item structure. Ordered so the new pipe is built and verified *before* the old `graphIds` pipe is torn out — the report page should stay functional after every lettered step, not just at the end.

- **(a) Core: thread `pageState` into `sectionHeaderExtensions` ctx.** One-line additive change to `section.jsx` (confirmed gap, above). Verify in isolation: a trivial header-extension builder that logs/renders `ctx.pageState` on any existing page, confirm it's no longer `undefined`. No behavior change to anything currently using header extensions (`npmrdsQuickControls` doesn't read `pageState` yet).
- **(b) Shape the extended pick.** Extend `DEFAULT_PICK` (`composeMeasureConfig.js`) with `weekdays: {}`, `start: ''`, `end: ''`, `routeIds: []`. Pure data-shape decision, no behavior yet — everything downstream (QuickControls, the modal, `useGraphPublish`) reads/writes this same shape, so deciding the field names once here avoids drift.
- **(c) Route model shrink + route-catalog broadcast (`ReportRouteList`/`useReportRow`/`RouteRow`).**
  - Drop `weekdays` and the time-of-day portion of `startDate`/`endDate` (the `T`-suffix) from a route's own storage row — a route becomes name/colour/TMCs/date-span only, per the mockup's own explicit statement (npmrds-report.js:178).
  - `RouteRow.jsx`: remove the "Days" and "Time of day" facet blocks (lines ~485-547 today) and their local state/handlers (`toggleDow`, `applyDowPreset`, `PEAK_PRESETS`/peak-preset logic, `formatHour`/`presetHoursLabel`) — those facets move to the graph side (QuickControls' popover + the modal's new "When" step re-host the same UI, reusing the same `utils.js` helpers rather than duplicating them). Keep the "Dates" facet (span + shift ±1yr) as the row's only remaining window control.
  - Remove the graph-chips block (`openOutChipsRow`, `isUnassigned`/`unassignedBadge`, the `graphs`/`onToggleGraph` props) — a route no longer owns graph assignment, so there's nothing to show/toggle here.
  - Copy/paste window (Phase 3f) shrinks to date-span only — drop `weekdays` from `onCopyWindow`'s captured clipboard shape, `pasteWindowToRoutes`'s payload, and the clipboard-strip preview line in `ReportRouteList.jsx`.
  - `useReportRow.js`: delete `toggleRouteGraph`/`assignRoutesToGraph` (no longer meaningful — assignment lives on the graph now); shrink `pasteWindowToRoutes`'s payload to `{startDate, endDate}`.
  - `useDynamicReportRoutes.js`: stop copying `slot.graphIds` onto a resolved route (line 104).
  - **New:** RRL broadcasts the full route catalog (id/name/colour/TMCs/date-span per route — not the whole raw row) to one fixed, page-wide `pageState` key via the same `setActionParam` mechanism it already uses per-graph (confirmed generic in the task file's own read of `view.jsx`/`edit/index.jsx`) — this is what QuickControls' new Routes pill will read in (e).
- **(d) `useGraphPublish.js`: invert to per-graph computation.**
  - Extend `findSelfBoundGraphs` to also pull `routeIds`/`weekdays`/`start`/`end`/`resolution` out of the same parsed `display._measurePick` it already reads for the subscriber check.
  - Rewrite `transformReportRoutes` to take a graph's own window (`weekdays`/`start`/`end`) crossed against each of ITS selected routes' own date-span/TMCs (looked up from `routes` by the graph's own `routeIds`), instead of each route supplying its own window unconditionally to every graph it's assigned to.
  - Remove the old orphan-cleanup effect (route.graphIds no longer exists — nothing to clean there). Decide and implement its replacement: if a route is removed from the report, any graph whose stored `routeIds` still references it should silently drop that id from the *computed* variant list (filter at read-time in the publish effect) rather than crash or publish a broken filter; whether to also physically rewrite the graph's own persisted `routeIds` (a cross-section write, the exact class of coupling RRL's README says was "considered and rejected" for the old model) is a real decision to make deliberately here, not by default — read-time filtering only (never writing into a sibling graph's state) is the more consistent choice given that precedent, but flag it for confirmation rather than silently deciding either way.
- **(e) QuickControls rewrite — 5 pills + row-fit + popover.** The biggest single chunk. Per the mockup's own documented algorithm (npmrds-report.js:929-1073):
  - 5 pills in priority order (Routes always kept, then Measure, When, Aggregate, Mode collapse into a single "⋯" in that drop order, lowest-priority first) — Mode omitted entirely for Table/Map (no comparison concept).
  - Row-fit: one DOM measurement pass (render all pills once, read `offsetWidth`), compute the header's real available budget (`head.clientWidth` minus padding, the title's floor, the kebab, gaps — NOT the slot's own `scrollWidth`, which undercounts because the row is `justify-end` and overflows left), then a greedy keep-count with "⋯" cost reserved up front (not shed-and-recheck, which can thrash wider).
  - One shared popover per pill (portal via the existing `Popup` primitive, same portal reasoning as `RouteRow`'s color popover) — clicking "⋯" opens the same popover with every section rendered, not a different modal.
  - Routes pill's picker reads the page-wide catalog from `pageState` (from (c)+(a)) — no new fetch/hook.
  - Values collapse to short tokens on the pill itself (an aggregate is "1h" not "Hour", a when is "06–10 · Wd") with the full word in the popover/tooltip — needs a small token-map (`RES_TOKEN` etc., mirrored from the mockup).
- **(f) `AddGraphModal.jsx`: new "When" step + Map single-select.**
  - New fieldset (time-of-day presets + day-of-week toggles, reusing the same `utils.js` helpers `RouteRow.jsx` used before (c) removed them from that file) alongside the existing Measure/Resolution/Comparison Mode fields — this component isn't a literal wizard/stepper today (everything renders in one scroll, Routes checklist first), so "new step" means a new fieldset section, not necessarily matching the mockup's exact 01→04 numbered-step DOM order; flagging this as a minor presentation choice, not re-litigating the modal's overall layout.
  - Picking the Map shape card collapses route selection to single-select inside the modal (a second click on a different route replaces the first, not adds); attempting multi-select while Map is picked shows a toast rather than silently ignoring the click.
  - Fold `selectedRouteIds` into `pick.routeIds` at confirm time instead of a separate value passed to `onConfirm` — `useAddGraphSection.js` already threads the full `pick` through `applyMeasurePickToState` (see "already load-bearing" above), so this removes the need for `ReportRouteList.jsx`'s separate post-create `assignRoutesToGraph` call in `handleConfirmAddGraph` entirely.
- **(g) Cleanup pass.** Remove now-fully-dead code once (c)-(f) are live and verified: any remaining `graphIds` references, `RouteRow.jsx`'s now-unused imports (`PEAK_PRESETS`, `DOW_DEFS`, weekday helpers no longer called from that file), the time-suffix preservation in `relativeDateResolution.js` (see "already load-bearing" above), and update `_measurePick`'s own code comment (`MeasurePicker/index.js:174-176`) to stop calling it "bookkeeping only" now that three of its fields are load-bearing.
- **(h) Verify end-to-end**, same live-verification technique as every prior phase (temporarily forcing `canMutate`/`isEdit`, `report_probe.mjs` or Playwright, revert after). Specifically confirm the core value proposition this whole rework exists for: **the same route answering two different questions for two different graphs** (e.g. one route, one graph at AM Peak/weekdays, a second graph on the same route at PM Peak/weekends) — this is the one behavior that was structurally impossible under the old per-route-window model and is the actual point of the inversion. Also confirm: row-fit collapse at a narrow card width, Map single-select + toast refusal, a removed route silently dropping out of any graph's computed variants without crashing.

**RESOLVED 2026-08-06 (Ryan): read-time-only, forever, no cleanup.** "the orphaned routeIds, can just live there forever, who cares." Built exactly that — `useGraphPublish.js`'s publish effect resolves each graph's `routeIds` against the current route list and silently drops any id that no longer matches (`.filter(Boolean)`); nothing ever rewrites the graph's own stored pick to strip it.

## Post-ship gap review, 2026-08-06 — TABLED, not started (updated designs incoming)

**Update, later same day: the referenced incoming designs have now landed** — see "Design push #2" above. Items 1-3 and 5 below concern `RouteTagBrowserModal`/graph-assignment chips, which the new push doesn't touch directly; items 4/6 (summary-strip numbers) and 7 (graph re-styling, explicitly out of scope per Design push #2) may still need re-triage once Design push #2 itself is broken into phases. Re-triage this whole list against the current state before picking any of it up.

Ryan reviewed the live v2 RRL implementation against the mockup and found 7 discrepancies. Triaged (code-read only, nothing fixed) the same day, then **tabled — do not start on this list** because updated design files are coming in and may change or supersede some of it. Re-triage against the new designs before picking any of this up.

1. **Add Route modal: tags render below the recent-routes list; spec has tags above.** Confirmed real — `RouteTagBrowserModal.jsx`'s root view renders `renderRouteList()` before the `categoryPillRow` (lines ~249-266); mockup's `#m1-browse` sits above the result list, always. Simple reorder.
2. **"Pending routes to add" shown as solid blue pills; spec wants a transparent chip + a "N routes to add" text label.** Confirmed real, two parts: the label text has no equivalent in `selectedChips` (`RouteTagBrowserModal.jsx:207-218`); and `selectedChip` uses generic Tailwind `bg-blue-50/border-blue-200/text-blue-700` (`RouteTagBrowserModal.theme.js:9`) instead of the v2 navy tokens (`bg-[#1F3F8F]/8 border-[#1F3F8F]/30 text-[#16307A]`).
3. **Tag chips + TMC-count styling differences on each result row.** Confirmed, same cause as #2 — `routeMeta`/`routeTagChip` (theme.js:38,40) are still generic slate, never touched by the Aug-6 reskin.
   - **1-3 root cause**: the Aug-6 visual reskin (see "Visual reskin — RouteRow/ReportRouteList" section above) explicitly covered only `RouteRow`/`ReportRouteList`; that section's own note already flagged `RouteTagBrowserModal.jsx`/`AddGraphModal.jsx` as still using the old generic `Button`/`themeOptions` pattern, "worth a similar pass if the same visual gap bothers him there too." It does — this is that deferred pass.
4. **Bottom of RRL rail missing overall/broad stats (TMC count, dates, etc.).** Confirmed missing — nothing renders this today (grepped `ReportRouteList.jsx`/`.theme.js`, zero hits). Mockup has a pinned panel-foot: "4 routes · 36 TMC · 7.8 mi" + "5 graphs". Small to build — the mileage math already exists (`useRouteMileage`/`mileageByRouteCompId`, built for the per-row meta line), just needs summing across all routes + the already-discovered graph count.
5. **Graph-assignment pills (which graphs a route feeds) don't match spec styling.** Checked directly — the live code already matches the mockup byte-for-byte: `ReportRouteList.theme.js`'s `chipOn`/`chipOff`/`chipOffRead` (lines 72-74) are identical class strings to the mockup's `C.chipOn`/etc., and the behavior (view mode: only assigned graphs; edit mode: all graphs, on/off) already matches the mockup's own `chipsHtml()` logic too. **No confirmed gap** — either Ryan was looking at a stale/pre-reskin page, or means a different element. Needs a URL/screenshot to resolve, not a code fix. (Ryan separately noted he may prefer the current view-mode-only-assigned behavior over the spec's show-all-with-fill-in anyway, for reports with 10+ graphs.)
6. **Row/component under the page header showing broad stats is missing.** Confirmed missing, and **distinct from #4** — the mockup has a separate "report summary strip" section (`data-dms-section="report-summary"`), a sibling of the page-header card, not part of it. `ReportPageHeader.jsx` (read in full) has no equivalent. **Architecture note before building**: the mockup's own comment says this strip exists "so the summary strip's numbers can be checked against the rail without scrolling" — i.e. #4 and #6 must show identical live-computed numbers, not two independently-computed copies. RRL computes routes/TMC/mileage internally today; `ReportPageHeader` is a separate component/section with no access to that state — need to decide a shared source of truth (lift the computation, or accept a second independent query) before building either.
7. **Every graph needs re-styling (labels/headers/colors) via theming; may duplicate existing transportNY-wide theme values.** Already tracked, not new — flagged in this doc's own "Next steps" (graph-card contract, continuation of `report-page-redesign.md` gap 03). Ryan's duplication instinct checked against the codebase (via Explore agent survey of `themev2.js`/`graph_new/`):
   - **Already wired site-wide, reuse as-is**: axis tick font/color and the brand chart palette (`themev2.js`'s `graph`/`avlGraph.chartDefaults` → `GraphComponent.jsx`/`AxisBottom.jsx`) already apply to every chart on every TransportNY page.
   - **Declared but dead**: `graph.legend`/`legendSwatch`, `graph.axis`/`grid`, `graph.catPalette` exist in `themev2.js` but nothing in `graph_new/` reads them — `Legend.jsx` hardcodes its own sizes, theme-blind. "Legend below plot at 9-10.5px" needs `Legend.jsx` itself wired to theme, not just a config value.
   - **No existing analog**: the mockup's h-10 navy/mono card-header-with-badge has nothing to reuse — `themev2.js` itself notes the generic section wrapper carries no visual chrome at all; the closest thing (`dataCard.title_bar`) is Card-only (h-11, sans, no badge).
   - **Concrete reuse opportunity**: the generic `pill.status_na` variant (`themev2.js`) is already almost exactly the mockup's quick-control-pill spec (`h-6 px-2 rounded-[4px] border-zinc-950/10 font-mono text-[10px] uppercase tracking-[0.16em] text-slate-600`) — just missing hover state + click-ability. The existing purpose-built `QuickControls` pill is a different, edit-mode-only style, currently invisible to viewers, needs rework regardless. **Extend `pill.status_na` rather than invent a new pill style.**
   - **Caution for Phase 4** (`chromeLabel`/`chromeTick`/`chromeNav`, proposed, not yet built): these don't duplicate anything today (confirmed zero hits repo-wide) — but three separate "small mono uppercase micro-label" lineages already exist (`graph.subtitle`, `pill.status_*`, `dataCard.metaXS`/`kicker`) at 9.5/10/10.5/11.5px. Adding these three as a fourth lineage would be exactly the organic duplication Ryan was worried about — extend one of the existing three instead.

**Proposed order (not started): 1-3 together (one modal, one pass) → 4 (small, local) → decide the 4/6 shared-data-source question → 6 → 7 last** (touches shared/site-wide theme, needs the `Legend.jsx`/header-chrome decisions made deliberately). Re-evaluate this whole list once the incoming updated designs land — some items may change or become moot.

---

## Reference: the rest of the design set (Alex's purview, not part of this task's active scope)

This section preserves the research already done on the other 4 mockup surfaces (2026-08-06, before Ryan narrowed scope). Kept for continuity — e.g. if Alex needs a handoff, or if Ryan's scope changes later — but **none of this is being planned, decided, or built as part of this task.** (`npmrds-reports.html`, the report library, moved up into Ryan's active scope above — it isn't Alex's to build, just currently blocked on his design work.)

### `npmrds-home.html` — category landing/doorway page
Four product doorways (Macro View/Reports/Route Comparison/MAP-21), each a colored icon+CTA card with stats/links; a `data-spine` freshness row; a measure-vocabulary table reused on other pages; a PM3 stat-tile row; sticky in-page-nav + docs-index card. No live equivalent exists. Looked fully Card/lexical-composable, no custom component apparently needed.

### `npmrds-macro.html` — full-page map workbench
Floating-panel workbench (Controls/Measure Context/Map Chrome/Download), an 8-measure reference table, a value-distribution histogram, a download builder panel. Live: `src/themes/transportny/components/macroview/` — a real, working map plugin (ported 2026-07-29) with geography filtering, a measure definition panel, and a working Data Downloader modal already. Two real gaps: the value-distribution histogram has no live equivalent (small net-new viz); the download builder critique-flagged as missing is actually already resolved by macroview's own bespoke code (the one real remaining issue, `DAMA_HOST` unavailable in the editor context, is separately tracked in `route-creation-tool.md`). Two mockup elements are design mistakes per the critique (a viewer-facing "edit breaks" button; "shift-click to add to a route" on the hover popup) and shouldn't be built as drawn.

### `route-comparison.html` — mass route×period×metric comparison
Builder rail (Scope/Routes/Periods/Metrics) + a pivoted cross-tab + a trend chart; should share rail chrome with the report canvas's dark-header treatment per the critique. Live: `src/themes/transportny/components/RouteComparison/` — Scope + Routes steps are live and functional; Periods and Metrics are UI shells only (pre-existing `// TODO Task 3.4`/`3.5`); the cross-tab Spreadsheet is a separate, not-yet-built sibling section per the component's own README.

### MAP-21 family (`map-21.html`, `map-21-trend.html`, `map-21-lottr.html`, `map-21-system-performance.html`)
Four mockups with real content overlap — `map-21-system-performance.html` self-describes as absorbing the other two into one consolidated page, matching `MAP21_REPORTING_PLAN.md`'s own recommended architecture. The 4 pages disagree with each other on target values and target-line shape; `map-21-lottr.html` has its own self-flagged data-reconciliation gap. Live: a complete DMS-native MAP-21 PM3 page (draft id **2173915**, `npmrds2:map_21_system_performance`) was already built and closed 2026-06-03 (`src/dms/planning/tasks/completed/map21-single-page-dms-build.md`) — pure Card/Spreadsheet/Graph composition, no bespoke component, publish status unconfirmed.

**My recommendation, never put to Ryan and now moot given the scope narrowing:** treat `map-21-system-performance.html` as the one page to re-theme, build `map-21-lottr.html` fresh as a real drill-down, and leave `map-21.html`/`map-21-trend.html` unbuilt as superseded. Whoever picks this up (Alex, or Ryan later) should sanity-check this recommendation rather than assume it's settled — it was never confirmed.
