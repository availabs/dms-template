# QA run — Freight Atlas — 2026-07-16

Scope: `freightatlas2` surface, Tier B (owner-approved). Target page **2189762** (sandbox copy,
pattern 2175436, served `freightatlas2.localhost` — the page the client/gallery/ticket-URLs see).
Map section **2189767**, edited in place (draft==published on this page; no publish needed).

## Key discovery + owner decisions
- Two `freight_atlas` pages had diverged: build-owned **1411761** (raw) vs the client-visible
  sandbox **2189762** (restyled Jul-10, the page the tickets are actually about). Owner decided:
  **do everything on 2189762**, **close tickets that are really about 1411761**, and **repoint the
  QA automation to 2189762**.
- Several complaints were regressions from the Jul-10 restyle (positional `recolorCategorical`,
  over-transparent boundaries, a wide non-legend casing, mislabeled legends). Captured as the new
  sub-skill `qa_skills/qa-fix-map-symbology-tickets.md`.

## Resolved (13) — symbology/legend fixes on 2189767, verified by screenshot
`#2191410`+`#146` border points → solid navy @ opacity 1 · `#137` MPO fill 0.05→0.22 + solid plum
line · `#145` intermodal orange→teal · `#144` REDC gold→violet dashed · `#150` core-rail Last
Mile→light / National→bold navy · `#154` STRAHNET purple casing removed (map=legend) · `#147`
air-cargo legend renamed from "Major Airports" · `#151` NFHN legend rebuilt to match paint ·
`#140`/`#141`/`#142`/`#143` Dairy Cow: FCHN default-off (phantom roads), stray sub-layers dropped,
bin labels fixed, high-value colour matched.

## Closed (4)
Test junk `#152` `#153`; against-1411761 `#114` (missing-syms assessed on the retired production
section) and `#113` (production workbench "unified map component" design).

## Parked — human queue
- **Needs data:** `#148` (Albany/Schenectady airports missing/miscategorised in source) ·
  `#136` (empty Data&Downloads tabs = uncategorised sources) · `#2191409` (MPO label edits).
- **Needs decision:** `#149` (rail "P"/"D" categories don't exist on any live layer — clarify) ·
  `#135` (stat-strip placement) · `#139` (road functional-class palette) · `#134` (public-comment
  map doesn't exist).

## Elevated (7, assignee HUMAN)
`#120` **blocker** (catalog link + anon `/freight_data` access — core datasets-pattern) ·
`#2191466` (truck-parking figure deep-link — re-verify vs 2189762's 39 syms) · `#138` (road route
shields — tileset investigation) · `#111` (responsive cards-grid — core Card, own BC task) ·
`#2191469` (shareable center/zoom viewport) · `#2190960` (can't deselect active layer — core map
interaction, reproduced) · `#129` (About in-page-nav rail scroll — transportnyv2 theme; anchor
exists, so it's the last-item-can't-scroll behaviour, not page data).

## Automation repoint (owner ask)
- `sitemgmt_patterns` config row **2186151**: `pattern` `freightatlas2`→`2175436`,
  `subdomain` override kept `freightatlas2` (pattern-list resolves by id; `patternRowFields` matches
  by name, so the explicit subdomain override is required for full URLs).
- Re-synced: `freightatlas2:{home,maps_gallery,freight_atlas}` now → sandbox pages; new rows
  `about_the_plan` (2175239) + `freight_map_gallery` (2175235). 3 About tickets re-keyed
  `freightatlas2:about`→`:about_the_plan`. DRY re-sync: **0 to patch**.
- Residue: stale `freightatlas2:about` row (production about, empty/green) left in place — not
  pruned (guardrail). `build_freightatlas2_freight_atlas.mjs` still targets the retired 1411761 —
  flagged in builds/README as a follow-up (regen against 2189762 or retire).

## Screenshots attached to tickets (new capability)
All 13 resolved tickets now carry an after-fix screenshot in their `screenshot` column (renders as a
link on the ticket detail page). Uploaded via `POST /dms-admin/npmrdsv5/file_upload` (server →
public `.avif` `dl_url`, source **2193919** "Freight Atlas QA screenshots"). Made repeatable +
part of the process: new tool **`qa_skills/tools/attach_screenshot.mjs`** (`--ticket --image
--caption`), wired into `qa-fix-ticket.md` (step 3, on Resolve) and `qa-assess-page.md` (T3, on
file). Optional future polish: render `screenshot` as an inline image (currently a link) — a
`build_cr_tickets.mjs` column change (`type:image`).

## Verify-after / follow-ups
- Client verifies at the ticket URLs (`//freightatlas2.devtny.org/freight_atlas?layers=…`).
- Reconcile the build custody (2189762) and decide the fate of the stale `about` row +
  `freight_map_gallery`.
