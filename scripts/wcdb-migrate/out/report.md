# WCDB migration — dry run

Generated by `transform.mjs`. Publishes nothing.

## djs
- 891 rows in, 891 out (nothing dropped — the roster is an archive)
- dropped 21 near-empty columns: `created`, `twitter`, `yahoo_id`, `phone_ext`, `phone_ext_2`, `heros`, `msn_id`, `date_of_birth`, `phone_number_2`, `im`, `fav_books`, `occupation`, `like_to_meet`, `myspace`, `about_me`, `hometown`, `interests`, `fav_magazines`, `fav_web_sites`, `fav_places_to_hang_out`, `gender`
- also dropped: `student_status` (codes 1–5, no recorded meaning — decision 2026-08-13: leave it behind, the legacy source keeps it), `playlists`, `isValid`, `fav_movie`, `fav_tv_show`
- status: 84 current / 807 alumni
- emails nulled because they read "n/a": 2

## shows
- 740 usable legacy rows → **705 shows** (35 duplicates collapsed on name+dj)
- dropped 29 corrupt rows (non-numeric `schedule_id`; 23 entirely empty, the rest description fragments from a CSV comma failure)
- `decription` → `description`
- **needs triage, migrated as-is:** 102 rows named the placeholder "Show Name", 54 with no name at all

## schedule
- **69 airings** — legacy rows carrying both a start time *and* a day
- day names → 0–6 (0 = Monday, matching the admin grid); 12-hour times → HH:MM
- overnights kept as `end <= start`; `end_day` dropped
- 37 rows carry a time but **no day** — left unscheduled in `shows` for the editor to place, not guessed
- 0 unparseable times — every day-bearing row converted cleanly
- overnight airings: 7

## events
- 3 rows seeded (no legacy source; the admin is the system of record)

## department vocabulary — NINE departments
`Hip-Hop/R&B`, `World`, `Rock`, `Metal`, `Jazz`, `Electronic`, `News`, `Sports`, `Specialty`

Mapped: `Alternative Rock`→`Rock`, `R.P.M. (Electronic Music)`→`Electronic`, `Metal/Hardcore`→`Metal`, `Urban`→`Hip-Hop/R&B`, `Talk`→`Specialty`, `Retro`→`Specialty`, `Folk`→`Specialty`, `Inspirational`→`Specialty`, `Street Team`→`Specialty`

Every value maps. 0 rows outside the vocabulary.

## primary keys
- `djs.dj_id` — 891 rows, 0 null, 0 duplicate → **OK**
- `shows.show_id` — 705 rows, 0 null, 0 duplicate → **OK**
- `schedule.airing_id` — 69 rows, 0 null, 0 duplicate → **OK**
- `events.event_id` — 3 rows, 0 null, 0 duplicate → **OK**
- referential: 0 orphan airings (schedule.show_id → shows.show_id)
